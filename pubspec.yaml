name: agenda
description: "Privacy-first personal HQ for tasks and finances"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: '>=3.38.1'

dependencies:
  # isar_community = community fork of abandoned isar package (Apr 2023)
  bloc: 9.2.0
  equatable: 2.0.8
  flutter:
    sdk: flutter
  flutter_bloc: 9.1.1
  flutter_localizations:
    sdk: flutter
  get_it: 9.2.1
  go_router: 17.2.0
  injectable: 2.7.1+4
  intl: 0.20.2
  isar_community: 3.3.2
  isar_community_flutter_libs: 3.3.2
  path_provider: 2.1.5
  shared_preferences: 2.5.5
  # --- Phase 3 (Finance Charts) — uncomment when Phase 3 starts ---
  # fl_chart: 1.2.0
  # --- Phase 4 (Notifications + Backup) — uncomment when Phase 4 starts ---
  # csv: 8.0.0
  # file_picker: 11.0.2
  # flutter_local_notifications: 21.0.0
  # flutter_timezone: 5.0.2
  # timezone: 0.11.0
  # --- Phase 5 (App Lock) — uncomment when Phase 5 starts ---
  # flutter_screen_lock: 9.2.2+2
  # flutter_secure_storage: 10.0.0
  # local_auth: 3.0.1

dev_dependencies:
  bloc_test: 10.0.0
  build_runner: ^2.13.1
  flutter_lints: 6.0.0
  flutter_test:
    sdk: flutter
  injectable_generator: 2.12.1
  isar_community_generator: 3.3.2
  mocktail: 1.0.5
  very_good_analysis: 10.2.0

flutter:
  uses-material-design: true
  generate: true
