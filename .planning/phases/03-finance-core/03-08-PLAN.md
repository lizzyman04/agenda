---
phase: 03-finance-core
plan: 08
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - lib/presentation/tasks/widgets/detail/task_detail_finance_chip.dart
  - lib/presentation/tasks/widgets/detail/README.md
  - test/presentation/tasks/task_detail_screen_test.dart
autonomous: true
requirements: [FIN-05, FIN-07]

must_haves:
  truths:
    - "A task linked to a debt shows 'Ligado a Empréstimo Joao' on the task detail screen — the debt's title, not 'Ligado a Dívidas #1'."
    - "A task linked to a savings goal shows that goal's title on the task detail screen."
    - "A task linked to an entity that no longer exists still renders the chip (falling back to the kind label plus the id) instead of crashing or vanishing."
    - "A task with no finance link renders no chip at all, exactly as before."
  artifacts:
    - path: "lib/presentation/tasks/widgets/detail/task_detail_finance_chip.dart"
      provides: "Finance-link chip that resolves the linked goal/debt title"
      contains: "loadFinanceLinks"
    - path: "test/presentation/tasks/task_detail_screen_test.dart"
      provides: "Regression tests for the resolved name and the unresolved fallback"
  key_links:
    - from: "lib/presentation/tasks/widgets/detail/task_detail_finance_chip.dart"
      to: "loadFinanceLinks in lib/presentation/tasks/form/task_form_logic.dart"
      via: "getIt<GoalRepository>() + getIt<DebtRepository>() resolved in initState"
      pattern: "loadFinanceLinks\\("
    - from: "test/presentation/tasks/task_detail_screen_test.dart"
      to: "GetIt"
      via: "mock GoalRepository/DebtRepository registered in setUp, reset in tearDown"
      pattern: "registerSingleton<(Goal|Debt)Repository>"
---

<objective>
Close UAT test 9 (severity: minor). The task detail screen's finance-link chip
renders `Ligado a Dívidas #1` — the entity kind plus a raw Isar id — instead of
the linked entity's name (`Ligado a Empréstimo Joao`). The link itself is correct
and persisted; the task FORM already displays the proper name.

Root cause (already diagnosed in 03-UAT.md, do not re-diagnose): the chip
interpolates `'#${item.linkedGoalId ?? item.linkedDebtId}'` directly and never
loads the goal/debt entity to resolve a name. Note the UAT's line reference
(`task_detail_screen.dart:303`) is STALE — Phase 06 extracted the chip into
`lib/presentation/tasks/widgets/detail/task_detail_finance_chip.dart`, where the
interpolation now sits at line 32.

Purpose: the same lookup the form already performs, applied to the read surface,
so a task linked to a goal (FIN-05) or a debt (FIN-07) names what it is linked to.
Output: the chip reuses the existing `loadFinanceLinks` helper — no new lookup
logic, no new repository method, no new l10n key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-finance-core/03-UAT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase; use directly. -->

lib/presentation/tasks/form/task_form_logic.dart (ALREADY EXISTS — reuse):
  Future<FinanceLinksSnapshot> loadFinanceLinks(
    GoalRepository goalRepo, DebtRepository debtRepo,
    {int? linkedGoalId, int? linkedDebtId});
  // Loads active goals + debts, and resolves the title for whichever id was
  // passed. A missing entity leaves the corresponding title null.

lib/presentation/tasks/form/task_form_fields_model.dart:
  class FinanceLinksSnapshot {
    final List<SavingsGoal> activeGoals;
    final List<Debt> activeDebts;
    final String? linkedGoalTitle;
    final String? linkedDebtTitle;
  }

lib/presentation/tasks/form/screens/task_form_screen.dart (THE PATTERN TO MIRROR,
initState, lines 47-54):
  loadFinanceLinks(
    getIt<GoalRepository>(),
    getIt<DebtRepository>(),
    linkedGoalId: _f.linkedGoalId,
    linkedDebtId: _f.linkedDebtId,
  ).then((s) {
    if (mounted) setState(() => _f.applySnapshot(s));
  });

lib/domain/tasks/item.dart:
  final int? linkedGoalId;
  final int? linkedDebtId;

lib/domain/finance/debt/debt.dart:
  const Debt({required this.id, required this.title, required this.amountCents,
    required this.direction, required this.counterparty, required this.dueDate,
    required this.isPaid, required this.createdAt, required this.updatedAt,
    this.paidAt, this.deletedAt});

lib/domain/finance/goal/savings_goal.dart:
  const SavingsGoal({required this.id, required this.title,
    required this.targetAmountCents, required this.contributions,
    required this.isCompleted, required this.createdAt, required this.updatedAt,
    this.deadline, this.deletedAt});

Existing l10n keys (lib/config/l10n/) — no new key is needed:
  linkedTo       → "Ligado a"  / "Linked to"
  goalsTabLabel  → "Objetivos" / "Goals"
  debtsTabLabel  → "Dívidas"   / "Debts"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the linked goal/debt title in the finance-link chip</name>
  <files>
    lib/presentation/tasks/widgets/detail/task_detail_finance_chip.dart,
    lib/presentation/tasks/widgets/detail/README.md
  </files>
  <read_first>
    - lib/presentation/tasks/widgets/detail/task_detail_finance_chip.dart (the file being modified — the raw-id interpolation is at line 32)
    - lib/presentation/tasks/form/screens/task_form_screen.dart (initState lines 47-54: the exact `loadFinanceLinks` + `getIt` + `if (mounted) setState` pattern to mirror)
    - lib/presentation/tasks/form/task_form_logic.dart (`loadFinanceLinks` signature and its null-title behaviour for a missing entity)
    - lib/presentation/tasks/form/task_form_fields_model.dart (lines 11-23: `FinanceLinksSnapshot` fields)
    - lib/presentation/tasks/widgets/detail/task_detail_info_cards.dart (the caller — it places the chip unconditionally and must keep working unchanged)
    - lib/presentation/tasks/widgets/detail/README.md (its `## Files` table carries per-file line counts, and its `## Conventions in this slice` list currently asserts "No cubit, no repository" for this directory — that claim becomes false and must be amended)
  </read_first>
  <action>
    Convert `TaskDetailFinanceChip` from `StatelessWidget` to `StatefulWidget`
    so it can resolve the linked entity's title asynchronously, reusing the
    helper the task form already calls.

    Keep the public constructor signature exactly as it is —
    `const TaskDetailFinanceChip({required this.item, super.key})` — so
    `task_detail_info_cards.dart` needs no change.

    In the `State`: hold `String? _linkedTitle`. In `initState`, return early
    without any repository call when both `widget.item.linkedGoalId` and
    `widget.item.linkedDebtId` are null (the unlinked case must stay
    zero-cost — the chip is placed unconditionally on every task detail
    screen). Otherwise call
    `loadFinanceLinks(getIt<GoalRepository>(), getIt<DebtRepository>(),
    linkedGoalId: widget.item.linkedGoalId, linkedDebtId:
    widget.item.linkedDebtId)` and, in a `.then((s) { ... })`, guard with
    `if (mounted)` before
    `setState(() => _linkedTitle = s.linkedGoalTitle ?? s.linkedDebtTitle);` —
    the same `.then` + `mounted` shape `TaskFormScreen.initState` uses.

    In `build`, keep the early `SizedBox.shrink()` return for the unlinked case,
    keep the `Padding`/`Wrap`/`ActionChip` structure, the `Icons.link` avatar,
    the `cs.secondaryContainer` background and the empty `onPressed` with its
    "deep-link routing deferred" comment. Change only the label text: it becomes
    `'${l10n.linkedTo} ${_linkedTitle ?? '$kindLabel #${item.linkedGoalId ??
    item.linkedDebtId}'}'`. That is, the resolved title when there is one, and
    the CURRENT string (kind label plus raw id) as the fallback for the first
    frame before the future resolves and for an entity that has since been
    deleted. Add an English comment saying the fallback is deliberate — it is
    the degraded case, not the normal one.

    Imports to add: `package:agenda/config/di/injection.dart`,
    `package:agenda/domain/finance/goal/goal_repository.dart`,
    `package:agenda/domain/finance/debt/debt_repository.dart`,
    `package:agenda/presentation/tasks/form/task_form_logic.dart`. Do NOT add a
    new repository method, do NOT write a second lookup function, and do NOT add
    an l10n key — `linkedTo`, `goalsTabLabel` and `debtsTabLabel` already exist
    in all three ARB files.

    Update `lib/presentation/tasks/widgets/detail/README.md`:
    - refresh the `Lines` value for `task_detail_finance_chip.dart` (currently
      44) to its real post-change count;
    - extend that row's Role cell to say the chip resolves the linked entity's
      title via `loadFinanceLinks`;
    - amend the `## Conventions in this slice` bullet that currently reads
      "**Purely presentational.** No cubit, no repository, no `Navigator`
      calls" — it is no longer true. Record `task_detail_finance_chip.dart` as
      the one documented exception (it resolves two repositories through
      `getIt` in `initState` because the chip's whole job is naming an entity
      that lives in another aggregate), mirroring how
      `presentation/finance/widgets/README.md` documents
      `transaction/transaction_form_submit.dart` as its deliberate exception.

    The file must stay at or under 150 lines (architecture guard); it is 44 now,
    so there is ample room. The directory holds 9 hand-written `.dart` files
    against a 10-file cap — this task adds no file, so do not split anything out.
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos --fatal-warnings && dart run tool/check_architecture.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "loadFinanceLinks" lib/presentation/tasks/widgets/detail/task_detail_finance_chip.dart` returns one match.
    - `grep -n "StatefulWidget" lib/presentation/tasks/widgets/detail/task_detail_finance_chip.dart` returns one match; `grep -n "StatelessWidget"` on the same file returns none.
    - `grep -n "if (mounted)" lib/presentation/tasks/widgets/detail/task_detail_finance_chip.dart` returns at least one match, guarding the `setState`.
    - `grep -n "const TaskDetailFinanceChip({required this.item" lib/presentation/tasks/widgets/detail/task_detail_finance_chip.dart` still matches — the caller's construction is unchanged.
    - `git diff --name-only` does NOT list `lib/presentation/tasks/widgets/detail/task_detail_info_cards.dart` or any ARB file under `lib/config/l10n/`.
    - `flutter analyze --no-fatal-infos --fatal-warnings` exits 0 with no more than 65 infos.
    - `dart run tool/check_architecture.dart` exits 0.
    - `awk 'END{print NR}' lib/presentation/tasks/widgets/detail/task_detail_finance_chip.dart` is ≤ 150 and equals the `Lines` value in that directory's README row for the file.
    - The README no longer claims the directory contains no repository access; it names `task_detail_finance_chip.dart` as the documented exception.
    - `flutter test --no-pub test/presentation/tasks/task_detail_screen_test.dart` is EXPECTED to fail after this task (the chip now resolves `getIt<GoalRepository>()` with nothing registered) — Task 2 repairs it. Do not weaken this task to avoid that.
  </acceptance_criteria>
  <done>The chip resolves the linked goal or debt title via `loadFinanceLinks` and falls back to the kind label plus id only when the entity cannot be resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Register the finance repositories in the detail-screen test and assert the resolved name</name>
  <files>test/presentation/tasks/task_detail_screen_test.dart</files>
  <read_first>
    - test/presentation/tasks/task_detail_screen_test.dart (the file being modified — its first test builds an `Item` with `linkedGoalId: 7` and asserts `find.byIcon(Icons.link)`)
    - test/presentation/tasks/task_form_test.dart (lines 22-27 and 71-85: the exact `MockGoalRepository`/`MockDebtRepository` declarations, the `getIt..registerSingleton<...>` setUp and the `await getIt.reset()` tearDown to copy)
    - lib/presentation/tasks/widgets/detail/task_detail_finance_chip.dart (post-Task-1 widget under test)
    - lib/domain/finance/debt/debt.dart and lib/domain/finance/goal/savings_goal.dart (constructor arguments for the fixtures)
  </read_first>
  <action>
    The chip now resolves two repositories through `getIt` in `initState`, so
    every test that mounts `TaskDetailScreen` needs them registered. Follow the
    precedent `task_form_test.dart` already set for exactly this reason.

    Add `class MockGoalRepository extends Mock implements GoalRepository {}` and
    `class MockDebtRepository extends Mock implements DebtRepository {}` to
    `test/presentation/tasks/task_detail_screen_test.dart`, with a comment
    explaining that `TaskDetailFinanceChip` resolves them via `getIt` in
    `initState` to name the linked goal/debt.

    In the existing `setUp`, instantiate both, stub
    `when(goalRepo.getActiveGoals).thenAnswer((_) async => const
    Success(<SavingsGoal>[]))` and `when(debtRepo.getDebts).thenAnswer((_) async
    => const Success(<Debt>[]))` as the defaults, and register them with
    `getIt..registerSingleton<GoalRepository>(goalRepo)
    ..registerSingleton<DebtRepository>(debtRepo)`. Add a `tearDown` with
    `await getIt.reset();`. Import `package:agenda/config/di/injection.dart`,
    `package:agenda/core/failures/result.dart`, and the goal/debt domain +
    repository files. The two existing tests must keep passing unchanged — with
    empty repositories the `linkedGoalId: 7` item falls back to the kind label
    plus id, so `find.byIcon(Icons.link)` is still `findsOneWidget`.

    Add two new `testWidgets` cases inside the same `group`:

    `'finance chip names the linked debt instead of its raw id'` — build an
    `Item` with `linkedDebtId: 1` (and `linkedGoalId` left null), stub
    `debtRepo.getDebts` to return `Success([Debt(id: 1, title: 'Empréstimo Joao',
    counterparty: 'Joao', ...)])`, pump the widget and then `pumpAndSettle` so
    the `loadFinanceLinks` future resolves. Assert `find.text('Linked to
    Empréstimo Joao')` is `findsOneWidget` (the test harness loads the `en`
    locale, so `l10n.linkedTo` is "Linked to" — prefer `l10n.linkedTo` in the
    expected string over a hardcoded literal) and assert
    `find.textContaining('#1')` is `findsNothing`. This is the assertion that
    fails against the pre-Task-1 chip.

    `'finance chip falls back to the kind label and id when the entity is
    gone'` — same item, repositories returning empty lists; assert
    `find.byIcon(Icons.link)` is `findsOneWidget`, that the chip text contains
    `'#1'`, and that no exception was thrown (`tester.takeException()` returns
    null).

    Give the new cases a comment naming UAT test 9 as the defect they guard. All
    test code and comments in English; the fixture title stays PT-BR because that
    is the string the UAT reported.
  </action>
  <verify>
    <automated>flutter test --no-pub test/presentation/tasks/task_detail_screen_test.dart && flutter test --no-pub && flutter analyze --no-fatal-infos --fatal-warnings && dart run tool/check_architecture.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test --no-pub test/presentation/tasks/task_detail_screen_test.dart` exits 0 with 5 tests passing (3 existing + 2 new).
    - `grep -n "registerSingleton<GoalRepository>\|registerSingleton<DebtRepository>" test/presentation/tasks/task_detail_screen_test.dart` returns both.
    - `grep -n "getIt.reset()" test/presentation/tasks/task_detail_screen_test.dart` returns at least one match inside a `tearDown`.
    - The debt-name test asserts `find.textContaining('#1')` is `findsNothing`.
    - `grep -rn "mockito\|package:provider\|package:get/" test/presentation/tasks/task_detail_screen_test.dart` returns no matches.
    - `flutter test --no-pub` exits 0 with at least 270 tests passing (268 baseline + 2 added here; higher if plans 03-06/03-07 have already merged).
    - `flutter analyze --no-fatal-infos --fatal-warnings` exits 0 with no more than 65 infos.
    - `dart run tool/check_architecture.dart` exits 0.
    - Temporarily reverting Task 1's label expression to the raw `'#${item.linkedGoalId ?? item.linkedDebtId}'` form makes the debt-name test fail; restore before finishing.
  </acceptance_criteria>
  <done>The detail-screen test registers the finance repositories, the existing three tests still pass, and two new tests pin both the resolved-name and the fallback paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Isar (finance aggregate) → tasks presentation | Goal and debt titles are user-authored text read across an aggregate boundary and rendered in the task detail UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Information disclosure | `TaskDetailFinanceChip` label | accept | Goal/debt titles are device-local and already shown on the finance tab and in the task form to the same single user; no new exposure, no network, no logging. |
| T-03-08-02 | Denial of service | `loadFinanceLinks` in `initState` | mitigate | The call is skipped entirely when the task carries no link; on a missing entity `loadFinanceLinks` leaves the title null and the chip renders the `#id` fallback. The `if (mounted)` guard prevents `setState` after dispose. Both paths covered by the Task 2 tests. |
| T-03-08-03 | Elevation of privilege | `getIt` resolution from a leaf widget | accept | GetIt is process-local, populated only by `configureDependencies()` from the app's own injectable graph; no external code can register a substitute at runtime on a mobile-only offline app. |
</threat_model>

<verification>
- `dart run tool/check_architecture.dart` exits 0 — including the 10-file cap on `lib/presentation/tasks/widgets/detail/` (still 9 files) and the 150-line cap.
- `flutter analyze --no-fatal-infos --fatal-warnings` exits 0, no more than 65 infos.
- `flutter test --no-pub` exits 0 with no regression against the 268-test baseline.
- No ARB file changed, no new l10n key added, no new package in `pubspec.yaml`.
- `lib/presentation/tasks/widgets/detail/README.md` line counts match reality and its conventions list no longer contains a false claim.
</verification>

<success_criteria>
1. A task linked to a debt titled "Empréstimo Joao" shows "Ligado a Empréstimo Joao" on the task detail screen.
2. A task linked to a savings goal shows that goal's title by the same path.
3. A task whose linked entity no longer exists still renders the chip with the kind label and id, and throws nothing.
4. A task with no finance link renders no chip and performs no repository call.
5. All architecture/analyze/test gates pass with no regression.
</success_criteria>

<output>
After completion, create `.planning/phases/03-finance-core/03-08-SUMMARY.md`.
</output>
