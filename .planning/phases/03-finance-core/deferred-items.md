# Deferred items — phase 03-finance-core

Out-of-scope discoveries logged during execution. Not fixed, by scope rule.

| Found in | File | Item |
|---|---|---|
| 03-10 | `lib/presentation/finance/widgets/debt/debt_card.dart:101` | The paid-status `SwitchListTile` title is a hardcoded PT-BR literal — `debt.isPaid ? 'Pago' : 'Pendente'`. It renders "Pago"/"Pendente" even under the `en` locale. Pre-existing, untouched by CR-01, and belongs to the hardcoded-string defect class rather than the undo path. Needs `paid` / `pending` keys in all three ARB files. |
| 03-11 | `lib/presentation/finance/widgets/recurring/recurring_payment_schedule_fields.dart:80` and `recurring_payment_card.dart:62` | The shared `cycleLabel()` returns hardcoded PT-BR (`'Diário'`, `'Mensal'`, …) and the card's subtitle hardcodes the `'Próximo:'` prefix, so both stay Portuguese under `en`. Same defect class as the `debtDeleted` row above, but wider: 6 cycle keys plus one prefix, in three ARB files, affecting the recurring form as well as the list card. 03-11 localized only the paused/active toggle label it was scoped to. |
| 03-10 | `lib/presentation/finance/screens/transaction_list_screen.dart:46,54` | Two analyzer infos (`discarded_futures`, `cascade_invocations`) inside `_handleDelete`, both counted in the 65-info baseline. The debt equivalent written in 03-10 avoids them via `unawaited(...)` and a `messenger` cascade; the transaction original could take the same two-line treatment and drop the baseline to 63. |
| 03-14 | `lib/data/finance/goal/savings_goal_dao.dart` `findAll()` and `lib/data/finance/debt/debt_dao.dart` `findAll()` | **Same defect class as BL-01, two more live instances.** Both carry `.limit(500)` with no `sortBy` and feed money totals rather than display lists: `getActiveGoals()` → `computeGoalsSavedTotal` (a `.fold` sum) and `getDebts()` → `computeDebtTotal`, both summed into `computeNetWorth` in `dashboard_aggregator.dart`. Isar returns rows in insertion order, so past 500 rows the net-worth figure silently reads wrong, exactly as budget spend and goal progress did. Net worth's balance input is unaffected — it comes from the uncapped `findAllForAggregates`. Judged non-blocking by both the code review (IN-03) and the verifier because 500+ active savings goals or 500+ active debts is not realistically reachable, unlike 500+ transactions. Fix is the same one-line uncap plus a harness-based regression test; `test/support/isar_test_harness.dart` now exists to make that cheap. |
