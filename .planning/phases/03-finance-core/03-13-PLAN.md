---
phase: 03-finance-core
plan: 13
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - test/support/isar_test_harness.dart
  - test/support/isar_test_harness_test.dart
  - tool/warm_isar_core.dart
  - .github/workflows/ci.yml
autonomous: true
requirements: [FIN-04, FIN-06]

must_haves:
  truths:
    - "A flutter test process can open a real isar_community Isar instance against an isolated temporary directory and close it cleanly, leaving no file behind."
    - "The Isar Core native binary acquisition route is an explicit, documented decision with a stated rationale and a stated residual risk — not an implicit default."
    - "The concurrent-download race in isar_community's core-download path (two flutter test worker processes writing the same native-library file at once) is mitigated in CI by a synchronous pre-warm step that runs before the parallel test fan-out — not merely documented as a risk."
    - "The harness is one shared, reusable helper — not something later plans copy-paste per test file."
  artifacts:
    - path: "test/support/isar_test_harness.dart"
      provides: "IsarTestHarness — open()/close() wrapping Isar.initializeIsarCore(download: true) plus a per-instance temp directory; doc comment names both the no-network risk and the concurrent-download race"
      contains: "initializeIsarCore"
    - path: "test/support/isar_test_harness_test.dart"
      provides: "Self-test proving the harness opens a real Isar, writes, reads back, and tears down with no leftover directory"
    - path: "tool/warm_isar_core.dart"
      provides: "Standalone script that calls Isar.initializeIsarCore(download: true) once, synchronously, so the binary is cached before flutter test's parallel workers start"
      contains: "initializeIsarCore"
    - path: ".github/workflows/ci.yml"
      provides: "A pre-warm step calling tool/warm_isar_core.dart, positioned before the Test step"
      contains: "warm_isar_core"
  key_links:
    - from: "test/support/isar_test_harness_test.dart"
      to: "test/support/isar_test_harness.dart"
      via: "IsarTestHarness().open([TransactionModelSchema])"
      pattern: "IsarTestHarness"
    - from: ".github/workflows/ci.yml"
      to: "tool/warm_isar_core.dart"
      via: "a CI step running before the Test step, single-process, before flutter test's -j fan-out"
      pattern: "warm_isar_core"
---

<objective>
Build the one piece of test infrastructure this project has never had: a way to open a
**real** `isar_community` instance inside `flutter test`, against an isolated temp
directory, and tear it down cleanly.

Why this cannot be skipped: `.limit(500)`-without-`sortBy` has now been the root cause of
three Critical findings in this phase (CR-04, and the two `BL-01` gaps this fixes next in
03-14). Every regression test written against this defect class so far
(`transaction_dao_ordering_test.dart`) asserts on DAO **source text**
(`File(path).readAsStringSync()` + substring match), not on query **behavior** — filed as
WR-D6 in `03-REVIEW.md`, which states plainly: these tests "pass unchanged whether the bug
is present or not." A mocked repository cannot substitute either, because `.limit(500)`
lives in the DAO, below the repository layer that gets mocked. The only fix is a real Isar
instance in the test process.

**The user has locked this decision: build the real-Isar harness.** This is not being
re-evaluated here.

**A second decision this plan must also settle, surfaced by plan-checker revision:**
`isar_community` 3.3.2's own core-download path (`lib/src/native/isar_core.dart`,
`_downloadIsarCore`) is a bare check-then-act with no lock file and no atomic
temp-then-rename — it checks whether the destination file exists, and if not, streams the
download straight to that final path. Two OS processes racing that write can corrupt or
partially write the native library both then try to load via `DynamicLibrary.open()`. This
is not theoretical: `flutter test` runs test files as **separate concurrent OS processes**
by default (`-j`/`--concurrency`), this project's CI (`.github/workflows/ci.yml`) runs
`flutter test --no-pub --coverage` unqualified on a fresh `ubuntu-latest` runner with zero
cached binary every push, and after 03-14 lands there will be two files that call into this
harness — enough for the race to trigger on essentially every CI run. The harness's own
per-process `_coreInitialized` guard cannot fix this, because that guard is per-process and
the race is cross-process. This plan's mitigation (pre-warm the cache in a single
synchronous CI step before the parallel test fan-out starts) is detailed in Task 1.

Purpose: give 03-14 (and any future DAO test) a genuine behavioral foundation instead of
another source-text assertion — and make sure that foundation is actually reliable in CI,
not just on a single developer's warm cache.
Output: `test/support/isar_test_harness.dart` (the reusable helper),
`test/support/isar_test_harness_test.dart` (proof it works), `tool/warm_isar_core.dart`
(the CI pre-warm script), and a `.github/workflows/ci.yml` step that runs it before `Test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-finance-core/03-VERIFICATION.md
@.planning/phases/03-finance-core/03-REVIEW.md

<interfaces>
lib/data/database/isar_service.dart:
  class IsarService {
    Isar get db;                                            // throws if not opened
    Future<void> open(List<CollectionSchema<dynamic>> schemas);
    Future<void> close();
  }
  // Production shape: Isar.open(schemas, directory: dir.path) against the app's
  // documents directory. The harness mirrors the schemas argument but opens
  // against an isolated temp directory instead, and returns the raw Isar
  // instance directly rather than wrapping it in IsarService (IsarService has
  // a private constructor and calls path_provider, which is unavailable/
  // unnecessary in a headless unit test).

lib/data/finance/transaction/transaction_model.dart (+ transaction_model.g.dart):
  @Collection() class TransactionModel { Id id; ... }
  // Generates: TransactionModelSchema — reuse this existing schema for the
  // harness self-test. Do NOT create a new @Collection model for this plan;
  // that would require running build_runner, which is out of scope here.

pubspec.yaml:
  isar_community: 3.3.2
  isar_community_flutter_libs: 3.3.2
  // isar_community is the drop-in-API fork of the abandoned `isar` package.
  // Its static Isar.initializeIsarCore({bool download}) method is the
  // documented route for opening Isar Core outside a running Flutter app
  // (e.g. inside `flutter test`, which runs on the host VM, not a device/
  // emulator with the plugin's bundled native binary already present).

.github/workflows/ci.yml (current step order, single job `analyze-and-test`):
  Checkout -> Setup Flutter (cache: true, SDK only, NOT the isar-core download
  path) -> Install dependencies (flutter pub get) -> Run code generation ->
  Generate l10n -> Analyze -> Architecture guard -> Test (flutter test --no-pub
  --coverage, unqualified concurrency) -> Verify offline guarantee.
  // The new pre-warm step must land strictly before "Test". Any point after
  // "Install dependencies" (isar_community must be resolved) is acceptable;
  // placing it immediately before "Test" is clearest.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build IsarTestHarness, decide the binary route, and pre-warm CI against the download race</name>
  <files>
    test/support/isar_test_harness.dart,
    tool/warm_isar_core.dart,
    .github/workflows/ci.yml
  </files>
  <read_first>
    - test/data/database/migration_runner_test.dart (confirms the current state: a `FakeIsar extends Fake implements Isar` — no real Isar Core is ever initialized anywhere in this test suite today)
    - test/data/tasks/item_dao_test.dart (the empty `group('ItemDao', () {})` stub this harness will eventually be able to fill — not filled by this plan)
    - lib/data/database/isar_service.dart (the production `Isar.open(schemas, directory: dir.path)` call shape to mirror)
    - lib/data/finance/transaction/transaction_model.dart (existing `@Collection` model whose generated `TransactionModelSchema` the Task 2 self-test will reuse)
    - pubspec.yaml (confirms exact dependency: `isar_community: 3.3.2`)
    - .github/workflows/ci.yml (the whole file — exact current step order and step names; the new step must be inserted correctly relative to `Install dependencies` and `Test`, and must not duplicate `flutter pub get`)
    - tool/check_architecture.dart (existing precedent for a standalone `dart run tool/*.dart` script in this repo — mirror its `void main()` / exit-code style, not its content)
  </read_first>
  <action>
    Create `test/support/isar_test_harness.dart`.

    **Decision 1 — binary acquisition route.** Record this in a top-of-file doc comment,
    explicitly and with rationale (this is the residual-risk decision the user asked to
    see stated, not silently assumed):

    Route taken: **download-on-demand via `Isar.initializeIsarCore(download: true)`**,
    called once per test process behind an idempotency guard (a private top-level
    `bool _coreInitialized` flag, or equivalent), NOT a vendored/committed native binary.

    Rationale to write into the comment: `Isar.initializeIsarCore(download: true)` is
    isar_community's own documented mechanism for exactly this situation (opening Isar
    outside a running Flutter app), requires zero repository changes, and the binary it
    fetches is cached by the package itself so only the *first* invocation on a given
    machine needs network access. A vendored binary was considered and rejected: it would
    require committing a platform-specific native library (`.so`/`.dylib`/`.dll`) obtained
    by actually running the download once — something a planning-only pass cannot do and
    which is fragile because it would silently drift from whatever `isar_community`
    version is pinned in `pubspec.yaml`.

    Residual risk to write into the comment, explicitly: on a machine or CI runner with
    no cached Isar Core binary AND no network access, the first `flutter test` run that
    touches this harness will fail at `initializeIsarCore` with a download/network error.
    This is a **dev-toolchain** limitation, not a violation of CLAUDE.md's privacy/offline
    constraint — that constraint governs the shipped app's runtime behavior, and the
    shipped app never calls this method.

    **Decision 2 — the concurrent-download race, and why pre-warming CI is the fix.**
    Write this into the same doc comment, as its own clearly headed paragraph (a future
    reader hitting a corrupted-binary failure in CI must find the explanation here, not
    have to re-derive it from `isar_community`'s source):

    Name the mechanism precisely: `isar_community` 3.3.2's `_downloadIsarCore`
    (`lib/src/native/isar_core.dart`) does a bare check-then-act — checks whether the
    destination file exists, and if not, streams the download straight to that final
    path, with no lock file and no atomic temp-file-then-rename. Two processes racing
    that write can corrupt the native library both then load via `DynamicLibrary.open()`.
    The package's own doc comment says outright to always use `flutter test -j 1` when
    relying on auto-download for exactly this reason. State plainly that this project's
    `_coreInitialized` guard is per-process and **cannot** fix a cross-process race,
    because `flutter test` runs each test file as a separate OS process by default.

    State the three options considered and the one chosen, with rationale:
    (a) pre-warm the cache with a single synchronous step before the test runner
    parallelizes — chosen; (b) pin CI to `flutter test -j 1`, serializing the whole
    297+-test suite — rejected, pays a wall-clock cost across every run to fix a race
    that only needs to happen once per machine; (c) an OS-level file lock inside
    `IsarTestHarness.open()` — rejected, a cross-process advisory lock implemented in
    Dart test code is more failure-prone and harder to verify correct at plan-writing
    time than a single ordinary sequential CI step, and it would only protect processes
    that go through this harness, not any other future `flutter test` invocation.
    State where the actual mitigation lives: `tool/warm_isar_core.dart`, run once by a
    dedicated CI step in `.github/workflows/ci.yml` **before** the `Test` step — CI steps
    within one job already run sequentially, so ordering alone serializes the download
    against the later parallel fan-out with no extra locking needed. Add a one-line local-
    dev note: a developer running `flutter test` locally with concurrency > 1 across
    multiple harness-using files, on a machine with no cached binary and without ever
    having run the pre-warm script, can still hit this race; the fix is running
    `dart run tool/warm_isar_core.dart` once, or `flutter test -j 1`, the first time.

    **Implementation — IsarTestHarness.** A class `IsarTestHarness` with two private
    fields, `Isar? _isar` and `Directory? _tempDir`, and this public surface:

    - `Isar? get isar` — the currently open instance, or null.
    - `Directory? get tempDir` — the temp directory backing the currently open instance,
      or null. Exposed so callers (and the Task 2 self-test) can assert on cleanup without
      needing to introspect the `Isar` instance's own directory API.
    - `Future<Isar> open(List<CollectionSchema<dynamic>> schemas) async` — calls the
      idempotency-guarded core-init, creates a fresh
      `Directory.systemTemp.createTempSync('agenda_isar_test_')`, opens
      `Isar.open(schemas, directory: <that dir's path>, name: <a value unique per call,
      e.g. derived from `DateTime.now().microsecondsSinceEpoch`>)` so two harness
      instances opened in the same process (parallel test files, or two tests in one
      file that both forget to close) never collide on instance name, stores both in the
      private fields, and returns the `Isar`.
    - `Future<void> close() async` — closes the instance with
      `await _isar?.close(deleteFromDisk: true)`, then defensively deletes `_tempDir` if
      it still exists (`if (_tempDir?.existsSync() ?? false) _tempDir!.deleteSync(recursive: true);`),
      then nulls both fields. Must be safe to call twice (idempotent) and safe to call
      without a prior `open()`.

    Every public member gets a one-line doc comment. All comments in English.

    **Implementation — CI pre-warm.** Create `tool/warm_isar_core.dart`: a standalone
    script (`Future<void> main() async { ... }`) that imports
    `package:isar_community/isar.dart`, calls
    `await Isar.initializeIsarCore(download: true);` exactly once, and prints a short
    confirmation line on success, following the `dart run tool/*.dart` convention already
    established by `tool/check_architecture.dart`. No test framework import needed — this
    is a plain Dart entrypoint, not a `flutter_test` file.

    Edit `.github/workflows/ci.yml`: insert a new step named `Warm Isar Core cache
    (pre-fan-out; prevents concurrent-download race — see
    test/support/isar_test_harness.dart)` immediately before the existing `Test` step
    (after `Architecture guard`), running `dart run tool/warm_isar_core.dart`. Do not
    reorder or remove any existing step.
  </action>
  <verify>
    <automated>flutter analyze --no-fatal-infos --fatal-warnings</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "initializeIsarCore" test/support/isar_test_harness.dart` returns a match.
    - `grep -n "class IsarTestHarness" test/support/isar_test_harness.dart` returns a match.
    - `grep -n "Future<Isar> open(" test/support/isar_test_harness.dart` returns a match.
    - `grep -n "Future<void> close(" test/support/isar_test_harness.dart` returns a match.
    - The file's top doc comment states, in prose a future reader can act on without
      re-deriving it: (1) the binary-acquisition decision, its rationale, and the
      no-network residual risk, AND (2) the concurrent-download race by name — citing
      `_downloadIsarCore`'s check-then-act with no lock/atomic-rename, the package's own
      `-j 1` warning, why the harness's per-process guard cannot fix a cross-process race,
      and where the actual mitigation lives (`tool/warm_isar_core.dart` + the CI step).
      A doc comment describing the race without naming the mitigation's location does
      NOT satisfy this criterion.
    - `grep -n "initializeIsarCore" tool/warm_isar_core.dart` returns a match.
    - `grep -n "Warm Isar Core" .github/workflows/ci.yml` and
      `grep -n "flutter test --no-pub" .github/workflows/ci.yml` both return a match, and
      the warm-step's line number is LESS than the `flutter test` line's line number —
      confirm this by inspecting both `grep -n` outputs directly, not by assumption.
    - `grep -c "run: flutter pub get" .github/workflows/ci.yml` is still exactly 1 (the
      new step must not duplicate dependency installation).
    - `flutter analyze --no-fatal-infos --fatal-warnings` exits 0 with no increase over the
      65-issue budget and 0 `lines_longer_than_80_chars`.
  </acceptance_criteria>
  <done>
    `IsarTestHarness` exists and compiles; its doc comment states the binary-acquisition
    decision, rationale, no-network residual risk, AND the concurrent-download race with
    its mitigation's location. `tool/warm_isar_core.dart` exists and calls
    `Isar.initializeIsarCore(download: true)`. CI runs it as a step strictly before `Test`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the harness works — open, write, read, tear down</name>
  <files>test/support/isar_test_harness_test.dart</files>
  <read_first>
    - test/support/isar_test_harness.dart (Task 1's output — read the exact `open`/`close`/`isar`/`tempDir` signatures before writing against them)
    - test/data/finance/transaction_dao_ordering_test.dart (the `_model()` helper pattern for constructing a `TransactionModel` fixture — mirror its style, do not diverge gratuitously)
    - lib/data/finance/transaction/transaction_model.dart (fields required on `TransactionModel`: `id`, `type`, `amountCents`, `categoryId`, `date`, `note`, `linkedGoalId`, plus the soft-delete field — read the full field list before constructing one)
  </read_first>
  <action>
    Create `test/support/isar_test_harness_test.dart`. One test group, using
    `IsarTestHarness` from Task 1 and the existing generated `TransactionModelSchema`
    (import from `package:agenda/data/finance/transaction/transaction_model.dart`, which
    exports the schema via its generated part file — do not add a new `@Collection`
    model for this test).

    Test 1 — "opens a real Isar, writes a row, and reads it back": construct
    `final harness = IsarTestHarness();`, call
    `final isar = await harness.open([TransactionModelSchema]);`, build one
    `TransactionModel` fixture (mirroring `_model()` in
    `transaction_dao_ordering_test.dart`), write it with
    `await isar.writeTxn(() => isar.collection<TransactionModel>().put(model));`, then
    read it back with `await isar.collection<TransactionModel>().get(model.id)` and assert
    every field round-trips (amountCents, type, categoryId, date at minimum). Call
    `addTearDown(harness.close)` right after `open()` so cleanup runs even on assertion
    failure.

    Test 2 — "close() removes the temp directory": open a harness, capture
    `final dir = harness.tempDir!;` immediately after `open()`, assert `dir.existsSync()`
    is true, call `await harness.close();`, then assert `dir.existsSync()` is false —
    this is the assertion that proves teardown actually cleans up rather than leaking a
    directory per test run.

    If `Isar.initializeIsarCore(download: true)` fails in this execution environment
    because no network is reachable and no binary is cached, do NOT work around it by
    weakening the test or skipping it silently. Record the exact failure in the SUMMARY —
    that is the residual risk Task 1's doc comment names, materializing, and the user
    needs to see it, not have it hidden.
  </action>
  <verify>
    <automated>flutter test --no-pub test/support/isar_test_harness_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TransactionModelSchema" test/support/isar_test_harness_test.dart` returns a match.
    - `flutter test --no-pub test/support/isar_test_harness_test.dart` exits 0, both tests passing.
    - `flutter test --no-pub` (full suite) exits 0 with strictly more tests passing than the 297 baseline.
    - `flutter analyze --no-fatal-infos --fatal-warnings` exits 0, budget still exactly 65 issues, 0 long lines.
    - `dart run tool/check_architecture.dart` exits 0 (this task touches no `lib/` file, so this should be a no-op pass — confirm it anyway).
  </acceptance_criteria>
  <done>
    A real `isar_community` instance opens, accepts a write, returns it on read, and
    leaves no directory behind after `close()` — proven by an executed, passing test, not
    asserted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> local filesystem (OS temp dir) | The harness opens a real Isar database file under the OS temp directory for the lifetime of a test run. |
| dev/CI machine -> network (Isar Core binary) | `Isar.initializeIsarCore(download: true)` fetches a native binary over HTTPS the first time a machine with no cached binary runs a test that opens the harness. |
| CI job -> parallel `flutter test` worker processes | Multiple test files sharing one `flutter test` invocation run as separate OS processes that can race on the same on-disk binary-download destination. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13-01 | Tampering | temp Isar db file under `Directory.systemTemp` | accept | Test-only artifact containing only synthetic fixture rows, deleted in `close()`, never read by the shipped app, never committed. No user data ever crosses this boundary. |
| T-03-13-02 | Information disclosure | downloaded Isar Core binary | accept | Fetched over HTTPS by the `isar_community` package itself from its own release infrastructure, identical in kind to every other pub.dev package's native binary; no project secrets or user data are transmitted, only an anonymous binary fetch. |
| T-03-13-03 | Denial of service | `Isar.initializeIsarCore(download: true)` in a network-isolated environment | accept | Documented residual risk (Task 1's doc comment). Surfaces as a clear, loud test-run failure, not a silent defect, and does not affect the shipped app — the production `IsarService` never calls this method. |
| T-03-13-04 | Tampering / Denial of service | `isar_community`'s `_downloadIsarCore` destination file, written concurrently by multiple `flutter test` worker processes | mitigate | `tool/warm_isar_core.dart`, run as its own CI step strictly before `Test`, downloads and caches the binary once, single-process, before `flutter test`'s parallel fan-out ever starts — eliminating the race at its cause (cold cache + concurrent fan-out) rather than only documenting it. Local `flutter test` runs without the pre-warm step retain residual exposure, called out explicitly in the harness doc comment with a `dart run tool/warm_isar_core.dart` / `flutter test -j 1` fallback. |
</threat_model>

<verification>
- `dart run tool/check_architecture.dart` exits 0 (no `lib/` file touched by this plan).
- `flutter analyze --no-fatal-infos --fatal-warnings` exits 0, exactly 65 issues, 0 long lines.
- `flutter test --no-pub` exits 0, strictly more tests passing than the 297 baseline.
- `test/support/isar_test_harness.dart`, `test/support/isar_test_harness_test.dart`, and
  `tool/warm_isar_core.dart` all exist.
- `.github/workflows/ci.yml` runs `tool/warm_isar_core.dart` in a step strictly before the
  `Test` step, and still runs `flutter pub get` exactly once.
</verification>

<success_criteria>
1. `IsarTestHarness` opens a real `isar_community` instance against an isolated temp
   directory and closes it cleanly, with no leftover files — proven by an executed test.
2. The Isar Core binary acquisition route is stated explicitly, with rationale and
   residual risk, in the harness file's own doc comment.
3. The concurrent-download race is not just documented but mitigated in CI: a single
   synchronous pre-warm step runs before `flutter test`'s parallel fan-out, so the race's
   actual precondition (cold cache + concurrent processes) never occurs in CI.
4. 03-14 (and any future DAO test) can depend on this helper directly, with zero
   copy-pasted setup, and with CI already protected against the download race their
   second (and any further) consumer file introduces.
</success_criteria>

<output>
After completion, create `.planning/phases/03-finance-core/03-13-SUMMARY.md`.
</output>
