---
phase: 03-finance-core
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lib/data/finance/transaction_model.dart
  - lib/data/finance/transaction_category_model.dart
  - lib/data/finance/budget_model.dart
  - lib/data/finance/savings_goal_model.dart
  - lib/data/finance/debt_model.dart
  - lib/data/finance/recurring_payment_model.dart
  - lib/data/finance/transaction_dao.dart
  - lib/data/finance/transaction_category_dao.dart
  - lib/data/finance/budget_dao.dart
  - lib/data/finance/savings_goal_dao.dart
  - lib/data/finance/debt_dao.dart
  - lib/data/finance/recurring_payment_dao.dart
  - lib/data/finance/finance_mappers.dart
  - lib/infrastructure/finance/transaction_repository_impl.dart
  - lib/infrastructure/finance/transaction_category_repository_impl.dart
  - lib/infrastructure/finance/budget_repository_impl.dart
  - lib/infrastructure/finance/goal_repository_impl.dart
  - lib/infrastructure/finance/debt_repository_impl.dart
  - lib/infrastructure/finance/recurring_payment_repository_impl.dart
  - lib/data/database/isar_service.dart
  - lib/data/database/migration_runner.dart
  - lib/core/config/app_config.dart
  - lib/config/di/finance_module.dart
  - lib/main.dart
  - test/data/finance/transaction_mapper_test.dart
  - test/data/finance/savings_goal_mapper_test.dart
  - test/data/database/migration_runner_test.dart
autonomous: true
requirements:
  - FIN-01
  - FIN-02
  - FIN-03
  - FIN-04
  - FIN-05
  - FIN-06
  - FIN-07
  - FIN-08

must_haves:
  truths:
    - "App launches with all 6 finance Isar collections open alongside ItemModelSchema"
    - "MigrationRunner case 3 seeds exactly 9 expense + 4 income default categories idempotently"
    - "Transaction mapper round-trip preserves amountCents exactly (no floating-point corruption)"
    - "SavingsGoal mapper correctly handles embedded List<GoalContribution> initialized as growable"
    - "All repository impls return Err(ValidationFailure) when amountCents <= 0, never throw"
    - "FinanceModule registered in DI graph; injection.config.dart regenerated"
  artifacts:
    - path: "lib/data/finance/transaction_model.dart"
      provides: "TransactionModel @Collection with amountCents int and @Enumerated enums"
      contains: "@Collection"
    - path: "lib/data/finance/savings_goal_model.dart"
      provides: "SavingsGoalModel with embedded List<GoalContribution> = List.empty(growable: true)"
      contains: "List.empty(growable: true)"
    - path: "lib/data/database/migration_runner.dart"
      provides: "case 3 block — seeds default categories"
      contains: "case 3"
    - path: "lib/core/config/app_config.dart"
      provides: "schemaVersion bumped to 3"
      contains: "schemaVersion = 3"
    - path: "lib/config/di/finance_module.dart"
      provides: "Populated FinanceModule with all DAOs and mappers"
      contains: "TransactionDao"
    - path: "lib/main.dart"
      provides: "IsarService.open includes all 6 finance schemas"
      contains: "TransactionModelSchema"
  key_links:
    - from: "lib/data/database/migration_runner.dart"
      to: "lib/data/finance/transaction_category_model.dart"
      via: "isar.transactionCategoryModels.count() guard + putAll()"
      pattern: "transactionCategoryModels"
    - from: "lib/main.dart"
      to: "lib/data/finance/*_model.dart"
      via: "IsarService.instance.open([...*Schema])"
      pattern: "TransactionModelSchema"
    - from: "lib/infrastructure/finance/transaction_repository_impl.dart"
      to: "lib/domain/finance/transaction_repository.dart"
      via: "@LazySingleton(as: TransactionRepository)"
      pattern: "@LazySingleton"
---

<objective>
Build the complete finance data layer: six Isar `@Collection` models with correct enum annotations and soft-delete patterns, six DAOs following the established item_dao pattern, a finance_mappers.dart with all five mapper classes, five repository implementations wired to domain interfaces, AppConfig.schemaVersion bumped to 3, MigrationRunner case 3 for category seeding, IsarService.open updated, and FinanceModule populated.

Purpose: Establishes all persistence contracts consumed by the application layer in plan 03-03. After this plan, finance data can be saved and retrieved from Isar.
Output: 26 modified/created Dart source files. Run `flutter pub run build_runner build --delete-conflicting-outputs` after creating all @Collection files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-finance-core/03-CONTEXT.md
@.planning/phases/03-finance-core/03-RESEARCH.md
@.planning/phases/03-finance-core/03-PATTERNS.md
@.planning/phases/03-finance-core/03-01-SUMMARY.md

<interfaces>
<!-- Contracts from existing codebase that this plan extends. -->

From lib/data/tasks/item_model.dart (Isar @Collection pattern):
```dart
import 'package:isar_community/isar.dart';
part 'item_model.g.dart';

@Collection()
class ItemModel {
  Id id = Isar.autoIncrement;
  @Index(composite: [CompositeIndex('deletedAt')])
  @Enumerated(EnumType.name)
  late ItemType type;           // enum persisted as string
  @Index()
  DateTime? deletedAt;          // soft delete — all active queries filter .deletedAtIsNull()
  late DateTime createdAt;
  late DateTime updatedAt;
}
```

From lib/data/tasks/item_dao.dart (DAO pattern):
```dart
class ItemDao {
  const ItemDao(this._isarService);
  final IsarService _isarService;
  IsarCollection<ItemModel> get _collection =>
      _isarService.db.collection<ItemModel>();

  Future<List<ItemModel>> findAll() async => _collection
      .filter()
      .deletedAtIsNull()
      .limit(500)
      .findAll();

  Future<int> save(ItemModel model) async =>
      _isarService.db.writeTxn(() => _collection.put(model));

  Future<void> softDelete(int id) async {
    await _isarService.db.writeTxn(() async {
      final model = await _collection.get(id);
      if (model == null) return;
      model.deletedAt = DateTime.now();
      await _collection.put(model);
    });
  }

  Stream<void> watchLazy() =>
      _isarService.db.collection<ItemModel>().watchLazy();
}
```

From lib/data/tasks/item_mapper.dart (mapper pattern):
```dart
class ItemMapper {
  const ItemMapper();
  Item toDomain(ItemModel model) { /* field-by-field mapping */ }
  ItemModel toModel(Item entity) {
    final model = ItemModel();
    if (entity.id != 0) model.id = entity.id;  // autoIncrement guard
    model..field = entity.field /* cascade */;
    return model;
  }
}
```

From lib/infrastructure/tasks/item_repository_impl.dart (repository impl pattern):
```dart
import 'package:injectable/injectable.dart';
@LazySingleton(as: ItemRepository)
class ItemRepositoryImpl implements ItemRepository {
  const ItemRepositoryImpl(this._dao, this._mapper);
  final ItemDao _dao;
  final ItemMapper _mapper;

  @override
  AsyncResult<Item> createItem(Item item) async {
    try {
      if (/* validation fails */) return const Err(ValidationFailure('...'));
      final id = await _dao.save(_mapper.toModel(item));
      final saved = await _dao.findById(id);
      return Ok(_mapper.toDomain(saved!));
    } on Object catch (e) {
      return Err(DatabaseFailure('createItem failed: $e'));
    }
  }

  @override
  Stream<void> watchChanges() => _dao.watchLazy();
}
```

From lib/config/di/tasks_module.dart (DI module pattern):
```dart
@module
abstract class TasksModule {
  @lazySingleton
  ItemDao itemDao(IsarService isarService) => ItemDao(isarService);
  @lazySingleton
  ItemMapper get itemMapper => const ItemMapper();
}
```

From lib/data/database/migration_runner.dart (extend this):
```dart
static Future<void> _runMigration(Isar isar, int toVersion) async {
  switch (toVersion) {
    case 1: return;
    case 2: return;
    // Add case 3 here
  }
}
```

From lib/core/config/app_config.dart:
```dart
static const int schemaVersion = 2;  // Bump to 3
```

From lib/main.dart:
```dart
await IsarService.instance.open([ItemModelSchema]);  // Add 6 finance schemas
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1a: Isar @Collection models, build_runner, mappers, and tests</name>
  <files>
    lib/data/finance/transaction_model.dart,
    lib/data/finance/transaction_category_model.dart,
    lib/data/finance/budget_model.dart,
    lib/data/finance/savings_goal_model.dart,
    lib/data/finance/debt_model.dart,
    lib/data/finance/recurring_payment_model.dart,
    lib/data/finance/finance_mappers.dart,
    test/data/finance/transaction_mapper_test.dart,
    test/data/finance/savings_goal_mapper_test.dart
  </files>
  <read_first>
    lib/data/tasks/item_model.dart
    lib/data/tasks/item_mapper.dart
    .planning/phases/03-finance-core/03-RESEARCH.md (Pattern 2 — all six model schemas; Pattern 8 — embedded list)
    .planning/phases/03-finance-core/03-PATTERNS.md (Isar Model Pattern, Mapper Pattern)
    .planning/phases/03-finance-core/03-01-SUMMARY.md
  </read_first>
  <behavior>
    - Test: TransactionMapper.toDomain(model) preserves amountCents exactly (model.amountCents = 123456 → domain.amountCents == 123456)
    - Test: TransactionMapper.toModel(tx with id==0) does NOT set model.id (autoIncrement sentinel)
    - Test: TransactionMapper.toModel(tx with id==5) sets model.id = 5
    - Test: GoalMapper.toDomain(model with 2 contributions) produces SavingsGoal with contributions.length == 2
    - Test: GoalMapper.toModel(goal).contributions initialized as growable (can add element without error)
    - Test: TransactionType enum values round-trip through mapper (income → income, expense → expense)
    - Test: DebtDirection.toPay and DebtDirection.toReceive both round-trip through DebtMapper
  </behavior>
  <action>
    Create lib/data/finance/ directory. Each model file starts with:
    `import 'package:isar_community/isar.dart'; part '{model_file_name}.g.dart';`

    Define TransactionType, DebtDirection, RecurringCycle enums IN the model files (not in domain — these are the Isar-layer enum declarations; mappers convert between them). Each enum field on a @Collection gets @Enumerated(EnumType.name) — no exceptions.

    transaction_model.dart: @Collection() TransactionModel with Id id = Isar.autoIncrement; @Enumerated(EnumType.name) late TransactionType type; late int amountCents; @Index() late int categoryId; @Index() late DateTime date; String? note; int? linkedGoalId; @Index() DateTime? deletedAt; late DateTime createdAt; late DateTime updatedAt.

    transaction_category_model.dart: @Collection() TransactionCategoryModel with Id id = Isar.autoIncrement; late String namePtBr; String? nameEn; @Enumerated(EnumType.name) late TransactionType type; bool isDefault = false; late DateTime createdAt. NO deletedAt — per PATTERNS.md shared patterns (custom categories are hard-deleted; default categories are undeletable).

    budget_model.dart: @Collection() BudgetModel with Id id = Isar.autoIncrement; @Index() late int categoryId; late int month; late int year; late int limitCents; late DateTime createdAt; late DateTime updatedAt. Add composite unique index: @Index(composite: [CompositeIndex('year'), CompositeIndex('categoryId')], unique: true) on the month field (so one budget record per categoryId+month+year).

    savings_goal_model.dart: @Collection() SavingsGoalModel with Id id = Isar.autoIncrement; late String title; late int targetAmountCents; DateTime? deadline; List<GoalContribution> contributions = List.empty(growable: true); — CRITICAL: List.empty(growable: true) not []. bool isCompleted = false; DateTime? deletedAt; late DateTime createdAt; late DateTime updatedAt. Define @embedded GoalContribution with GoalContribution() no-arg constructor, int amountCents = 0, late DateTime date, String? note.

    debt_model.dart: @Collection() DebtModel with Id id = Isar.autoIncrement; late String title; late int amountCents; @Enumerated(EnumType.name) late DebtDirection direction; late String counterparty; @Index() late DateTime dueDate; bool isPaid = false; DateTime? paidAt; DateTime? deletedAt; late DateTime createdAt; late DateTime updatedAt.

    recurring_payment_model.dart: @Collection() RecurringPaymentModel with Id id = Isar.autoIncrement; late String title; late int amountCents; late int categoryId; @Enumerated(EnumType.name) late RecurringCycle cycle; @Index() late DateTime nextDueDate; bool isActive = true; DateTime? deletedAt; late DateTime createdAt; late DateTime updatedAt.

    After creating all six model files, run: flutter pub run build_runner build --delete-conflicting-outputs

    finance_mappers.dart: Define five mapper classes (TransactionMapper, TransactionCategoryMapper, BudgetMapper, GoalMapper, DebtMapper, RecurringPaymentMapper) in a single file. Each is a const-constructable class. Domain types imported with aliases where enum names clash (e.g., `import 'package:agenda/domain/finance/transaction_type.dart' as domain`). Mapper methods: toDomain(Model) and toModel(Entity) following item_mapper.dart pattern exactly. For GoalMapper.toModel: set contributions as List.empty(growable: true) and populate with GoalContribution objects from domain SavingsGoalContribution values. Switch-based enum conversion methods for TransactionType, DebtDirection, RecurringCycle.

    Write mapper tests using real TransactionModel and SavingsGoalModel objects (no Isar opened — just construct models directly and assert field values after mapping).
  </action>
  <verify>
    <automated>flutter test test/data/finance/ -x</automated>
  </verify>
  <done>
    All 7+ mapper tests pass. build_runner generates *.g.dart files for all 6 model files without errors. flutter analyze lib/data/finance/ reports 0 errors. Each model file contains @Enumerated(EnumType.name) on every enum field. savings_goal_model.dart contains "List.empty(growable: true)".
  </done>
</task>

<task type="auto">
  <name>Task 1b: Six DAOs</name>
  <files>
    lib/data/finance/transaction_dao.dart,
    lib/data/finance/transaction_category_dao.dart,
    lib/data/finance/budget_dao.dart,
    lib/data/finance/savings_goal_dao.dart,
    lib/data/finance/debt_dao.dart,
    lib/data/finance/recurring_payment_dao.dart
  </files>
  <read_first>
    lib/data/tasks/item_dao.dart
    lib/data/finance/transaction_model.g.dart
    lib/data/finance/transaction_category_model.g.dart
    lib/data/finance/budget_model.g.dart
    lib/data/finance/savings_goal_model.g.dart
    lib/data/finance/debt_model.g.dart
    lib/data/finance/recurring_payment_model.g.dart
    .planning/phases/03-finance-core/03-RESEARCH.md (Pattern 3 — IsarService; Pattern 4 — Isar aggregate queries)
    .planning/phases/03-finance-core/03-PATTERNS.md (DAO Pattern)
  </read_first>
  <action>
    Create six DAO classes following item_dao.dart pattern exactly. Each DAO takes IsarService in constructor. Build against the *.g.dart files generated in Task 1a.

    TransactionDao includes: findAll() with .deletedAtIsNull().limit(500), findById(int id), findByMonth(int month, int year) with date range filter and .typeEqualTo(TransactionType.expense), findByLinkedGoal(int goalId) queries .linkedGoalIdEqualTo(goalId).and().deletedAtIsNull(), save(TransactionModel), softDelete(int id), watchLazy(). TransactionCategoryDao: findAll(), findByType(TransactionType), save(), hardDelete(int id) using writeTxn(() => _collection.delete(id)), count(). BudgetDao: findByCategoryMonthYear(int categoryId, int month, int year) → TransactionModel?, findByMonth(int month, int year), save(), deleteWhere(int categoryId, int month, int year). SavingsGoalDao: findAll() active only, findById(), save(), softDelete(), watchLazy(). DebtDao: findAll() active only, findById(), save(), softDelete(), watchLazy(). RecurringPaymentDao: findAll() with .isActiveEqualTo(true).and().deletedAtIsNull(), findById(), save(), softDelete().
  </action>
  <verify>
    <automated>flutter analyze lib/data/finance/ -x</automated>
  </verify>
  <done>
    dart analyze lib/data/finance/ reports 0 errors. All 6 DAO files exist. Each DAO follows the constructor pattern `const DaoName(this._isarService); final IsarService _isarService;`. TransactionDao.findByMonth() includes date range filter scoped to expense type. SavingsGoalDao.findAll() filters deletedAtIsNull().
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repository impls, DI wiring, schema registration, migration case 3</name></task>

<task type="auto" tdd="true">
  <name>Task 2: Repository impls, DI wiring, schema registration, migration case 3</name>
  <files>
    lib/infrastructure/finance/transaction_repository_impl.dart,
    lib/infrastructure/finance/transaction_category_repository_impl.dart,
    lib/infrastructure/finance/budget_repository_impl.dart,
    lib/infrastructure/finance/goal_repository_impl.dart,
    lib/infrastructure/finance/debt_repository_impl.dart,
    lib/infrastructure/finance/recurring_payment_repository_impl.dart,
    lib/data/database/isar_service.dart,
    lib/data/database/migration_runner.dart,
    lib/core/config/app_config.dart,
    lib/config/di/finance_module.dart,
    lib/main.dart,
    test/data/database/migration_runner_test.dart
  </files>
  <read_first>
    lib/infrastructure/tasks/item_repository_impl.dart
    lib/data/database/migration_runner.dart
    lib/data/database/isar_service.dart
    lib/core/config/app_config.dart
    lib/config/di/tasks_module.dart
    lib/config/di/finance_module.dart
    lib/main.dart
    .planning/phases/03-finance-core/03-RESEARCH.md (Pattern 7 — MigrationRunner case 3; Pattern 3 — IsarService.open)
    .planning/phases/03-finance-core/03-PATTERNS.md (Repository Implementation Pattern, DI Module Pattern, IsarService.open Schema Extension, MigrationRunner Case 3 Pattern)
    .planning/phases/03-finance-core/03-CONTEXT.md (D-03, D-04, D-05, D-06)
  </read_first>
  <behavior>
    - Test: MigrationRunner case 3 inserts exactly 13 categories (9 expense + 4 income) on first run
    - Test: MigrationRunner case 3 is idempotent — running twice does NOT insert duplicates
    - Test: Expense category namePtBr values include 'Alimentação', 'Transporte', 'Moradia', 'Saúde', 'Educação', 'Lazer', 'Roupas', 'Tecnologia', 'Outros'
    - Test: Income category namePtBr values include 'Salário', 'Freelance', 'Investimentos', 'Outros'
    - Test: All default categories have isDefault == true
  </behavior>
  <action>
    Create lib/infrastructure/finance/ directory. Create six repository impl files following item_repository_impl.dart pattern.

    Each impl uses @LazySingleton(as: InterfaceType) annotation and implements the corresponding repository interface from 03-01. Constructor takes the DAO and mapper via DI injection.

    VALIDATION RULES (enforced in every create/update method before any Isar write):
    - amountCents must be > 0: return Err(ValidationFailure('amountCents must be greater than zero'))
    - title must not be empty string: return Err(ValidationFailure('title must not be empty')) — applies to Debt, RecurringPayment, SavingsGoal
    - categoryId must be > 0: return Err(ValidationFailure('categoryId must be a valid reference'))

    TransactionRepositoryImpl: implements TransactionRepository. createTransaction validates amountCents > 0 and categoryId > 0. getTransactions calls dao.findAll(). getByMonth delegates to dao.findByMonth(). getByLinkedGoal delegates to dao.findByLinkedGoal(). updateTransaction validates amountCents > 0. softDelete calls dao.softDelete() then reads back with dao.findById() (NOT getTransaction — bypasses deletedAtIsNull filter). watchChanges delegates to dao.watchLazy().

    TransactionCategoryRepositoryImpl: implements TransactionCategoryRepository. delete(int id) must first verify isDefault == false: load the category, if isDefault return Err(ValidationFailure('Cannot delete a default category')). Hard deletes custom categories.

    BudgetRepositoryImpl: implements BudgetRepository. setLimit is an upsert: call dao.findByCategoryMonthYear() to check if record exists; if yes, update limitCents and updatedAt; if no, create new BudgetModel. Both paths go through dao.save().

    GoalRepositoryImpl: implements GoalRepository. addContribution(goalId, contribution): load goal model by id, append GoalContribution to model.contributions list (possible because list is growable), save, return updated domain entity. Validate contribution.amountCents > 0.

    DebtRepositoryImpl: implements DebtRepository. togglePaid(id): load model, flip model.isPaid, if now isPaid set model.paidAt = DateTime.now() else model.paidAt = null, save.

    RecurringPaymentRepositoryImpl: implements RecurringPaymentRepository. No watchChanges needed — Phase 4 notification system reads directly; Phase 3 only stores.

    lib/core/config/app_config.dart: Change `static const int schemaVersion = 2;` to `static const int schemaVersion = 3;`

    lib/data/database/migration_runner.dart: Add case 3 to _runMigration switch: `case 3: await _seedDefaultCategories(isar); return;` Add private static method _seedDefaultCategories(Isar isar) with idempotency guard `if (await isar.transactionCategoryModels.count() > 0) return;`. Seed 9 expense categories (namePtBr: 'Alimentação','Transporte','Moradia','Saúde','Educação','Lazer','Roupas','Tecnologia','Outros'; nameEn: 'Food','Transport','Housing','Health','Education','Leisure','Clothing','Technology','Other'; type: TransactionType.expense; isDefault: true) and 4 income categories (namePtBr: 'Salário','Freelance','Investimentos','Outros'; nameEn: 'Salary','Freelance','Investments','Other'; type: TransactionType.income; isDefault: true). Write all in single isar.writeTxn(). Add import for TransactionCategoryModel and TransactionType.

    lib/data/database/isar_service.dart: No structural changes needed — the schemas list is already a parameter passed from main.dart.

    lib/config/di/finance_module.dart: Replace the empty placeholder with populated @module abstract class FinanceModule. Register all 6 DAOs as @lazySingleton factory methods taking IsarService. Register all 6 mappers as @lazySingleton getters returning const instances. Follow tasks_module.dart pattern exactly. Repository impls register themselves via @LazySingleton(as: InterfaceType) class annotations — do NOT declare them in the module.

    lib/main.dart: Update IsarService.instance.open([...]) to include all 7 schemas: ItemModelSchema, TransactionModelSchema, TransactionCategoryModelSchema, BudgetModelSchema, SavingsGoalModelSchema, DebtModelSchema, RecurringPaymentModelSchema. Add imports for all 6 new model files.

    After completing all files: flutter pub run build_runner build --delete-conflicting-outputs (regenerates injection.config.dart to include finance module registrations).

    Migration tests: Use mocktail to mock SharedPreferences. Create an in-memory Isar instance using Isar.open with an in-memory directory. Call MigrationRunner.run(isar, mockPrefs) with mockPrefs returning 2 (simulating schema version 2). Verify 13 TransactionCategoryModel rows inserted. Call run again; verify count still 13.
  </action>
  <verify>
    <automated>flutter test test/data/database/migration_runner_test.dart test/data/finance/ -x</automated>
  </verify>
  <done>
    Migration tests pass: case 3 inserts 13 categories on first run, 13 still on second run (idempotent). dart analyze lib/infrastructure/finance/ reports 0 errors. finance_module.dart contains at least one TransactionDao reference. injection.config.dart has been regenerated (contains FinanceModule references). lib/main.dart contains TransactionModelSchema. app_config.dart contains schemaVersion = 3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository impl → Isar write | User-supplied amounts and strings enter Isar at this boundary |
| MigrationRunner → Isar | Seeding writes at schema version upgrade; no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | TransactionRepositoryImpl.createTransaction | mitigate | Validate amountCents > 0 before Isar write; return ValidationFailure — never store zero/negative monetary values |
| T-03-02-02 | Tampering | TransactionCategoryRepositoryImpl.delete | mitigate | Guard: load category, check isDefault == false before hard delete; return ValidationFailure if default. Prevents user from deleting seed data |
| T-03-02-03 | Tampering | BudgetModel composite index | accept | Isar unique index on (month, year, categoryId) prevents duplicate budget records. Low risk — local single-user data. |
| T-03-02-04 | Tampering | GoalRepositoryImpl.addContribution — embedded list append | mitigate | Validate contribution.amountCents > 0. Embedded list is growable — no runtime crash possible from this operation given correct initialization in 03-01. |
| T-03-02-05 | Information Disclosure | Isar database file on device | accept | Local file in app documents directory. No additional encryption in Phase 3 (Phase 5 adds PIN; DATA-01 confirmed local-only). Acceptable at ASVS L1 for a local-only app. |
| T-03-02-06 | Denial of Service | 500-item query cap | accept | Protects against OOM on large transaction history. A user with >500 transactions will see truncated balance — acceptable for MVP. Noted in RESEARCH.md assumption A3. |
</threat_model>

<verification>
flutter test test/data/finance/ test/data/database/migration_runner_test.dart -x
dart analyze lib/data/finance/ lib/infrastructure/finance/ lib/config/di/finance_module.dart
grep -c "schemaVersion = 3" lib/core/config/app_config.dart
grep -c "TransactionModelSchema" lib/main.dart
grep -c "List.empty(growable: true)" lib/data/finance/savings_goal_model.dart
grep -c "case 3" lib/data/database/migration_runner.dart
</verification>

<success_criteria>
1. flutter test test/data/ passes all migration + mapper tests
2. dart analyze reports 0 errors across all new data/infrastructure/config files
3. build_runner generates all 6 *.g.dart files without errors
4. app_config.dart: schemaVersion = 3
5. main.dart includes all 7 schemas in IsarService.instance.open()
6. migration_runner.dart: case 3 inserts 13 default categories idempotently
7. finance_module.dart is populated with all DAO and mapper registrations
8. injection.config.dart is regenerated and includes FinanceModule
</success_criteria>

<output>
After completion, create .planning/phases/03-finance-core/03-02-SUMMARY.md
</output>
