---
context: phase
phase: 03-finance-core
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-08-13T18:03:03.965Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Wave-1 work exists only on unmerged worktree branches** — `main` is at `c10f314` and clean; every line of the wave-1 implementation lives on `worktree-agent-a02b75e60f0b5ed1c` (plan 03-06) and `worktree-agent-a61e24f07fe8ace8d` (plan 03-08) — structural mitigation required: **do not delete either worktree or branch before merging both into `main`.** `git worktree remove --force` on these discards real work.
- [ ] CONSTRAINT: **Widget tests in this project default to the `en` locale, not pt-BR** — a `MaterialApp` with no `locale:` renders English ARB values, so `resolveCategoryDisplay` returns `nameEn` and any assertion on a pt-BR string silently finds nothing — structural mitigation required: **every `MaterialApp` in a new widget test must pass an explicit `locale:`.** Proof: `test/presentation/tasks/task_detail_screen_test.dart` builds a locale-less `MaterialApp` (lines 20, 41) and asserts English `'Dates'`/`'Flags'` (lines 111, 114) inside the green suite.
- [ ] CONSTRAINT: **The "65 infos" gate number is real and load-bearing** — `discarded_futures` is an active lint, and the bare `.then` pattern at `task_form_screen.dart:52` is already one of the 65 — structural mitigation required: **use `unawaited(...)` for fire-and-forget futures in `initState`, never a bare `.then`, and never an `// ignore: discarded_futures` comment.** Copying that analog verbatim produces a 66th info and reads as a gate failure everywhere the number is asserted.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting a stale line reference in UAT/planning docs | `03-UAT.md` pointed at `task_detail_screen.dart:303` for the finance-chip defect. Phase 06 had since extracted that chip into `lib/presentation/tasks/widgets/detail/task_detail_finance_chip.dart`; the screen is now only 94 lines. Planning against the stale reference would have targeted a file that no longer contains the code. | blocking | Re-resolve every file:line reference by reading the file before planning against it. Phase 06 moved a large amount of code; any reference written before it is suspect. |
| Re-diagnosing already-diagnosed defects | Three of the four Phase 03 defects already carried confirmed root causes and prescribed fixes in `03-UAT.md`. Sending them through a debug session would have burned a full investigation to rediscover known causes. | advisory | Read the `## Gaps` section's `root_cause`, `artifacts` and `missing` fields first. Route already-diagnosed gaps to planning; route only `NOT ISOLATED` entries to `/gsd-debug`. |
| Blaming the device before checking the framework | The undo-timer defect was attributed for two days to Transsion/XOS power management throttling Dart timers. The real cause was a Flutter 3.38 breaking change (`SnackBar.persist` defaults to `action != null`). It reproduced deterministically in `flutter test` with no device attached. | advisory | Before accepting an environment-specific explanation, try to reproduce host-side. Check the framework's breaking-change notes for the pinned SDK range — `pubspec.yaml` pins `flutter: '>=3.38.1'`, which is why this was never seen working. |
| Piped exit codes | `cmd \| tail` captures `tail`'s status in `$?`, so a failing guard can look like exit 0. | advisory | Run gate commands unpiped, or capture `${PIPESTATUS[0]}`. |

<current_state>
Paused mid-wave-1 of Phase 03 gap closure. `main` is at `c10f314` with a CLEAN working tree — nothing is staged or modified in the main checkout.

All wave-1 implementation work is committed but **unmerged**, sitting on two worktree branches:

| Branch | Plan | Commits | State |
|---|---|---|---|
| `worktree-agent-a61e24f07fe8ace8d` | 03-08 | `fec622d`, `c360c25`, `ecb9a80` | COMPLETE — SUMMARY.md committed, agent returned |
| `worktree-agent-a02b75e60f0b5ed1c` | 03-06 | `76f7a89`, `6066031`, `b22e87a`, `8666c41` | COMPLETE — SUMMARY.md committed, agent returned |

Worktree paths are under `.claude/worktrees/agent-<id>/`. **Both wave-1 plans are done; only the merge is outstanding.**

Per-branch gate numbers, reported by each executor and NOT yet measured together:

| Gate | Baseline `c10f314` | 03-06 branch | 03-08 branch |
|---|---|---|---|
| `flutter test --no-pub` | 268 | 277 | 270 |
| `flutter analyze` | exit 0, 65 infos | exit 0, 65 infos | exit 0, 65 infos |
| `check_architecture.dart` | exit 0 | exit 0 | exit 0 |

The merged suite should land near 279, but that is arithmetic, not a measurement. Re-run the gate on the merged tree.

Note: the 03-06 and 03-08 worktrees spawned at `d102f2b` — one commit *behind* the declared base `c10f314` — so the plan files did not exist until the branch-check protocol reset them forward. This is expected and was handled; mention only because a resuming orchestrator should keep the same assertion in its dispatch prompts.
</current_state>

<completed_work>

**Committed on `main` (pushed through `d102f2b`):**
- `d102f2b` — undo-timer fix. `persist: false` at three undo SnackBar call sites plus three real-screen regression tests. Verified red 0-passed/3-failed with the fix stashed, green 268/268 with it.
- `64ea714` — gap-closure plans 03-06/03-07/03-08 written.
- `4e5faae` — `03-UAT.md` undo-timer gap marked resolved; two now-false claims in it corrected.
- `c10f314` — plans revised after plan-checker review (locale pinning, `unawaited`).

**Committed on worktree branches (NOT on main):**
- 03-08 complete: finance chip now resolves the linked goal/debt title via the existing `loadFinanceLinks`. Reported gate on its branch: guard exit 0, analyze exit 0 / 65 infos, tests 270 (268 + 2 new). Mutation-tested — reverting the label to the raw id failed exactly the new debt-name test.
- 03-06 complete: `TransactionLoaded` now carries the category list; cards resolve `categoryId` to a localized name and render the note once. Reported gate on its branch: guard exit 0, analyze exit 0 / 65 infos, tests 277. Red-state proven by temporary revert — reverting the card title produced `Found 2 widgets with text "Mercado semana"`, and restoring the `#id` stub produced `Found 0 widgets` for both resolution tests. It added one test beyond the plan (`start() still emits TransactionLoaded when the category repo Errs`) covering truth #3.
</completed_work>

<newly_discovered_traps>

Two environment traps surfaced during wave 1. Neither is a code defect, both will recur:

1. **A fresh worktree has no `.dart_tool/`** (it is gitignored), so `flutter test --no-pub` crashes with `Bad state: No element` in native-assets rather than running any test. Run `flutter pub get` as the first action inside any new worktree.
2. **A pt_BR `MaterialApp` without `GlobalCupertinoLocalizations.delegate`** emits a "not supported by all of its localization delegates" warning that `flutter_test` records as a pending exception, failing the test. This is latent in `test/presentation/undo_snackbar_auto_dismiss_test.dart` — it escapes only because that harness loads the `en` locale. Fix it there if pt_BR cases are ever added.

Also: 03-06's `key_links` regex for `injection.config.dart` expects a single-line factory, but injectable wrapped it across lines 210-214. A single-line grep reads as a false negative — the same class of stale-assertion problem flagged in the anti-patterns table.
</newly_discovered_traps>

<remaining_work>

1. Merge BOTH wave-1 branches into `main`. Both are complete; nothing needs reconstructing.
2. Run the full gate on the merged tree — this is the first time the two changes meet. 03-06 edits `transaction_list_screen.dart` and `transaction_card.dart`; 03-08 edits `task_detail_finance_chip.dart`. Declared `files_modified` were disjoint, but the merged test count and info count still need re-measuring rather than added up.
3. Execute wave 2 = plan 03-07 (SnackBar FIFO queueing — missing `hideCurrentSnackBar()`). It was deliberately serialized behind 03-06 because both edit `transaction_list_screen.dart`, so it must be dispatched against the MERGED main, not `c10f314`.
5. Run `gsd-verifier` over Phase 03; update `STATE.md` and `ROADMAP.md` (orchestrator owns those writes — the worktree agents were told not to touch them).
6. Then: the three pre-existing UI defects (double X close button in the GTD sheet header, "Tamanho" `SegmentedButton` text wrap, hardcoded unlocalised strings). These were agreed as the follow-on bucket after Phase 03.
7. On-device re-test of the undo-timer fix when hardware is available; only then move `.planning/debug/undo-timer-never-fires.md` to `resolved/`.
</remaining_work>

<decisions_made>

- **Narrowed the debug session to the undo-timer defect alone** rather than all four Phase 03 UAT defects — the other three already had confirmed root causes and prescribed fixes in `03-UAT.md`, and that document itself prescribed `/gsd-debug` for the timer defect specifically.
- **Fixed the undo timer with `persist: false`, touching no timer, cubit, repository or Isar code.** Root cause is a Flutter 3.38 breaking change: `SnackBar` computes `persist = persist ?? action != null` (`snack_bar.dart:303`) and `ScaffoldMessengerState`'s timeout callback opens with `if (snackBar.persist) { return; }` (`scaffold.dart:622`) before it would reach `hideCurrentSnackBar`. The timer fired all along. Blast radius confirms it: exactly 3 of 11 `showSnackBar` sites carry an action, and those are the 3 reported stuck.
- **Chose `unawaited(...)` over raising the info budget to 66** in 03-08 — keeps the gate number stable, and `unawaited` is the wider presentation-layer convention (4 uses vs the 1 bare `.then` being copied).
- **Pinned explicit locales in the new 03-06 tests** rather than dropping `nameEn` from the fixture — preserves en/pt discrimination in both directions.
- **Did not rewrite git history for the Phase 6 renumber** — commit subjects still say `3.1`/`03.1`; `STATE.md` records the mapping.
</decisions_made>

<blockers>
- **Unmerged worktree branches**: both wave-1 branches must be merged before anything else. Deleting the worktrees first destroys the work. This is the only real blocker.
- **No Android device**: `adb devices` is empty. The undo-timer fix is verified host-side only, so its debug session stays open at `awaiting_human_verify`. Phase 03 also cannot be fully closed without re-running UAT test 2 on device — 03-06 is widget-test-proven only.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured form of this state, including exact commit hashes per task.
2. `.planning/phases/03-finance-core/03-UAT.md` — the authoritative gap source. Its `## Gaps` section drives what "done" means for this phase.
3. `.planning/phases/03-finance-core/03-06-PLAN.md` and `03-07-PLAN.md` — 03-06 to verify what was built, 03-07 because it is the next thing to execute.
4. `.planning/debug/undo-timer-never-fires.md` — open debug session; its `next_action` holds the exact on-device steps still owed.
5. `./CLAUDE.md` — stack is decided and not negotiable; the architecture guard is enforcing.

## Critical Anti-Patterns (do NOT repeat these)

- [ANTI-PATTERN]: Deleting a worktree before merging its branch → always `git log --oneline <base>..<worktree-branch>` first and merge, then remove.
- [ANTI-PATTERN]: Asserting a pt-BR string in a widget test without setting `locale:` → pass an explicit `locale:` on every `MaterialApp` under test.
- [ANTI-PATTERN]: Copying the `.then` pattern from `task_form_screen.dart:52` → wrap in `unawaited(...)` instead; that analog is itself a lint info.
- [ANTI-PATTERN]: Accepting a subagent's gate numbers without re-running them → re-run the gate yourself on the merged tree.

## Infrastructure State

- **git**: `main` at `c10f314`, clean, pushed through `d102f2b`. Two live worktrees under `.claude/worktrees/`.
- **adb**: no devices attached. The daemon needed `adb kill-server && adb start-server` to see the phone previously.
- **CI**: GitHub Actions runs analyze → architecture guard → test on push. Green as of `d102f2b`.
- **Gate baseline to hold**: `dart run tool/check_architecture.dart` exit 0; `flutter analyze --no-fatal-infos --fatal-warnings` exit 0 with 65 infos; `flutter test --no-pub` 268/268 on `main` (03-08's branch reported 270).
- **Session note**: a caveman-mode output hook is active in the paused session. It affects chat prose only — never code, commits, or file content.

<context>
The through-line of this session was that recorded diagnoses were repeatedly wrong in the same direction: they blamed the environment or a distant layer when the cause was local and checkable. The undo timer was blamed on OEM power management for two days and was actually a framework default. `task_list_cubit.dart:78` was recorded as having a non-firing timer and has no defect at all. `03-UAT.md` pointed at a line number that Phase 06 had relocated.

So the working method here has been: re-verify every claim before acting on it, including claims made by subagents and by my own earlier notes. That is why the plan-checker's two blockers were re-confirmed by hand before being sent back, why the executor's red state was re-run rather than relayed, and why the framework source was read directly instead of trusting the breaking-change note.

Apply the same skepticism to 03-06's gate numbers when it lands — they were never reported.

Note the `grep -l "To be filled\|placeholder\|TBD"` sweep the pause workflow prescribes returns ~21 files here, but they are false positives: the matches are prose describing the placeholder bug (e.g. `03-06-PLAN.md:257` quotes the source comment "For now, use a placeholder that works"). No SUMMARY.md has unfilled template content.
</context>

<next_action>
Start with: `git worktree list && git log --oneline c10f314..worktree-agent-a02b75e60f0b5ed1c` to confirm the 03-06 branch state, then check whether `03-06-SUMMARY.md` exists on that branch. If it does not, write it from the three task commit diffs. Then merge both wave-1 branches into `main`, run the full gate on the merged tree, and only then dispatch wave 2 (plan 03-07).
</next_action>
