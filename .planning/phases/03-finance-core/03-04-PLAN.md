---
phase: 03-finance-core
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - lib/presentation/finance/screens/transaction_list_screen.dart
  - lib/presentation/finance/screens/transaction_form_screen.dart
  - lib/presentation/finance/screens/budget_overview_screen.dart
  - lib/presentation/finance/screens/goal_list_screen.dart
  - lib/presentation/finance/screens/goal_detail_screen.dart
  - lib/presentation/finance/screens/debt_list_screen.dart
  - lib/presentation/finance/screens/recurring_payment_screen.dart
  - lib/presentation/finance/screens/recurring_payment_form_screen.dart
  - lib/presentation/finance/screens/goal_form_screen.dart
  - lib/presentation/finance/screens/debt_form_screen.dart
  - lib/presentation/finance/widgets/transaction_card.dart
  - lib/presentation/finance/widgets/budget_progress_bar.dart
  - lib/presentation/finance/widgets/goal_progress_card.dart
  - lib/presentation/finance/widgets/finance_empty_state.dart
  - lib/presentation/finance/screens/finance_dashboard_screen.dart
  - lib/config/l10n/app_pt_BR.arb
  - lib/config/l10n/app_en.arb
  - lib/app.dart
  - lib/domain/tasks/item.dart
  - lib/presentation/tasks/screens/task_form_screen.dart
  - lib/presentation/tasks/screens/task_detail_screen.dart
  - test/presentation/finance/finance_empty_state_test.dart
autonomous: false
requirements:
  - FIN-01
  - FIN-02
  - FIN-03
  - FIN-04
  - FIN-05
  - FIN-06
  - FIN-07
  - FIN-08
  - UX-04

must_haves:
  truths:
    - "Finance tab (Finanças) appears as index 4 in app.dart NavigationBar"
    - "FinanceDashboardScreen contains a 6-tab TabBar: Resumo, Transações, Orçamentos, Objetivos, Dívidas, Recorrências"
    - "TransactionFormScreen has SegmentedButton for income/expense type, amount field with decimal keyboard, category BottomSheet picker, DatePicker for date, optional goal link picker for expense type"
    - "All list screens show FinanceEmptyState with CTA button when list is empty (UX-04)"
    - "BudgetProgressBar changes color: cs.primary (0-79%), warningAmber (80-99%), cs.error (≥100%) per UI-SPEC"
    - "Task form shows 'Vincular a...' section with goal/debt picker (D-21); task detail shows ActionChip when linkedGoalId or linkedDebtId is set (D-19)"
    - "All UI strings stored in ARB files — no hardcoded strings in widget files"
  artifacts:
    - path: "lib/presentation/finance/screens/finance_dashboard_screen.dart"
      provides: "FinanceDashboardScreen with DefaultTabController(length: 6) + TabBar + TabBarView"
      contains: "DefaultTabController"
    - path: "lib/presentation/finance/widgets/finance_empty_state.dart"
      provides: "FinanceEmptyState widget with icon, heading, body, CTA FilledButton"
      exports: ["FinanceEmptyState"]
    - path: "lib/presentation/finance/widgets/budget_progress_bar.dart"
      provides: "BudgetProgressBar widget with three-state color logic"
      exports: ["BudgetProgressBar"]
    - path: "lib/presentation/finance/widgets/transaction_card.dart"
      provides: "TransactionCard with Dismissible swipe-delete + income/expense color"
      exports: ["TransactionCard"]
    - path: "lib/app.dart"
      provides: "NavigationBar with 5 destinations including Finanças at index 4"
      contains: "FinanceDashboardScreen"
    - path: "lib/config/l10n/app_pt_BR.arb"
      provides: "All finance ARB keys from UI-SPEC Copywriting Contract"
      contains: "financeTabLabel"
  key_links:
    - from: "lib/app.dart"
      to: "lib/presentation/finance/screens/finance_dashboard_screen.dart"
      via: "_screens list index 4 + NavigationDestination label financeTabLabel"
      pattern: "FinanceDashboardScreen"
    - from: "lib/presentation/finance/screens/transaction_list_screen.dart"
      to: "lib/application/finance/transaction/transaction_cubit.dart"
      via: "BlocProvider + context.read<TransactionCubit>().start() in initState"
      pattern: "TransactionCubit"
    - from: "lib/presentation/tasks/screens/task_form_screen.dart"
      to: "lib/application/finance/goal/goal_cubit.dart"
      via: "GoalListCubit + DebtCubit to populate 'Vincular a...' picker options"
      pattern: "linkedGoalId|linkedDebtId"
---

<objective>
Build all finance presentation screens, widgets, form screens, the FinanceDashboardScreen shell, all finance ARB strings, and the task↔finance link display. Wire the Finance tab into app.dart.

Purpose: After this plan, a user can interact with the entire finance UI: log transactions, view budget progress, manage goals, track debts, and manage recurring payments. The dashboard tab shell is wired; charts are populated in plan 03-05.
Output: 21 new/modified Dart files + 2 ARB files. One human checkpoint to verify the UI visually.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-finance-core/03-CONTEXT.md
@.planning/phases/03-finance-core/03-RESEARCH.md
@.planning/phases/03-finance-core/03-PATTERNS.md
@.planning/phases/03-finance-core/03-UI-SPEC.md
@.planning/phases/03-finance-core/03-03-SUMMARY.md

<interfaces>
<!-- Existing codebase patterns this plan extends. -->

From lib/presentation/tasks/screens/task_list_screen.dart (list screen pattern):
```dart
class TaskListScreen extends StatefulWidget { const TaskListScreen({super.key}); }
class _TaskListScreenState extends State<TaskListScreen> {
  @override
  void initState() { super.initState(); context.read<TaskListCubit>().start(); }
  // ...
  body: BlocConsumer<TaskListCubit, TaskListState>(
    builder: (context, state) => switch (state) {
      TaskListInitial() || TaskListLoading() => const Center(child: CircularProgressIndicator()),
      TaskListError(:final failure) => Center(child: Text(failure.message)),
      TaskListLoaded(:final items) when items.isEmpty => const _EmptyState(),
      TaskListLoaded(:final items) => _TaskList(items: items),
      // ...
    },
  ),
}
```

From lib/presentation/tasks/screens/task_form_screen.dart (form pattern):
```dart
class _TaskFormScreenState extends State<TaskFormScreen> {
  final _formKey = GlobalKey<FormState>();
  late final TextEditingController _titleController;
  bool get _isEditing => widget.task != null;
  @override
  void initState() { /* init controllers */ }
  @override
  void dispose() { _titleController.dispose(); super.dispose(); }
  // AppBar action: FilledButton("Salvar tarefa") calls _save()
  void _save() { if (!_formKey.currentState!.validate()) return; /* cubit call */ }
}
```

From lib/presentation/tasks/widgets/task_card.dart (card pattern):
```dart
class TaskCard extends StatelessWidget {
  const TaskCard({super.key, required this.item, required this.onDelete, required this.onTap});
  final Item item;
  // ...
  Widget build(BuildContext context) => Card(
    margin: EdgeInsets.symmetric(horizontal: 8, vertical: 4),
    elevation: 0,
    // ...
  );
}
```

From lib/app.dart (navigation — extend this):
```dart
static const _screens = [
  TaskListScreen(),
  EisenhowerScreen(),
  DayPlannerScreen(),
  GtdFilterScreen(usedAsTab: true),
  // ADD: FinanceDashboardScreen()
];
// NavigationBar destinations: Tarefas, Eisenhower, Dia, GTD, (ADD: Finanças)
```

From lib/domain/tasks/item.dart (add linkedGoalId / linkedDebtId to Item — check current state):
```dart
// Fields already exist on ItemModel (Phase 2 extensibility contract)
// Need to confirm they exist on the Item domain entity too — read before modifying
int? linkedGoalId;
int? linkedDebtId;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reusable widgets, ARB strings, and core infrastructure</name>
  <files>
    lib/presentation/finance/widgets/transaction_card.dart,
    lib/presentation/finance/widgets/budget_progress_bar.dart,
    lib/presentation/finance/widgets/goal_progress_card.dart,
    lib/presentation/finance/widgets/finance_empty_state.dart,
    lib/config/l10n/app_pt_BR.arb,
    lib/config/l10n/app_en.arb,
    test/presentation/finance/finance_empty_state_test.dart
  </files>
  <read_first>
    lib/presentation/tasks/widgets/task_card.dart
    lib/presentation/tasks/widgets/slot_section.dart
    lib/config/l10n/app_pt_BR.arb
    lib/config/l10n/app_en.arb
    lib/core/constants/finance_colors.dart
    lib/core/utils/amount_formatter.dart
    .planning/phases/03-finance-core/03-UI-SPEC.md (Transaction Card Contract, Budget Progress Card Contract, Goal Progress Card Contract, Empty State Design, Copywriting Contract)
  </read_first>
  <action>
    Create lib/presentation/finance/widgets/ directory.

    transaction_card.dart: StatelessWidget TransactionCard. Constructor params: required Transaction transaction, required VoidCallback onDelete, required VoidCallback onTap, required String categoryName, required String currencySymbol, required Locale locale. Wrapped in Dismissible(key: Key('tx-${transaction.id}'), direction: DismissDirection.endToStart, background: Container with alignment.centerRight, red background cs.error, Icons.delete icon, onDismissed: (_) => onDelete()). Card: elevation 0, surfaceContainerLow, BorderRadius.circular(12), margin EdgeInsets.symmetric(horizontal:8, vertical:4). InkWell onTap. ListTile: leading Icon(transaction.type == TransactionType.income ? Icons.arrow_upward : Icons.arrow_downward, color: income=FinanceColors.incomeGreen, expense=FinanceColors.expenseRed, size:24). title Text(transaction.note ?? categoryName, style bodyLarge, overflow ellipsis). trailing Text(formatAmount(transaction.amountCents, currencySymbol, locale), style bodyLarge with w500 weight and income/expense color). subtitle Row with Text(categoryName + ' · ' + DateFormat('dd/MM/yyyy').format(transaction.date), style bodySmall, color onSurfaceVariant). If transaction.note != null AND note != categoryName, show below subtitle a Chip(label: Text(transaction.note!, overflow ellipsis maxLines 1), labelStyle TextStyle(fontSize:11)). No Flutter/Isar imports except flutter/material.dart and package:agenda/... domain imports + intl.

    budget_progress_bar.dart: StatelessWidget BudgetProgressBar. Constructor params: required String categoryName, required int spentCents, required int limitCents, required String currencySymbol, required Locale locale. If limitCents == 0: show Text('Sem limite definido' / via l10n, style bodySmall, color onSurfaceVariant) — no progress bar shown. Otherwise: Column with titleSmall categoryName, SizedBox(height:4), Row(mainAxisAlignment: spaceBetween, children: [Text(formatAmount(spent...), bodyMedium, onSurfaceVariant), Text(formatAmount(limit...), bodyMedium, onSurfaceVariant)]), SizedBox(height:4), ClipRRect(borderRadius: BorderRadius.circular(4), child: LinearProgressIndicator(value: min(1.0, spentCents/limitCents), minHeight: 8, color: _progressColor(ratio), backgroundColor: cs.surfaceContainerHighest)). _progressColor: ratio < 0.80 → cs.primary; 0.80 <= ratio < 1.0 → FinanceColors.warningAmber; ratio >= 1.0 → cs.error. Import dart:math for min().

    goal_progress_card.dart: StatelessWidget GoalProgressCard. Constructor params: required SavingsGoal goal, required int taggedTransactionsCents, required VoidCallback onTap, required String currencySymbol, required Locale locale. Card: elevation 0, surfaceContainerLow, BorderRadius.circular(16), margin EdgeInsets.symmetric(horizontal:8, vertical:4). InkWell onTap. Padding 16. Column: Row(titleMedium goal.title, spacer, if completed: Icon(Icons.check_circle, color: FinanceColors.incomeGreen) else Text(progressPercent%, labelMedium, cs.primary)), SizedBox(height:4), Text('{saved} de {target}', bodySmall, onSurfaceVariant), SizedBox(height:8), LinearProgressIndicator(value: min(1.0, goal.progressPercent(taggedTransactionsCents)), minHeight: 8, color: cs.primary, backgroundColor: cs.surfaceContainerHighest), if goal.deadline != null: SizedBox(height:4) + Text('Prazo: ' + DateFormat('dd/MM/yyyy').format(goal.deadline!), bodySmall, onSurfaceVariant).

    finance_empty_state.dart: StatelessWidget FinanceEmptyState. Constructor params: required IconData icon, required String heading, required String body, required String ctaLabel, required VoidCallback onCta. Layout: Center → Column(mainAxisSize: min, children: [Icon(icon, size:72, color: cs.onSurfaceVariant), SizedBox(height:16), Text(heading, style: titleMedium, textAlign: center), SizedBox(height:8), Text(body, style: bodyMedium, color: onSurfaceVariant, textAlign: center), SizedBox(height:24), FilledButton(onPressed: onCta, child: Text(ctaLabel))]). No BloC imports — pure presentation widget.

    ARB files: Add ALL finance keys from UI-SPEC Copywriting Contract section to both app_pt_BR.arb and app_en.arb. Keys include: financeTabLabel, dashboardTabLabel, transactionsTabLabel, budgetsTabLabel, goalsTabLabel, debtsTabLabel, recurringTabLabel, addTransaction, addGoal, addDebt, addRecurring, addContribution, saveTransaction, saveGoal, saveDebt, saveRecurringPayment, setBudgetLimit, emptyTransactions, emptyTransactionsBody, emptyBudgets, emptyBudgetsBody, emptyGoals, emptyGoalsBody, emptyDebts, emptyDebtsBody, emptyRecurring, emptyRecurringBody, emptyDashboard, emptyDashboardBody, errorLoadFailed, errorSaveFailed, errorAmountRequired, errorTitleRequired, errorCategoryRequired, fieldAmount, fieldNote, fieldCategory, noLimitSet. Also add category l10n keys for all 13 defaults: categoryAlimentacao, categoryTransporte, categoryMoradia, categorySaude, categoryEducacao, categoryLazer, categoryRoupas, categoryTecnologia, categoryOutros (expense), categorySalario, categoryFreelance, categoryInvestimentos (income). After editing ARBs, run: flutter gen-l10n.

    finance_empty_state_test.dart: Widget test. Pump FinanceEmptyState with mock params. Verify Icon renders. Verify heading Text renders. Verify FilledButton with ctaLabel renders. Verify onCta callback fires on tap.
  </action>
  <verify>
    <automated>flutter test test/presentation/finance/finance_empty_state_test.dart -x && flutter gen-l10n</automated>
  </verify>
  <done>
    Empty state widget test passes. flutter gen-l10n completes without errors (all new ARB keys are parity across pt_BR and en). BudgetProgressBar color thresholds match UI-SPEC: 0-79% cs.primary, 80-99% warningAmber, ≥100% cs.error. All finance widgets have zero Isar imports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Finance screens, form screens, Finance tab wiring, task-finance link</name>
  <files>
    lib/presentation/finance/screens/finance_dashboard_screen.dart,
    lib/presentation/finance/screens/transaction_list_screen.dart,
    lib/presentation/finance/screens/transaction_form_screen.dart,
    lib/presentation/finance/screens/budget_overview_screen.dart,
    lib/presentation/finance/screens/goal_list_screen.dart,
    lib/presentation/finance/screens/goal_form_screen.dart,
    lib/presentation/finance/screens/goal_detail_screen.dart,
    lib/presentation/finance/screens/debt_list_screen.dart,
    lib/presentation/finance/screens/debt_form_screen.dart,
    lib/presentation/finance/screens/recurring_payment_screen.dart,
    lib/presentation/finance/screens/recurring_payment_form_screen.dart,
    lib/app.dart,
    lib/presentation/tasks/screens/task_form_screen.dart,
    lib/presentation/tasks/screens/task_detail_screen.dart
  </files>
  <read_first>
    lib/presentation/tasks/screens/task_list_screen.dart
    lib/presentation/tasks/screens/task_form_screen.dart
    lib/presentation/tasks/screens/task_detail_screen.dart
    lib/app.dart
    lib/domain/tasks/item.dart
    .planning/phases/03-finance-core/03-UI-SPEC.md (Navigation Architecture, Form Pattern, Interaction Contracts, Add Transaction Flow, Delete Transaction Flow, Add Goal Contribution Flow, Mark Debt as Paid Flow, Budget Limit Set Flow, Task ↔ Finance Link Display)
    .planning/phases/03-finance-core/03-CONTEXT.md (D-19, D-20, D-21, D-22)
  </read_first>
  <action>
    Create lib/presentation/finance/screens/ directory.

    finance_dashboard_screen.dart: StatelessWidget FinanceDashboardScreen. Build: DefaultTabController(length: 6, child: Scaffold(appBar: AppBar(bottom: TabBar(isScrollable: true, tabs: [Tab(text: l10n.dashboardTabLabel), Tab(text: l10n.transactionsTabLabel), Tab(text: l10n.budgetsTabLabel), Tab(text: l10n.goalsTabLabel), Tab(text: l10n.debtsTabLabel), Tab(text: l10n.recurringTabLabel)])), body: MultiBlocProvider(providers: [BlocProvider(create: (_) => getIt<TransactionCubit>()), BlocProvider(create: (_) => getIt<BudgetCubit>()), BlocProvider(create: (_) => getIt<GoalListCubit>()), BlocProvider(create: (_) => getIt<DebtCubit>()), BlocProvider(create: (_) => getIt<RecurringPaymentCubit>()), BlocProvider(create: (_) => getIt<HomeDashboardCubit>())], child: TabBarView(children: [_DashboardTab(), TransactionListScreen(), BudgetOverviewScreen(), GoalListScreen(), DebtListScreen(), RecurringPaymentScreen()])))). _DashboardTab is a placeholder Column('Resumo — ver plano 03-05') — replaced in plan 03-05.

    transaction_list_screen.dart: StatefulWidget. initState: context.read<TransactionCubit>().start(). BlocConsumer<TransactionCubit, TransactionState> builder uses switch on state. On TransactionLoaded with empty list: FinanceEmptyState(icon: Icons.receipt_long_outlined, heading: l10n.emptyTransactions, body: l10n.emptyTransactionsBody, ctaLabel: l10n.addTransaction, onCta: () => Navigator.push to TransactionFormScreen). On TransactionLoaded with data: ListView.builder with TransactionCard per item. FAB: FloatingActionButton(tooltip: l10n.addTransaction, onPressed: () => push TransactionFormScreen). TransactionCard onDelete: cubit.softDelete(tx.id) then ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(l10n.transactionDeleted), duration: AppConstants.undoSnackbarDuration, action: SnackBarAction(label: l10n.undo, onPressed: () => cubit.restoreTransaction(tx.id)))). Add restoreTransaction(int id) to TransactionCubit if not already present. TransactionCard onTap: push TransactionFormScreen(transaction: tx) for edit.

    transaction_form_screen.dart: StatefulWidget. Optional Transaction? transaction param. isEditing = transaction != null. Controllers: _amountController (pre-fill with transaction?.amountCents / 100 as string), _noteController (pre-fill with note). State: _selectedType (TransactionType, default income), _selectedCategory (TransactionCategory?), _selectedDate (DateTime, default today), _selectedGoalId (int?). AppBar title: isEditing ? l10n.editTransaction : l10n.addTransaction. AppBar action: FilledButton(l10n.saveTransaction). Form fields using _FieldRow pattern from UI-SPEC: (1) SegmentedButton<TransactionType>({income: l10n.income, expense: l10n.expense}) toggles _selectedType; (2) TextFormField for amount with TextInputType.numberWithOptions(decimal:true), FilteringTextInputFormatter.allow(RegExp(r'[\d,.]')), validator checks parseDouble > 0; (3) ListTile for category — taps showModalBottomSheet with TransactionCategoryRepository categories filtered by _selectedType; (4) ListTile for date — taps showDatePicker; (5) TextFormField for optional note; (6) if _selectedType == expense: ListTile for goal link — taps showModalBottomSheet with GoalListCubit active goals picker, sets _selectedGoalId. _save(): validate form, amountCents = (double.parse(cleaned) * 100).round(), call cubit.createTransaction or cubit.updateTransaction, Navigator.pop on success. If error state, show ScaffoldMessenger error snackbar. Form card style per UI-SPEC.

    budget_overview_screen.dart: StatefulWidget. initState: context.read<BudgetCubit>().start(). BlocBuilder<BudgetCubit, BudgetState>. On BudgetLoaded: if budgets.isEmpty and no expense categories exist yet: show FinanceEmptyState. Otherwise: ListView of expense categories — each row is a Card containing BudgetProgressBar. Tapping a row opens ModalBottomSheet with single TextFormField pre-filled with existing limit (0 if none). Save button calls cubit.setLimit(categoryId, now.month, now.year, limitCents).

    goal_list_screen.dart: StatefulWidget. initState: context.read<GoalListCubit>().start(). On GoalListLoaded with empty list: FinanceEmptyState with goals config. On GoalListLoaded with data: ListView with GoalProgressCard per goal (pass taggedTransactionsCents = 0 for list view — full computation in detail). GoalProgressCard onTap: push GoalDetailScreen(goalId: goal.id). FAB: push GoalFormScreen.

    goal_form_screen.dart: StatefulWidget with optional SavingsGoal? goal param. Fields: title (TextFormField), targetAmount (decimal TextFormField), optional deadline (DatePicker). AppBar: FilledButton(l10n.saveGoal). _save() calls GoalListCubit.createGoal or updateGoal.

    goal_detail_screen.dart: StatefulWidget with required int goalId. initState: context.read<GoalCubit>().loadGoal(goalId). BlocBuilder on GoalCubit. GoalLoaded state shows: progress header (title, GoalProgressCard-style progress bar, amounts), FilledButton.tonal(l10n.addContribution) showing ModalBottomSheet with amount + optional date + optional note, a ListView of contribution history below. Contribution BottomSheet: TextFormField for amount, optional DatePicker (default today), optional note TextFormField. Save calls GoalCubit.addContribution(goalId, SavingsGoalContribution(amountCents: ..., date: ..., note: ...)). AppBar actions: IconButton(Icons.edit) push GoalFormScreen, IconButton(Icons.delete_outline, tooltip: l10n.deleteGoal) shows AlertDialog confirmation.

    debt_list_screen.dart: StatefulWidget. initState: cubit.start(). On DebtLoaded with empty: FinanceEmptyState with debts config. On DebtLoaded with data: ListView of debt cards. Each card shows title, counterparty, amount, direction chip (a pagar / a receber), dueDate, SwitchListTile for isPaid (toggles via cubit.togglePaid). FAB: push DebtFormScreen. Swipe-to-delete supported via Dismissible same as TransactionCard.

    debt_form_screen.dart: StatefulWidget with optional Debt? debt param. Fields: title, amount, direction (SegmentedButton toPay/toReceive), counterparty, dueDate. AppBar: FilledButton(l10n.saveDebt). _save() calls DebtCubit.createDebt or updateDebt.

    recurring_payment_screen.dart: StatefulWidget. initState: cubit.start(). On RecurringPaymentLoaded with empty: FinanceEmptyState with recurring config. On loaded with data: ListView of recurring payment cards showing title, amount, cycle, nextDueDate, isActive SwitchListTile. FAB: push RecurringPaymentFormScreen.

    recurring_payment_form_screen.dart: StatefulWidget with optional RecurringPayment? payment param. Fields: title, amount, category picker (expense categories only), cycle (DropdownButton for RecurringCycle enum values), nextDueDate (DatePicker). AppBar: FilledButton(l10n.saveRecurringPayment). _save() calls RecurringPaymentCubit.createPayment or updatePayment.

    lib/app.dart: Add FinanceDashboardScreen to _screens list at index 4. Add NavigationDestination(icon: Icon(Icons.account_balance_wallet_outlined), selectedIcon: Icon(Icons.account_balance_wallet), label: l10n.financeTabLabel) at index 4. Import FinanceDashboardScreen. Import l10n key. Note: no BlocProvider needed in app.dart for finance cubits — they are provided inside FinanceDashboardScreen.

    lib/domain/tasks/item.dart: Verify that linkedGoalId (int?) and linkedDebtId (int?) fields are present on the Item domain entity (they should be from Phase 2 extensibility contract). If missing, add them with copyWith support per D-21.

    lib/presentation/tasks/screens/task_form_screen.dart: Add a new optional "Vincular a..." section at the bottom of the form (per D-21). Load available goals via GoalListCubit (needs to be read from context or provided) and active debts via DebtCubit. Render as a ListTile that opens a ModalBottomSheet with two sections: goals (radio-list) and debts (radio-list). Only one can be selected (linkedGoalId and linkedDebtId are mutually exclusive). When user selects a goal, clear linkedDebtId; vice versa. On save, include the selected FK in the Item passed to the cubit. If both are null, do not add to Item. Scope: minimal change — add one optional field to the task form _save() method and one new form card in the UI using the same _FieldRow and Card pattern.

    lib/presentation/tasks/screens/task_detail_screen.dart: In the detail card, after the existing fields, add a conditional section: if item.linkedGoalId != null OR item.linkedDebtId != null, render ActionChip(avatar: Icon(Icons.link, size:16), label: Text('Ligado a: {entity name}'), backgroundColor: cs.secondaryContainer, onPressed: navigate to goal detail or debt list). For the entity name: read it from GoalListCubit or DebtCubit state already in context. If neither cubit is in context (task detail opened from non-finance context), show 'Ligado a: #${id}' as fallback. If both FKs are null, render nothing — no empty chip section shown.
  </action>
  <verify>
    <automated>flutter analyze lib/presentation/finance/ lib/app.dart lib/presentation/tasks/screens/task_form_screen.dart lib/presentation/tasks/screens/task_detail_screen.dart</automated>
  </verify>
  <done>
    flutter analyze reports 0 errors for all modified presentation files. app.dart _screens list has 5 elements with FinanceDashboardScreen at index 4. finance_dashboard_screen.dart contains DefaultTabController(length: 6). All 6 list screens have FinanceEmptyState with CTA. budget_progress_bar.dart color logic covers all 3 thresholds. ARB files contain financeTabLabel key in both locales. flutter gen-l10n completes without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 10 finance screens, 4 widgets, 2 form screens for task↔finance link, Finance tab in NavigationBar, all ARB strings. Full CRUD flows for transactions, budgets, goals, debts, recurring payments.
  </what-built>
  <how-to-verify>
    1. Run `flutter run` (Android emulator or device)
    2. Tap the "Finanças" tab (5th tab in NavigationBar at bottom)
    3. Verify: 6 sub-tabs appear: Resumo, Transações, Orçamentos, Objetivos, Dívidas, Recorrências
    4. Tap "Transações" → verify empty state shows icon + "Nenhuma transação" + "Adicionar transação" button
    5. Tap FAB or CTA button → TransactionFormScreen opens; verify SegmentedButton (Receita/Despesa), amount field, category picker opens BottomSheet when tapped
    6. Log one income (e.g., Salário, MT 5000) and one expense (e.g., Alimentação, MT 200) → verify both appear in list with green/red amounts
    7. Swipe left on a transaction → verify red background + delete confirmation snackbar with "Desfazer"
    8. Tap "Orçamentos" → verify all expense categories shown with "Sem limite definido"; tap a row → BottomSheet opens to set limit
    9. Tap "Objetivos" → verify empty state; tap CTA → GoalFormScreen opens
    10. Tap "Dívidas" → verify empty state with "Nenhuma dívida"
    11. Open any task → verify task detail screen (check for ActionChip section — should be absent if no links set)
    12. Open a task form → verify "Vincular a..." section appears at bottom
    13. (Optional) Tap "Resumo" — a placeholder message is acceptable here (charts come in 03-05)
  </how-to-verify>
  <resume-signal>Type "approved" if all screens render correctly, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → TransactionFormScreen → Cubit | Amount field, note field, category selection enter the system here |
| User action → DebtCubit.togglePaid | Simple toggle — no sensitive input |
| Task form → Item.linkedGoalId/linkedDebtId | FK values sourced from cubit state — not freeform user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Tampering | TransactionFormScreen amount field | mitigate | Input filtered to digits + comma + period via FilteringTextInputFormatter. Amount validated > 0 before cubit call. ValidationFailure returned from repo if bypass attempted. |
| T-03-04-02 | Tampering | Category picker — user can only select categories from repository list | accept | Categories come from TransactionCategoryRepository (seeded + user-created). FK written by the app, not free-typed. Low risk. |
| T-03-04-03 | Tampering | linkedGoalId / linkedDebtId in task form | accept | Values populated from cubit state (active goals/debts list). Not freeform text. If the goal/debt no longer exists when chip is tapped, navigation simply fails gracefully. |
| T-03-04-04 | Spoofing | Delete confirmation (AlertDialog) — swipe vs. detail screen | mitigate | Swipe delete uses SnackBar undo (5s soft delete) per UI-SPEC. Detail screen uses AlertDialog before permanent action. Both paths have undo/cancel escape. |
| T-03-04-05 | Information Disclosure | Currency symbol displayed from SharedPreferences | accept | `userCurrency` SharedPreferences key is not sensitive — it is a display preference (e.g. "MT", "R$"). No financial data in the preference. |
</threat_model>

<verification>
flutter analyze lib/presentation/finance/ lib/app.dart lib/presentation/tasks/screens/task_form_screen.dart
flutter test test/presentation/finance/ -x
flutter gen-l10n
grep -c "financeTabLabel" lib/config/l10n/app_pt_BR.arb
grep -c "FinanceDashboardScreen" lib/app.dart
</verification>

<success_criteria>
1. flutter analyze reports 0 errors across all modified presentation files
2. flutter test test/presentation/finance/ passes empty state widget test
3. flutter gen-l10n completes without errors (all ARB keys present in both locales)
4. app.dart _screens has 5 entries; FinanceDashboardScreen at index 4
5. finance_dashboard_screen.dart has DefaultTabController(length: 6)
6. All 5 list screens (transaction, budget, goal, debt, recurring) show FinanceEmptyState when list is empty
7. Human checkpoint approved — all screens render visually correct on device/emulator
</success_criteria>

<output>
After completion, create .planning/phases/03-finance-core/03-04-SUMMARY.md
</output>
