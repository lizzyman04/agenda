---
phase: 03-finance-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/domain/finance/transaction.dart
  - lib/domain/finance/transaction_type.dart
  - lib/domain/finance/transaction_repository.dart
  - lib/domain/finance/transaction_category.dart
  - lib/domain/finance/transaction_category_repository.dart
  - lib/domain/finance/budget.dart
  - lib/domain/finance/budget_repository.dart
  - lib/domain/finance/savings_goal.dart
  - lib/domain/finance/savings_goal_contribution.dart
  - lib/domain/finance/goal_repository.dart
  - lib/domain/finance/debt.dart
  - lib/domain/finance/debt_direction.dart
  - lib/domain/finance/debt_repository.dart
  - lib/domain/finance/recurring_payment.dart
  - lib/domain/finance/recurring_cycle.dart
  - lib/domain/finance/recurring_payment_repository.dart
  - lib/core/constants/currencies.dart
  - lib/core/constants/finance_colors.dart
  - lib/core/utils/amount_formatter.dart
  - test/domain/finance/transaction_test.dart
  - test/domain/finance/savings_goal_test.dart
autonomous: true
requirements:
  - FIN-01
  - FIN-02
  - FIN-03
  - FIN-04
  - FIN-05
  - FIN-06
  - FIN-07
  - FIN-08

must_haves:
  truths:
    - "Transaction domain entity exists with amountCents (int), type, categoryId, date, note, linkedGoalId; no double money fields"
    - "SavingsGoal entity exposes amountSavedCents getter aggregating contributions + taggedTransactionsCents"
    - "Goal progress percentage is computable from domain entity without Isar"
    - "All repository interfaces return AsyncResult<T>; Stream<void> watchChanges() on every interface"
    - "Currency constant list exists with full ISO 4217 entries and MZN first"
    - "Domain tests pass: int-cents arithmetic, goal progress computation, balance formula"
  artifacts:
    - path: "lib/domain/finance/transaction.dart"
      provides: "Transaction domain entity — pure Dart, zero Flutter/Isar imports"
      exports: ["Transaction", "clearField"]
    - path: "lib/domain/finance/transaction_repository.dart"
      provides: "TransactionRepository abstract interface"
      exports: ["TransactionRepository"]
    - path: "lib/domain/finance/savings_goal.dart"
      provides: "SavingsGoal entity with amountSavedCents + progressPercent getters"
      exports: ["SavingsGoal"]
    - path: "lib/core/constants/currencies.dart"
      provides: "Currencies.all (full ISO 4217) + Currencies.priorityCodes"
      exports: ["Currencies", "Currency"]
    - path: "lib/core/utils/amount_formatter.dart"
      provides: "formatAmount(int amountCents, String symbol, Locale locale) -> String"
      exports: ["formatAmount"]
    - path: "lib/core/constants/finance_colors.dart"
      provides: "FinanceColors semantic constants (incomeGreen, expenseRed, warningAmber)"
      exports: ["FinanceColors"]
  key_links:
    - from: "lib/domain/finance/savings_goal.dart"
      to: "lib/domain/finance/savings_goal_contribution.dart"
      via: "SavingsGoal.contributions field (List<SavingsGoalContribution>)"
      pattern: "contributions.*SavingsGoalContribution"
    - from: "lib/domain/finance/transaction_repository.dart"
      to: "lib/core/failures/result.dart"
      via: "AsyncResult<T> return type"
      pattern: "AsyncResult"
    - from: "lib/core/utils/amount_formatter.dart"
      to: "intl/intl.dart"
      via: "NumberFormat.currency(locale: ...)"
      pattern: "NumberFormat\\.currency"
---

<objective>
Define the entire finance domain in pure Dart: five entities, their enums, five repository interfaces, and three core utilities (Currency constants, finance semantic colors, amount formatter).

Purpose: Domain-first architecture ensures data and application layers have stable contracts to build against. Zero Flutter/Isar imports in this layer — it is the dependency-free core.
Output: 16 Dart source files + 2 test files. All downstream plans (03-02 through 03-05) import from these files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-finance-core/03-CONTEXT.md
@.planning/phases/03-finance-core/03-RESEARCH.md
@.planning/phases/03-finance-core/03-PATTERNS.md

<interfaces>
<!-- Key contracts from the existing codebase that this plan extends. -->
<!-- Executor: use these directly — no further codebase exploration needed. -->

From lib/core/failures/result.dart:
```dart
// Result<T> and AsyncResult<T> — every repository method returns these
typedef Result<T> = Ok<T> | Err<T>;      // sealed
typedef AsyncResult<T> = Future<Result<T>>;

final class Ok<T> {
  const Ok(this.value);
  final T value;
}

final class Err<T> {
  const Err(this.failure);
  final Failure failure;
}
```

From lib/core/failures/failure.dart (sealed hierarchy — no new subtypes needed):
```dart
sealed class Failure { const Failure(this.message); final String message; }
final class DatabaseFailure extends Failure { const DatabaseFailure(super.message); }
final class ValidationFailure extends Failure { const ValidationFailure(super.message); }
```

From lib/domain/tasks/item.dart (domain entity pattern to replicate):
```dart
// clearField sentinel for nullable copyWith params
const Object clearField = _Absent();
final class _Absent { const _Absent(); }

class Item {
  const Item({required this.id, /* ... all required fields ... */});
  // Pure Dart — zero Flutter/Isar imports
  Item copyWith({int? id, Object? note = clearField, /* ... */}) { ... }
}
```

From lib/domain/tasks/item_repository.dart (interface pattern):
```dart
abstract class ItemRepository {
  AsyncResult<Item> createItem(Item item);
  AsyncResult<Item> getItem(int id);
  AsyncResult<List<Item>> filterItems({ /* ... */ });
  AsyncResult<Item> updateItem(Item item);
  AsyncResult<Item> softDelete(int id);
  AsyncResult<Item> restoreItem(int id);
  Stream<void> watchChanges();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finance domain entities and enums</name>
  <files>
    lib/domain/finance/transaction.dart,
    lib/domain/finance/transaction_type.dart,
    lib/domain/finance/transaction_category.dart,
    lib/domain/finance/budget.dart,
    lib/domain/finance/savings_goal.dart,
    lib/domain/finance/savings_goal_contribution.dart,
    lib/domain/finance/debt.dart,
    lib/domain/finance/debt_direction.dart,
    lib/domain/finance/recurring_payment.dart,
    lib/domain/finance/recurring_cycle.dart,
    test/domain/finance/transaction_test.dart,
    test/domain/finance/savings_goal_test.dart
  </files>
  <read_first>
    lib/domain/tasks/item.dart
    lib/domain/tasks/item_type.dart
    lib/domain/tasks/priority.dart
    lib/core/failures/result.dart
    .planning/phases/03-finance-core/03-CONTEXT.md (decisions D-07, D-08, D-11, D-12, D-13)
    .planning/phases/03-finance-core/03-PATTERNS.md (Domain Entity Pattern, Cubit State Pattern — HomeDashboardLoaded state shape)
  </read_first>
  <behavior>
    - Test: Transaction with amountCents=12345, type=expense has amountCents==12345 (no double)
    - Test: Transaction with amountCents=0 is still constructable at domain level (validation is repository-layer)
    - Test: SavingsGoal.amountSavedCents = sum of contributions.amountCents + taggedTransactionsCents param
    - Test: SavingsGoal.progressPercent = amountSavedCents / targetAmountCents; returns 0.0 when targetAmountCents==0
    - Test: SavingsGoal.progressPercent caps computation is accurate (e.g. 15000 / 10000 = 1.5 — no UI cap in domain)
    - Test: computeBalance([income:5000, income:3000, expense:2000]) = 6000 (pure int arithmetic)
    - Test: computeNetWorth(balance:6000, goalAmountSaved:2000, toPayDebts:[4000]) = 4000
  </behavior>
  <action>
    Create lib/domain/finance/ directory with these files. Zero Flutter/Isar imports in all domain files.

    transaction_type.dart: enum TransactionType { income, expense } — pure Dart, no annotation (annotation is data layer only).

    transaction_category.dart: class TransactionCategory with fields id (int), namePtBr (String), nameEn (String?), type (TransactionType), isDefault (bool), createdAt (DateTime). Include clearField sentinel and copyWith. No Isar annotations.

    transaction.dart: class Transaction with fields id (int), type (TransactionType), amountCents (int), categoryId (int), date (DateTime), createdAt (DateTime), updatedAt (DateTime), note (String?), linkedGoalId (int?), deletedAt (DateTime?). Include clearField sentinel and copyWith per D-13. Pure Dart — no double for money.

    savings_goal_contribution.dart: class SavingsGoalContribution with fields amountCents (int), date (DateTime), note (String?). Value object — no id, no copyWith needed. Const constructor.

    savings_goal.dart: class SavingsGoal with fields id (int), title (String), targetAmountCents (int), contributions (List<SavingsGoalContribution>), isCompleted (bool), createdAt (DateTime), updatedAt (DateTime), deadline (DateTime?), deletedAt (DateTime?). Computed getters per D-11: amountSavedCents(int taggedTransactionsCents) method that returns contributions.fold(0, (s,c) => s + c.amountCents) + taggedTransactionsCents. Also provide progressPercent(int taggedTransactionsCents) = targetAmountCents == 0 ? 0.0 : amountSavedCents(taggedTransactionsCents) / targetAmountCents. Include clearField + copyWith.

    budget.dart: class Budget with fields id (int), categoryId (int), month (int, 1-12), year (int), limitCents (int), createdAt (DateTime), updatedAt (DateTime). copyWith included.

    debt_direction.dart: enum DebtDirection { toPay, toReceive }

    debt.dart: class Debt with fields id (int), title (String), amountCents (int), direction (DebtDirection), counterparty (String), dueDate (DateTime), isPaid (bool), createdAt (DateTime), updatedAt (DateTime), paidAt (DateTime?), deletedAt (DateTime?). clearField + copyWith per D-07.

    recurring_cycle.dart: enum RecurringCycle { daily, weekly, biweekly, monthly, quarterly, yearly }

    recurring_payment.dart: class RecurringPayment with fields id (int), title (String), amountCents (int), categoryId (int), cycle (RecurringCycle), nextDueDate (DateTime), isActive (bool), createdAt (DateTime), updatedAt (DateTime), deletedAt (DateTime?). clearField + copyWith.

    Test files: test/domain/finance/transaction_test.dart and test/domain/finance/savings_goal_test.dart. Write tests first (RED), then implement entities to pass (GREEN). Tests use dart:test only — no flutter_test, no bloc_test, no mocktail needed at domain level. Import package:agenda/domain/finance/... paths only.

    Also create two free-standing test helper functions in transaction_test.dart: computeBalance(List<Transaction>) and computeNetWorth(int balance, List<SavingsGoal> goals, List<Debt> debts). These test the D-07/D-08 formulas in pure Dart before they appear in HomeDashboardCubit.
  </action>
  <verify>
    <automated>flutter test test/domain/finance/ -x</automated>
  </verify>
  <done>
    All 7 behavior tests pass. Domain entities exist at correct paths. No Flutter or Isar import appears in any lib/domain/finance/ file. grep -rn "import 'package:flutter" lib/domain/finance/ returns no output. grep -rn "isar" lib/domain/finance/ returns no output.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repository interfaces + core utilities</name>
  <files>
    lib/domain/finance/transaction_repository.dart,
    lib/domain/finance/transaction_category_repository.dart,
    lib/domain/finance/budget_repository.dart,
    lib/domain/finance/goal_repository.dart,
    lib/domain/finance/debt_repository.dart,
    lib/domain/finance/recurring_payment_repository.dart,
    lib/core/constants/currencies.dart,
    lib/core/constants/finance_colors.dart,
    lib/core/utils/amount_formatter.dart
  </files>
  <read_first>
    lib/domain/tasks/item_repository.dart
    lib/core/failures/result.dart
    lib/core/constants/app_constants.dart
    .planning/phases/03-finance-core/03-CONTEXT.md (D-14, D-15, D-16, D-17, D-18)
    .planning/phases/03-finance-core/03-UI-SPEC.md (Number Formatting Contract, Color section)
    .planning/phases/03-finance-core/03-PATTERNS.md (Repository Interface Pattern, Core Constants Pattern)
  </read_first>
  <behavior>
    - Test: formatAmount(125000, 'MT', Locale('pt', 'BR')) returns 'MT 1.250,00'
    - Test: formatAmount(125000, 'R\$', Locale('en')) returns 'R\$ 1,250.00'
    - Test: formatAmount(0, 'MT', Locale('pt', 'BR')) returns 'MT 0,00'
    - Test: Currencies.all is not empty and first entry has code 'MZN'
    - Test: Currencies.priorityCodes contains 'MZN', 'BRL', 'USD', 'EUR', 'GBP'
  </behavior>
  <action>
    Create five repository interfaces in lib/domain/finance/. Each is an abstract class. Zero Flutter/Isar imports. All methods return AsyncResult<T> or Stream<void>.

    transaction_repository.dart: abstract class TransactionRepository with methods:
    - AsyncResult<Transaction> createTransaction(Transaction transaction)
    - AsyncResult<Transaction> getTransaction(int id)
    - AsyncResult<List<Transaction>> getTransactions() — active only (500 cap enforced at impl)
    - AsyncResult<List<Transaction>> getByMonth(int month, int year) — expense transactions only, for budget/chart
    - AsyncResult<List<Transaction>> getByLinkedGoal(int goalId) — tagged transactions for goal progress
    - AsyncResult<Transaction> updateTransaction(Transaction transaction)
    - AsyncResult<Transaction> softDelete(int id)
    - Stream<void> watchChanges()

    transaction_category_repository.dart: abstract class TransactionCategoryRepository with methods:
    - AsyncResult<List<TransactionCategory>> getAll()
    - AsyncResult<List<TransactionCategory>> getByType(TransactionType type)
    - AsyncResult<TransactionCategory> create(TransactionCategory category)
    - AsyncResult<TransactionCategory> update(TransactionCategory category)
    - AsyncResult<void> delete(int id) — hard delete; caller must verify isDefault==false first

    budget_repository.dart: abstract class BudgetRepository with methods:
    - AsyncResult<Budget?> getForCategory(int categoryId, int month, int year) — nullable: null means no limit set
    - AsyncResult<List<Budget>> getForMonth(int month, int year)
    - AsyncResult<Budget> setLimit(int categoryId, int month, int year, int limitCents) — upsert
    - AsyncResult<void> delete(int categoryId, int month, int year)

    goal_repository.dart: abstract class GoalRepository with methods:
    - AsyncResult<SavingsGoal> createGoal(SavingsGoal goal)
    - AsyncResult<SavingsGoal> getGoal(int id)
    - AsyncResult<List<SavingsGoal>> getActiveGoals()
    - AsyncResult<SavingsGoal> updateGoal(SavingsGoal goal)
    - AsyncResult<SavingsGoal> addContribution(int goalId, SavingsGoalContribution contribution)
    - AsyncResult<SavingsGoal> softDelete(int id)
    - Stream<void> watchChanges()

    debt_repository.dart: abstract class DebtRepository with methods:
    - AsyncResult<Debt> createDebt(Debt debt)
    - AsyncResult<Debt> getDebt(int id)
    - AsyncResult<List<Debt>> getDebts() — active only
    - AsyncResult<Debt> updateDebt(Debt debt)
    - AsyncResult<Debt> togglePaid(int id) — flips isPaid, sets paidAt = now or null
    - AsyncResult<Debt> softDelete(int id)
    - Stream<void> watchChanges()

    recurring_payment_repository.dart: abstract class RecurringPaymentRepository with methods:
    - AsyncResult<RecurringPayment> createPayment(RecurringPayment payment)
    - AsyncResult<RecurringPayment> getPayment(int id)
    - AsyncResult<List<RecurringPayment>> getActivePayments()
    - AsyncResult<RecurringPayment> updatePayment(RecurringPayment payment)
    - AsyncResult<RecurringPayment> softDelete(int id)

    lib/core/constants/currencies.dart: Define Currency value object (const constructor, code/name/symbol String fields, no Isar annotation) and abstract final class Currencies with static const List<Currency> all containing full ISO 4217 list per D-17. Start with priority entries: MZN ('Metical Moçambicano','MT'), BRL ('Real Brasileiro','R\$'), USD ('US Dollar','\$'), EUR ('Euro','€'), GBP ('British Pound','£'), JPY ('Japanese Yen','¥'), CAD ('Canadian Dollar','C\$'), AUD ('Australian Dollar','A\$'), CHF ('Swiss Franc','CHF'), CNY ('Chinese Yuan','¥'), ZAR ('South African Rand','R'). Then continue with remaining ISO 4217 currencies covering at least 150 entries total. Include static const List<String> priorityCodes listing the 11 priority codes. No network call, no external package — pure static const Dart.

    lib/core/constants/finance_colors.dart: abstract final class FinanceColors with three static const Color fields: incomeGreen = Color(0xFF2E7D32), expenseRed = Color(0xFFC62828), warningAmber = Color(0xFFF57F17). Import only package:flutter/material.dart.

    lib/core/utils/amount_formatter.dart: Top-level function formatAmount(int amountCents, String currencySymbol, Locale locale) -> String. Implementation: divide by 100.0, apply NumberFormat.currency(locale: locale.toString(), symbol: '', decimalDigits: 2), prepend symbol + space. Per D-18: 'MT 1.234,56' in PT-BR, 'MT 1,234.56' in EN. Import package:intl/intl.dart and package:flutter/widgets.dart (for Locale). Write 5 unit tests in test/core/utils/amount_formatter_test.dart covering PT-BR and EN locales and zero amount.
  </action>
  <verify>
    <automated>flutter test test/domain/finance/ test/core/utils/amount_formatter_test.dart -x</automated>
  </verify>
  <done>
    formatAmount tests pass for PT-BR and EN locales. Currencies.all contains MZN first, has >= 100 entries. FinanceColors.incomeGreen == Color(0xFF2E7D32). All five repository interfaces compile (dart analyze lib/domain/finance/ returns zero errors). No Flutter/Isar imports in lib/domain/finance/ files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart type system → domain entity | Domain entities are constructed from validated inputs; no user-facing parsing in this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | SavingsGoal.amountSavedCents | mitigate | Domain getter computes from immutable contribution list — no mutation path; test coverage verifies int arithmetic exactness |
| T-03-01-02 | Tampering | Transaction.amountCents field | accept | Domain entity is a value object; no validation here — validation occurs at repository boundary (03-02). Risk: low at domain layer. |
| T-03-01-03 | Information Disclosure | Currency constants | accept | Static list of public ISO 4217 data — no PII, no secrets. No risk. |
| T-03-01-04 | Tampering | formatAmount double conversion | accept | Conversion is display-only (amountCents / 100.0); result is never stored. Floating-point display rounding is acceptable for presentation. |
</threat_model>

<verification>
flutter test test/domain/finance/ test/core/utils/amount_formatter_test.dart -x
dart analyze lib/domain/finance/ lib/core/constants/currencies.dart lib/core/constants/finance_colors.dart lib/core/utils/amount_formatter.dart
grep -rn "import 'package:flutter" lib/domain/finance/ | grep -v "^Binary"
grep -rn "isar" lib/domain/finance/ | grep -v "^Binary"
</verification>

<success_criteria>
1. flutter test test/domain/finance/ passes all 7+ behavior tests (int-cents arithmetic, goal progress, balance formula, net worth formula)
2. flutter test test/core/utils/amount_formatter_test.dart passes all 5 formatting tests
3. dart analyze reports 0 errors across all 16 new files
4. No Flutter or Isar import appears in any lib/domain/finance/ file
5. Currencies.all has MZN as first entry and contains >= 100 ISO 4217 currencies
6. All repository interfaces declare Stream<void> watchChanges() (except BudgetRepository and RecurringPaymentRepository which are not reactive — they are loaded on demand)
</success_criteria>

<output>
After completion, create .planning/phases/03-finance-core/03-01-SUMMARY.md
</output>
