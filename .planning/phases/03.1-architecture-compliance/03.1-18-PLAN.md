---
phase: 03.1
plan: 18
type: execute
wave: 5
depends_on: ["03.1-01", "03.1-02", "03.1-03", "03.1-04", "03.1-05", "03.1-06", "03.1-07", "03.1-08", "03.1-09", "03.1-10", "03.1-11", "03.1-12", "03.1-13", "03.1-14", "03.1-15", "03.1-16", "03.1-17"]
files_modified:
  - "README.md files under every directory in lib/presentation/ and lib/application/ (enumerated at execution time — see Task 1/2)"
  - .github/workflows/ci.yml
  - tool/architecture_exemptions.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every directory under lib/presentation/ and lib/application/ has a README.md stating its responsibility, its contents, and any slice-specific rules — matching the literal, mechanically-checkable reading of SC-3 adopted in plan 03.1-01"
    - "`dart run tool/check_architecture.dart` exits 0 against the final tree — the guard is now enforcing, not just informational"
    - "CI runs the guard on every push/PR, immediately after the existing Analyze step"
    - "The full test suite (≥230 tests) and flutter analyze remain green — this final plan touches only README.md files and CI config, never lib/ source"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Architecture guard step wired in immediately after Analyze, before Test"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tool/check_architecture.dart"
      via: "run: dart run tool/check_architecture.dart"
      pattern: "check_architecture.dart"
---

<objective>
Close SC-3 (README per nest) across the final directory layout this phase produced, then flip the guard built in `3.1-01` from informational-only to CI-enforcing — the last task of the phase, run only once every other plan has landed so the guard's first enforcing run passes cleanly rather than immediately failing CI on day one.

**README scope decision (resolving research's Open Question 1):** every directory under `lib/presentation/` and `lib/application/` gets a `README.md`, including pure-umbrella leaf directories with zero direct `.dart` files (e.g. `lib/application/`, `lib/presentation/finance/`) and generic `screens/`/`widgets/` leaves — the literal reading, adopted because it is mechanically checkable by the guard script with no "does this directory have direct files" branching, and matches the roadmap's own "28 of 30 feature dirs still missing one" framing. Cost is one short paragraph per leaf directory, not a maintenance burden. `tool/architecture_exemptions.dart`'s `readmeExemptDirs` stays empty, confirming this decision in code.

Purpose: closes the last SC-3 gap and makes future architecture drift impossible to merge silently.
Output: a README.md in every presentation/application directory; CI enforces the guard from this point forward.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@lib/presentation/tasks/form/README.md
@lib/presentation/finance/goals/README.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write READMEs for every tasks-side and shared directory</name>
  <files>(one README.md per directory under lib/application/, lib/application/tasks/, lib/application/shared/, lib/presentation/, lib/presentation/tasks/ — enumerated by the command in this task's action)</files>
  <read_first>
    lib/presentation/tasks/form/README.md, lib/presentation/finance/goals/README.md (the two reference templates — match their heading structure: Responsibility, Layout, per-subdirectory file/role tables, Conventions, Upstream dependencies)
  </read_first>
  <action>
    Run `find lib/application lib/presentation/tasks -type d` (this covers `application/` root, `application/shared/`, `application/shared/locale/`, `application/tasks/`, `application/tasks/{day_planner,project,task_list}/`, `presentation/tasks/` and its full subtree including `form/`, `form/screens/`, `form/widgets/`, `form/gtd/`, `form/gtd/screens/`, `form/gtd/widgets/`, `screens/`, `widgets/`, `widgets/detail/`) to get the authoritative current list — this will differ slightly from the phase's original 30-directory baseline because plans `3.1-02`, `3.1-03`, `3.1-06` added new subfolders (`form/task_form_logic.dart`'s parent, `widgets/detail/`).

    For each directory that does NOT already have a `README.md` (i.e. every one except `presentation/tasks/form/`, which already has one — verify it and update its file/role tables if plan `3.1-02` didn't already do so), write a `README.md`. For pure-umbrella directories with zero direct `.dart` files (`lib/application/`, `lib/application/shared/`, `lib/application/tasks/`, `lib/presentation/`, `lib/presentation/tasks/`), write a short (5-15 line) README stating only what the directory groups and pointing to its subdirectories — do not pad these with invented detail. For directories with direct `.dart` files (`application/shared/locale/`, `application/tasks/day_planner/`, `application/tasks/project/`, `application/tasks/task_list/`, `presentation/tasks/screens/`, `presentation/tasks/widgets/`, `presentation/tasks/widgets/detail/`, `presentation/tasks/form/screens/`, `presentation/tasks/form/widgets/`, `presentation/tasks/form/gtd/`, `presentation/tasks/form/gtd/screens/`, `presentation/tasks/form/gtd/widgets/`), write a file/role table listing each `.dart` file and its one-line role, following the exact table format used in `form/README.md`.
  </action>
  <acceptance_criteria>
    - `for d in $(find lib/application lib/presentation/tasks -type d); do test -f "$d/README.md" || echo "MISSING: $d"; done` prints nothing.
    - Every generated `README.md` is non-empty and contains at minimum a one-sentence responsibility statement (spot-check: `for f in $(find lib/application lib/presentation/tasks -name README.md); do wc -l "$f"; done` — every count &gt; 3).
  </acceptance_criteria>
  <verify>
    <automated>for d in $(find lib/application lib/presentation/tasks -type d); do test -f "$d/README.md" || echo "MISSING: $d"; done</automated>
  </verify>
  <done>Every directory under lib/application/ and lib/presentation/tasks/ has a README.md.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write READMEs for every finance-side directory</name>
  <files>(one README.md per directory under lib/application/finance/ and lib/presentation/finance/ — enumerated by the command in this task's action)</files>
  <read_first>lib/presentation/finance/goals/README.md (already exists — verify/update its widgets table to include goal_form_fields.dart from plan 3.1-15)</read_first>
  <action>
    Run `find lib/application/finance lib/presentation/finance -type d` — covers `application/finance/` root + its 6 entity subfolders, `presentation/finance/` root, `presentation/finance/screens/`, `presentation/finance/widgets/` + its 5 new entity subfolders (`transaction/`, `recurring/`, `debt/`, `budget/`, `dashboard/`) from plans `3.1-08`–`3.1-10`/`3.1-12`–`3.1-14`, and `presentation/finance/goals/` + its `screens/`/`widgets/` subfolders.

    Same rules as Task 1: umbrella directories (`application/finance/`, `presentation/finance/`, `presentation/finance/widgets/`) get short pointer READMEs; directories with direct files get a file/role table. `presentation/finance/goals/README.md` already exists — verify it reflects the final state (add `goal_form_fields.dart` to its widgets table if plan `3.1-15` didn't already update it) rather than recreating it from scratch.
  </action>
  <acceptance_criteria>
    - `for d in $(find lib/application/finance lib/presentation/finance -type d); do test -f "$d/README.md" || echo "MISSING: $d"; done` prints nothing.
    - `grep -q "goal_form_fields" lib/presentation/finance/goals/README.md` succeeds (confirms the existing goals README was updated, not left stale).
  </acceptance_criteria>
  <verify>
    <automated>for d in $(find lib/application/finance lib/presentation/finance -type d); do test -f "$d/README.md" || echo "MISSING: $d"; done</automated>
  </verify>
  <done>Every directory under lib/application/finance/ and lib/presentation/finance/ has a README.md; the pre-existing goals README reflects the final file set.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Enforce the guard in CI and confirm full compliance</name>
  <files>.github/workflows/ci.yml, tool/architecture_exemptions.dart</files>
  <read_first>.github/workflows/ci.yml (full file), tool/check_architecture.dart (confirm its exit-code contract)</read_first>
  <action>
    Run `dart run tool/check_architecture.dart` against the final tree. If it reports any remaining violation, fix it now (this is the phase's final compliance gate — do not proceed to CI wiring with known violations). Confirm `tool/architecture_exemptions.dart`'s `readmeExemptDirs` is still empty (per the README-scope decision) and `lineLimitExemptions` still contains only the `currencies.dart` entry.

    Add a new step to `.github/workflows/ci.yml` immediately after the existing `Analyze` step and before `Test`: `- name: Architecture guard` / `  run: dart run tool/check_architecture.dart`.
  </action>
  <acceptance_criteria>
    - `dart run tool/check_architecture.dart; echo "exit: $?"` reports exit code 0 with "Architecture guard: PASS".
    - `grep -A2 "Architecture guard" .github/workflows/ci.yml` shows the new step positioned between `Analyze` and `Test`.
    - `flutter analyze --no-fatal-infos --fatal-warnings` reports zero errors/warnings (SC-5, final confirmation).
    - `flutter test --no-pub` full suite passes with test count ≥ 230 (SC-4, final confirmation).
  </acceptance_criteria>
  <verify>
    <automated>dart run tool/check_architecture.dart && flutter analyze --no-fatal-infos --fatal-warnings && flutter test --no-pub</automated>
  </verify>
  <done>Guard exits 0 against the final tree; CI wired to run it on every push/PR after Analyze; flutter analyze clean; full suite green at ≥230 tests — all five phase success criteria satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CI pipeline | Adding a new required step to `.github/workflows/ci.yml` — if the guard has a bug, it could start blocking merges on a false positive. Mitigated by this plan's Task 3 explicitly confirming a clean `dart run tool/check_architecture.dart` PASS before wiring it in, and by `3.1-01`'s unit tests covering the guard's own logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-03.1-18-01 | Denial of Service (CI false-positive block) | New CI step | mitigate | Task 3 verifies a clean PASS locally before adding the CI step; the guard itself was unit-tested against fixtures in `3.1-01`, independent of the live tree's state. |
</threat_model>

<verification>
Every directory under `lib/presentation/` + `lib/application/` has a README.md. `dart run tool/check_architecture.dart` exits 0. CI runs the guard after Analyze. `flutter analyze` clean. `flutter test --no-pub` green at ≥230 tests. All five ROADMAP success criteria for Phase 3.1 are now satisfied.
</verification>

<success_criteria>
100% README coverage across presentation/+application/ directories (literal reading). Guard enforcing in CI. All five phase success criteria verified green in one final pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-architecture-compliance/03.1-18-SUMMARY.md`
</output>
