---
phase: 03.1
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/application/finance/dashboard/home_dashboard_cubit.dart
  - lib/application/finance/dashboard/dashboard_aggregator.dart
  - lib/application/finance/budget/budget_cubit.dart
  - lib/application/finance/budget/budget_aggregator.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Dashboard balance, net worth, and category-spend chart data are computed identically to before (same D-07/D-08/D-09 formulas)"
    - "Budget overview's spent/limit map is computed identically to before for every category"
    - "home_dashboard_cubit.dart and budget_cubit.dart are at or under 150 lines"
  artifacts:
    - path: "lib/application/finance/dashboard/dashboard_aggregator.dart"
      provides: "computeBalance, computeGoalsSaved, computeNetWorth, computeCategorySpend — pure functions extracted from HomeDashboardCubit._reload()"
    - path: "lib/application/finance/budget/budget_aggregator.dart"
      provides: "mergeBudgetData({required transactions, required budgets}) -> Map<int, ({int limitCents, int spentCents})>"
  key_links:
    - from: "lib/application/finance/dashboard/home_dashboard_cubit.dart"
      to: "lib/application/finance/dashboard/dashboard_aggregator.dart"
      via: "computeBalance(allTx) / computeNetWorth(...) calls inside _reload()"
      pattern: "computeNetWorth\\("
    - from: "lib/application/finance/budget/budget_cubit.dart"
      to: "lib/application/finance/budget/budget_aggregator.dart"
      via: "mergeBudgetData(transactions: transactions, budgets: budgets) call inside _reload()"
      pattern: "mergeBudgetData\\("
---

<objective>
Apply Pattern 3 (pure-function extraction) to the two finance application-layer cubits over the line cap. Both `_reload()` methods interleave repository-calling orchestration (which must stay in the cubit — it needs `isClosed` checks and `emit`) with pure aggregation math (which does not need any cubit state and is the actual bulk of each file). Per research's Open Question 3, `budget_cubit.dart` gets the same aggregator treatment as `home_dashboard_cubit.dart` even though it is only 5 lines over the cap, for shape-consistency between the two cubits.

Purpose: closes both finance-dashboard and budget violations, and makes the aggregation math independently unit-testable (previously only reachable through the full cubit + mocked repositories).
Output: both cubits ≤150 lines; two new aggregator files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03.1-architecture-compliance/03.1-RESEARCH.md
</context>

<interfaces>
`home_dashboard_cubit.dart` (202 lines) `_reload()` (79-195): Step 2 balance loop (95-105, income/expense sum), Step 3 goals-saved computation (107-134, `taggedByGoal` map + `goalsSavedTotal` fold using `SavingsGoal.amountSavedCents`), Step 4 debt total (136-157, filter toPay&&!isPaid + fold), `netWorth = balance + goalsSavedTotal - debtTotal` (157), Step 5 categorySpend loop (159-172). Steps 1/3(load)/4(load)/6 (repository calls + `Success`/`Err` branching) stay in the cubit.

`budget_cubit.dart` (155 lines) `_reload()` (70-148): step (2) spentMap grouping (90-94), step (3b) limitMap building (109-113), step (5) combined-map building including the "categories with limits but no spend" backfill (129-145) — this whole block is the extraction target.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract dashboard aggregation math</name>
  <files>lib/application/finance/dashboard/home_dashboard_cubit.dart, lib/application/finance/dashboard/dashboard_aggregator.dart</files>
  <read_first>lib/application/finance/dashboard/home_dashboard_cubit.dart (full file)</read_first>
  <action>
    Create `lib/application/finance/dashboard/dashboard_aggregator.dart` with four top-level pure functions, each taking only the data it needs (no repository, no cubit state):
    - `int computeBalance(List&lt;Transaction&gt; transactions)` — the income/expense single-pass sum.
    - `Map&lt;int, int&gt; computeTaggedByGoal(List&lt;Transaction&gt; transactions)` — the goalId → tagged-amount map.
    - `int computeGoalsSavedTotal(List&lt;SavingsGoal&gt; goals, Map&lt;int, int&gt; taggedByGoal)` — the fold over `amountSavedCents`.
    - `int computeDebtTotal(List&lt;Debt&gt; debts)` — the toPay&&!isPaid filter+fold.
    - `Map&lt;int, int&gt; computeCategorySpend(List&lt;Transaction&gt; transactions, {required int year, required int month})` — the expense-filter-by-month groupBy.
    Move each block's logic verbatim, preserving the exact D-07/D-08/D-09 comments as doc comments on the corresponding function.

    Rewrite `home_dashboard_cubit.dart`'s `_reload()` to call these four functions after loading `allTx`/`goals`/`debts`, then compute `final netWorth = balance + goalsSavedTotal - debtTotal;` inline (this one-line combination formula stays in the cubit, it's the orchestration step, not extracted math) before emitting `HomeDashboardLoaded`.
  </action>
  <acceptance_criteria>
    - `wc -l lib/application/finance/dashboard/home_dashboard_cubit.dart lib/application/finance/dashboard/dashboard_aggregator.dart` both report ≤150.
    - `flutter test test/application/finance/home_dashboard_cubit_test.dart` passes unchanged with the same test count — this is the regression gate proving the D-07/D-08/D-09 formulas are byte-identical after the move.
    - `flutter analyze lib/application/finance/dashboard/` reports zero new errors or warnings.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/application/finance/dashboard/ && flutter test test/application/finance/home_dashboard_cubit_test.dart</automated>
  </verify>
  <done>home_dashboard_cubit.dart ≤150 lines; balance/net-worth/category-spend math extracted to pure functions; existing dashboard cubit test suite passes unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract budget merge logic</name>
  <files>lib/application/finance/budget/budget_cubit.dart, lib/application/finance/budget/budget_aggregator.dart</files>
  <read_first>lib/application/finance/budget/budget_cubit.dart (full file)</read_first>
  <action>
    Create `lib/application/finance/budget/budget_aggregator.dart` with `Map&lt;int, ({int limitCents, int spentCents})&gt; mergeBudgetData({required List&lt;Transaction&gt; transactions, required List&lt;Budget&gt; budgets})` — moves the spentMap grouping, limitMap building, combined-map building, and the "categories with limits but no spend" backfill loop verbatim into one function.

    Rewrite `budget_cubit.dart`'s `_reload()` to call `mergeBudgetData(transactions: transactions, budgets: budgets)` and pass the result directly to `emit(BudgetLoaded(budgets: ..., categories: categories))`.
  </action>
  <acceptance_criteria>
    - `wc -l lib/application/finance/budget/budget_cubit.dart lib/application/finance/budget/budget_aggregator.dart` both report ≤150.
    - `flutter test test/application/finance/budget_cubit_test.dart` passes unchanged with the same test count.
    - `flutter analyze lib/application/finance/budget/` reports zero new errors or warnings.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/application/finance/budget/ && flutter test test/application/finance/budget_cubit_test.dart</automated>
  </verify>
  <done>budget_cubit.dart ≤150 lines; spend/limit merge logic extracted to a pure function; existing budget cubit test suite passes unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure application-layer aggregation refactor; the formulas (D-07/D-08/D-09) are moved, not altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-03.1-07-01 | Tampering (financial calculation drift) | `dashboard_aggregator.dart` / `budget_aggregator.dart` | mitigate | Every function moved verbatim with its original comments; existing `home_dashboard_cubit_test.dart` and `budget_cubit_test.dart` (which assert on exact balance/net-worth/spend numbers) are the regression gate, re-run unchanged in this plan's verify step. |
</threat_model>

<verification>
`flutter analyze lib/application/finance/dashboard/ lib/application/finance/budget/` clean. `flutter test test/application/finance/home_dashboard_cubit_test.dart test/application/finance/budget_cubit_test.dart` green. Both target files ≤150 lines.
</verification>

<success_criteria>
home_dashboard_cubit.dart and budget_cubit.dart both ≤150 lines; existing financial-calculation test suites pass unchanged, proving the aggregation math is untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-architecture-compliance/03.1-07-SUMMARY.md`
</output>
