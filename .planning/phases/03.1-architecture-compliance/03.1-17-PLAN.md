---
phase: 03.1
plan: 17
type: execute
wave: 4
depends_on: ["03.1-11", "03.1-16"]
files_modified:
  - lib/data/finance/transaction_model.dart
  - lib/data/finance/transaction_model.g.dart
  - lib/data/finance/transaction_dao.dart
  - lib/data/finance/transaction_mapper.dart
  - lib/data/finance/budget_model.dart
  - lib/data/finance/budget_model.g.dart
  - lib/data/finance/budget_dao.dart
  - lib/data/finance/budget_mapper.dart
  - lib/data/finance/savings_goal_model.dart
  - lib/data/finance/savings_goal_model.g.dart
  - lib/data/finance/savings_goal_dao.dart
  - lib/data/finance/goal_mapper.dart
  - lib/data/finance/debt_model.dart
  - lib/data/finance/debt_model.g.dart
  - lib/data/finance/debt_dao.dart
  - lib/data/finance/debt_mapper.dart
  - lib/data/finance/recurring_payment_model.dart
  - lib/data/finance/recurring_payment_model.g.dart
  - lib/data/finance/recurring_payment_dao.dart
  - lib/data/finance/recurring_payment_mapper.dart
  - lib/data/finance/transaction_category_model.dart
  - lib/data/finance/transaction_category_model.g.dart
  - lib/data/finance/transaction_category_dao.dart
  - lib/data/finance/transaction_category_mapper.dart
  - lib/infrastructure/finance/transaction_repository_impl.dart
  - lib/infrastructure/finance/budget_repository_impl.dart
  - lib/infrastructure/finance/goal_repository_impl.dart
  - lib/infrastructure/finance/debt_repository_impl.dart
  - lib/infrastructure/finance/recurring_payment_repository_impl.dart
  - lib/infrastructure/finance/transaction_category_repository_impl.dart
  - lib/config/di/finance_module.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every Isar @Collection model's part 'x.g.dart' pairing still resolves correctly after the move — no dangling part directive"
    - "injection.config.dart resolves every finance DAO/repository at its new nested import path"
    - "Cold-starting the app after this change still reads back existing transactions, budgets, goals, and debts correctly (Isar collection names are unchanged — only file location moves)"
    - "lib/data/finance/ no longer holds more than 10 hand-written files directly"
  artifacts:
    - path: "lib/data/finance/transaction/transaction_model.dart"
      provides: "TransactionModel (+ .g.dart pairing), moved from the flat data/finance/ directory"
    - path: "lib/data/finance/budget/budget_model.dart"
      provides: "BudgetModel (+ .g.dart pairing), moved from the flat data/finance/ directory"
    - path: "lib/data/finance/goal/savings_goal_model.dart"
      provides: "SavingsGoalModel (+ .g.dart pairing), moved from the flat data/finance/ directory"
    - path: "lib/data/finance/debt/debt_model.dart"
      provides: "DebtModel (+ .g.dart pairing), moved from the flat data/finance/ directory"
    - path: "lib/data/finance/recurring/recurring_payment_model.dart"
      provides: "RecurringPaymentModel (+ .g.dart pairing), moved from the flat data/finance/ directory"
    - path: "lib/data/finance/category/transaction_category_model.dart"
      provides: "TransactionCategoryModel (+ .g.dart pairing), moved from the flat data/finance/ directory"
  key_links: []
---

<objective>
Nest `lib/data/finance/` (18 hand-written files after plan `3.1-11`'s mapper split — 4 per entity: model, DAO, mapper, plus each model's generated `.g.dart` — currently flat, over the 10-file cap) into the same six per-entity subfolders as `3.1-16`'s domain nest: `transaction/`, `budget/`, `goal/`, `debt/`, `recurring/`, `category/`.

This is the plan research specifically calls out for the finance_mappers-split-before-nest sequencing: because `3.1-11` already produced six per-entity mapper files, this plan's per-entity grouping is now mechanical — each subfolder gets exactly one model+`.g.dart`+DAO+mapper. This plan depends on `3.1-16` (not run in parallel with it) because both plans edit the same six `infrastructure/finance/*_repository_impl.dart` files and `config/di/finance_module.dart` — running them in the same wave would conflict.

Purpose: closes the `lib/data/finance` directory-size violation, the last SC-2 violation in the phase.
Output: 18 data files moved into 6 subfolders; every consumer's import path updated; `.g.dart` pairing and DI graph verified intact via build_runner + cold-start check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03.1-architecture-compliance/03.1-RESEARCH.md
</context>

<interfaces>
Target structure (post `3.1-11` mapper split): `transaction/`: transaction_model.dart(+.g.dart), transaction_dao.dart, transaction_mapper.dart. `budget/`: budget_model.dart(+.g.dart), budget_dao.dart, budget_mapper.dart. `goal/`: savings_goal_model.dart(+.g.dart), savings_goal_dao.dart, goal_mapper.dart. `debt/`: debt_model.dart(+.g.dart), debt_dao.dart, debt_mapper.dart. `recurring/`: recurring_payment_model.dart(+.g.dart), recurring_payment_dao.dart, recurring_payment_mapper.dart. `category/`: transaction_category_model.dart(+.g.dart), transaction_category_dao.dart, transaction_category_mapper.dart.

Each `*_model.dart` has a `part 'x_model.g.dart';` directive that is path-relative and regenerated based on the source file's own location — moving both files together in the same `git mv` batch and then re-running `build_runner` produces a correct pairing automatically (verified against the live files during research).

Consumers: each of the 6 `infrastructure/finance/*_repository_impl.dart` files imports its own entity's model+DAO+mapper (3 imports each); `config/di/finance_module.dart` imports every DAO and registers every repository impl.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Move the 18 data/finance files into 6 subfolders</name>
  <files>lib/data/finance/**</files>
  <read_first>lib/data/finance/*.dart (list the directory, confirm it holds exactly 6 models + 6 .g.dart + 6 DAOs + 6 mappers = 24 files, matching the post-3.1-11 state, before moving anything)</read_first>
  <action>
    Create the 6 subfolders and, for each entity, `git mv` its model `.dart`, its model `.g.dart`, its DAO, and its mapper together into the target subfolder (e.g. `git mv lib/data/finance/transaction_model.dart lib/data/finance/transaction_model.g.dart lib/data/finance/transaction_dao.dart lib/data/finance/transaction_mapper.dart lib/data/finance/transaction/`) — moving the model and its `.g.dart` in the SAME command is the specific mitigation for the documented "directory move breaks part/.g.dart pairing" pitfall. Do not edit file contents in this task.
  </action>
  <acceptance_criteria>
    - `find lib/data/finance -maxdepth 1 -name "*.dart" | wc -l` reports 0.
    - `for d in transaction budget goal debt recurring category; do n=$(find lib/data/finance/$d -maxdepth 1 -name "*.dart" ! -name "*.g.dart" | wc -l); echo "$d: $n"; done` reports 3 for every entity (model+dao+mapper, `.g.dart` excluded from the hand-written count).
    - `for d in transaction budget goal debt recurring category; do test -f lib/data/finance/$d/*_model.g.dart && echo "$d: g.dart present" || echo "$d: MISSING g.dart"; done` reports "present" for all 6 (confirms every model's generated part file moved alongside it).
  </acceptance_criteria>
  <verify>
    <automated>find lib/data/finance -maxdepth 1 -name "*.dart" | wc -l</automated>
  </verify>
  <done>All 18 hand-written files (+6 .g.dart) moved via git mv into 6 subfolders, each model paired correctly with its .g.dart.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Repoint every consumer import, regenerate, and cold-start verify</name>
  <files>lib/infrastructure/finance/transaction_repository_impl.dart, lib/infrastructure/finance/budget_repository_impl.dart, lib/infrastructure/finance/goal_repository_impl.dart, lib/infrastructure/finance/debt_repository_impl.dart, lib/infrastructure/finance/recurring_payment_repository_impl.dart, lib/infrastructure/finance/transaction_category_repository_impl.dart, lib/config/di/finance_module.dart</files>
  <read_first>lib/infrastructure/finance/transaction_repository_impl.dart (confirm the exact old import lines to replace)</read_first>
  <action>
    Run `grep -rl "package:agenda/data/finance/" lib/ test/ | grep -v "\.g\.dart"` to get the authoritative current consumer list. For each of the 6 entities' model/DAO/mapper imports, bulk `sed -i` the old flat path to the new subfolder path across every matched file (same mechanism as plan `3.1-16` Task 2). Also check `lib/main.dart` and `lib/config/di/finance_module.dart` for any Isar schema-registration references to model classes by import path (not just by class name) that need updating.

    Run `dart run build_runner build --delete-conflicting-outputs` — this both regenerates every moved model's `.g.dart` at its new location (confirming the `part`/`part of` pairing survived the move) and regenerates `injection.config.dart` with the new DAO/repository import paths.
  </action>
  <acceptance_criteria>
    - `grep -rl "package:agenda/data/finance/[a-z_]*\.dart'" lib/ test/ | grep -v "/data/finance/\(transaction\|budget\|goal\|debt\|recurring\|category\)/"` reports zero matches.
    - `dart run build_runner build --delete-conflicting-outputs` completes without error and reports no "part file not found" or "duplicate part" errors.
    - `flutter analyze` reports zero new errors or warnings across the whole tree.
    - `flutter test test/config/di/di_test.dart` passes — the DI smoke test, per research's Pitfall 3, is the fast/cheap way to catch a broken registration before running the full suite.
    - `flutter test --no-pub` full suite passes with test count ≥ 230.
    - Manual verification (Isar collection names are unchanged, but this is the one place in the phase where a break would be silent to `flutter analyze`): cold-start the app on an emulator/device with existing seeded data (or run through the app's first-run flow), navigate to Transactions/Budgets/Goals/Debts/Recurring tabs, confirm all existing records still load and display — per research Pitfall 3, `flutter analyze` passing does not guarantee the DI graph resolves correctly at runtime.
  </acceptance_criteria>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs && flutter analyze && flutter test test/config/di/di_test.dart && flutter test --no-pub</automated>
  </verify>
  <done>Every consumer repointed; build_runner regenerates cleanly with correct .g.dart pairing; DI smoke test and full suite green at ≥230 tests; cold-start manual check confirms existing data still reads back correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Isar collection resolution at app cold-start | The one place in this phase where a broken import could pass static analysis (a stale generated file) but fail at runtime — explicitly why this plan requires a manual cold-start check, not just `flutter analyze`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-03.1-17-01 | Tampering (broken import graph) | Bulk sed rewrite across infrastructure + DI consumers | mitigate | Full-tree `flutter analyze` + full `flutter test --no-pub` as the final verify step; zero-remaining-flat-path grep check before declaring done. |
| T-03.1-17-02 | Denial of Service (Isar model/part pairing break) | Model `.g.dart` co-location | mitigate | Model and its `.g.dart` moved in the same `git mv` command (Task 1); `build_runner` regeneration explicitly checked for "part file not found" errors (Task 2). |
| T-03.1-17-03 | Denial of Service (DI resolution failure at runtime, invisible to static analysis) | `injection.config.dart` staleness | mitigate | `di_test.dart` run explicitly as a fast pre-check per research Pitfall 3, PLUS a manual cold-start check that existing data reads back — the one behavior no automated check in this repo can fully prove without a real Isar file on a device/emulator. |
</threat_model>

<verification>
Zero remaining flat-path `data/finance` imports. `build_runner` regenerates cleanly (no part-pairing errors). Full-tree `flutter analyze` clean. `flutter test test/config/di/di_test.dart` and full `flutter test --no-pub` green, test count ≥ 230. Manual cold-start check confirms existing finance data still loads.
</verification>

<success_criteria>
data/finance nested into 6 subfolders of 3 hand-written files each (+ co-located .g.dart); every consumer repointed; DI graph and Isar model pairing intact; full suite green; existing data readable after cold start.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-architecture-compliance/03.1-17-SUMMARY.md`
</output>
