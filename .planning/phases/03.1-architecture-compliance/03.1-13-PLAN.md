---
phase: 03.1
plan: 13
type: execute
wave: 2
depends_on: ["03.1-08"]
files_modified:
  - lib/presentation/finance/screens/recurring_payment_form_screen.dart
  - lib/presentation/finance/widgets/recurring/recurring_payment_form_fields.dart
  - lib/presentation/finance/recurring_payment_form_logic.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Creating/editing a recurring payment still saves identical field values (title, amount, category, cycle, next due date) as before the split"
    - "recurring_payment_form_screen.dart is at or under 150 lines; every new file is at or under 150 lines"
  artifacts:
    - path: "lib/presentation/finance/recurring_payment_form_logic.dart"
      provides: "buildRecurringPaymentToSave, loadExpenseCategories — pure functions extracted from _save()/_loadCategories()"
    - path: "lib/presentation/finance/widgets/recurring/recurring_payment_form_fields.dart"
      provides: "RecurringPaymentFormFields — title/amount/category/cycle/date card, composed from the shared FormCard/FieldRow/FieldDivider"
  key_links:
    - from: "lib/presentation/finance/screens/recurring_payment_form_screen.dart"
      to: "lib/presentation/finance/widgets/category_picker_sheet.dart"
      via: "showModalBottomSheet<TransactionCategory>(builder: (_) => CategoryPickerSheet(...))"
      pattern: "CategoryPickerSheet\\("
---

<objective>
Split `recurring_payment_form_screen.dart` (431 lines) using the shared primitives/category-picker/amount-parser from `3.1-08`, following the same pattern as `3.1-12`'s transaction-form split (this screen has no goal-link sheet, so it is a strictly smaller version of that same shape).

Purpose: closes the recurring-payment-form violation.
Output: `recurring_payment_form_screen.dart` ≤150 lines; 2 new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
</context>

<interfaces>
Post-`3.1-08` shared files: `lib/presentation/finance/widgets/finance_form_primitives.dart`, `lib/presentation/finance/widgets/category_picker_sheet.dart`, `lib/core/utils/amount_parser.dart`.

Current `recurring_payment_form_screen.dart` (431 lines): fields `_titleController`, `_amountController`, `_cycle`, `_nextDueDate`, `_selectedCategory`, `_expenseCategories`, `_loadingCategories`. `_loadCategories(preselectedId)`(68-86): loads expense-only categories via `getByType(TransactionType.expense)`. `_pickCategory`(88-125): near-identical to `transaction_form_screen`'s (same shape, no goal exclusion logic, uses `_expenseCategories` directly instead of a filtered getter) — replace with the shared `CategoryPickerSheet`. `_cycleLabel`(139-146): private label helper, only used in the cycle dropdown — moves with the fields widget. `_save`(148-206): validates, parses amount, builds `RecurringPayment`/`copyWith` — extraction target. `build()`(208-384): one `_FormCard` with title/amount/category/cycle/next-due-date fields — extraction target. `_FormCard`/`_FieldRow`/`_FieldDivider`(386-431): delete, replaced by shared primitives.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Adopt shared utilities; extract save/load logic</name>
  <files>lib/presentation/finance/recurring_payment_form_logic.dart, lib/presentation/finance/screens/recurring_payment_form_screen.dart</files>
  <read_first>
    lib/presentation/finance/screens/recurring_payment_form_screen.dart (full file)
    lib/presentation/finance/widgets/finance_form_primitives.dart, lib/presentation/finance/widgets/category_picker_sheet.dart, lib/core/utils/amount_parser.dart (the 3.1-08 outputs)
  </read_first>
  <action>
    Delete the local `_FormCard`/`_FieldRow`/`_FieldDivider` classes; import and use the shared `finance_form_primitives.dart` versions in their place throughout `build()`. Replace `_pickCategory`'s inline sheet builder with `showModalBottomSheet&lt;TransactionCategory&gt;(builder: (_) => CategoryPickerSheet(categories: _expenseCategories, selectedCategoryId: _selectedCategory?.id, locale: Localizations.localeOf(context)))`. Replace the amount-parsing block inside `_save()` with `parseAmountCentsOrNull(_amountController.text)`.

    Create `lib/presentation/finance/recurring_payment_form_logic.dart` with:
    - `Future&lt;List&lt;TransactionCategory&gt;&gt; loadExpenseCategories(TransactionCategoryRepository repo)` — moves `_loadCategories`'s repository-call body (the preselection-by-id logic stays in the screen's `setState`, since it needs `_selectedCategory` assignment).
    - `RecurringPayment buildRecurringPaymentToSave({required bool isEditing, required RecurringPayment? original, required String title, required int amountCents, required int categoryId, required RecurringCycle cycle, required DateTime nextDueDate, required DateTime now})` — moves the `RecurringPayment(...)`/`copyWith(...)` construction verbatim, including the `isActive: true` default on create.
    Update `initState`/`_loadCategories` to call `loadExpenseCategories(...)` then apply the preselection in one `setState`. Update `_save()` to call `buildRecurringPaymentToSave(...)` then only the cubit call + navigation-pop.
  </action>
  <acceptance_criteria>
    - `grep -c "_FormCard\|_FieldRow\|_FieldDivider" lib/presentation/finance/screens/recurring_payment_form_screen.dart` reports 0.
    - `flutter analyze lib/presentation/finance/` reports zero new errors or warnings.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/presentation/finance/</automated>
  </verify>
  <done>Shared primitives/category-picker/amount-parser adopted; save/category-load logic extracted to recurring_payment_form_logic.dart.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract the form-fields composition widget and verify the whole slice</name>
  <files>lib/presentation/finance/widgets/recurring/recurring_payment_form_fields.dart, lib/presentation/finance/screens/recurring_payment_form_screen.dart</files>
  <read_first>lib/presentation/finance/screens/recurring_payment_form_screen.dart (post-Task-1 state)</read_first>
  <action>
    Create `lib/presentation/finance/widgets/recurring/recurring_payment_form_fields.dart` with `class RecurringPaymentFormFields extends StatelessWidget` composing the single `FormCard` (title `TextFormField`, amount `TextFormField`, category `ListTile`, cycle `DropdownButtonFormField`, next-due-date `ListTile`) — taking `titleController`, `amountController` (not owned), `isEditing`, `cycle`, `categoryDisplay`, `loadingCategories`, `nextDueDate`, and callbacks `onPickCategory`, `onCycleChanged`, `onPickDate`. Move `_cycleLabel` into this file as a private top-level helper. Wire it into `build()`.

    Re-measure `recurring_payment_form_screen.dart`. If still over 150 lines, unify the create/update cubit-call branches behind a single `_persist(RecurringPayment payment)` helper, matching plan `3.1-12`'s fallback approach.

    Re-measure `recurring_payment_form_fields.dart` as well — the screen fallback above does not shrink it, and it absorbs a ~176-line source block. If it exceeds 150 lines, split along the card boundary already present in the source: `RecurringPaymentIdentityFields` (title + amount + category `FormCard`) and `RecurringPaymentScheduleFields` (cycle dropdown + next-due-date + active toggle `FormCard`), both in `lib/presentation/finance/widgets/recurring/`, with `RecurringPaymentFormFields` reduced to composing the two.
  </action>
  <acceptance_criteria>
    - `for f in lib/presentation/finance/screens/recurring_payment_form_screen.dart lib/presentation/finance/widgets/recurring/recurring_payment_form_fields.dart lib/presentation/finance/recurring_payment_form_logic.dart; do n=$(wc -l &lt; "$f"); [ "$n" -le 150 ] || echo "FAIL $f: $n"; done` prints nothing.
    - `flutter analyze lib/presentation/finance/` reports zero new errors or warnings.
    - Manual verification (no existing test for this screen): on-device or `flutter run`, create a recurring payment with title+amount+category+cycle+next-due-date, save, reopen, confirm all fields persisted; confirm the cycle dropdown shows all 6 cycle labels correctly.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/presentation/finance/</automated>
  </verify>
  <done>recurring_payment_form_screen.dart and both new files ≤150 lines; manual smoke pass completed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure refactor of existing form UI/validation; amount parsing now goes through the shared, tested `amount_parser.dart`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-03.1-13-01 | Tampering (financial data integrity) | `buildRecurringPaymentToSave` | mitigate | Moved verbatim including the `isActive: true` create-default; manual smoke pass explicitly verifies round-trip persistence for every field. |
</threat_model>

<verification>
`flutter analyze lib/presentation/finance/` clean. Every file in scope ≤150 lines. Manual on-device smoke pass (no existing test for this screen).
</verification>

<success_criteria>
recurring_payment_form_screen.dart and both newly extracted files ≤150 lines; save/load logic follows the established pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-architecture-compliance/03.1-13-SUMMARY.md`
</output>
