---
phase: 03.1
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/presentation/finance/widgets/finance_form_primitives.dart
  - lib/presentation/finance/widgets/category_picker_sheet.dart
  - lib/core/utils/amount_parser.dart
  - test/core/utils/amount_parser_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "A shared FormCard/FieldRow/FieldDivider exists that is a byte-for-byte visual match to the four duplicated private copies currently in transaction_form_screen.dart, recurring_payment_form_screen.dart, debt_form_screen.dart, and goal_form_screen.dart"
    - "parseAmountCentsOrNull correctly parses both comma-decimal ('1.250,50') and plain ('12.5') input, and returns null for empty/zero/negative/garbage input"
  artifacts:
    - path: "lib/presentation/finance/widgets/finance_form_primitives.dart"
      provides: "Public FormCard, FieldRow, FieldDivider — deduplicates 4 private per-screen copies of the same widgets"
    - path: "lib/presentation/finance/widgets/category_picker_sheet.dart"
      provides: "CategoryPickerSheet — deduplicates the near-identical category-picker bottom sheet in transaction_form_screen.dart and recurring_payment_form_screen.dart"
    - path: "lib/core/utils/amount_parser.dart"
      provides: "parseAmountCentsOrNull(String), formatCentsForInput(int) — deduplicates the identical comma/dot amount-parsing logic repeated in 4 form screens"
  key_links: []
---

<objective>
Before splitting any of the four finance form screens (`transaction_form_screen.dart`, `recurring_payment_form_screen.dart`, `debt_form_screen.dart`, `goal_form_screen.dart`), extract the utilities they already duplicate near-identically: a private `_FormCard`/`_FieldRow`/`_FieldDivider` trio re-declared in three of the four files, a category-picker bottom sheet re-declared almost verbatim in two of them, and comma/dot amount-cents parsing re-implemented inline in all four. Creating these once, in Wave 1, means each of the four form-split plans (`3.1-12` through `3.1-15`) can `depends_on` this plan and immediately delete their local duplicates instead of inventing their own.

Purpose: removes real, already-present duplication (not duplication invented by this refactor) and gives every downstream form-split plan a shared, tested foundation.
Output: 3 new shared files, zero screens touched yet (wiring happens in the dependent plans).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@lib/presentation/tasks/form/widgets/form_primitives.dart
</context>

<interfaces>
Existing duplicated `_FormCard`/`_FieldRow`/`_FieldDivider` (identical in `transaction_form_screen.dart` lines 542-587, `recurring_payment_form_screen.dart` lines 386-431, `debt_form_screen.dart` lines 282-327; `goal_form_screen.dart` has only `_FormCard`, lines 271-286, and inlines its field rows manually instead of using a `_FieldRow` equivalent).

Existing near-identical category-picker sheet: `transaction_form_screen.dart._pickCategory()` (112-166) and `recurring_payment_form_screen.dart._pickCategory()` (88-125) both build a `DraggableScrollableSheet` + `ListView.builder` over a category list, resolving `nameEn`/`namePtBr` by locale, checking `_selectedCategory?.id == cat.id` for the trailing check icon, popping the tapped `TransactionCategory`.

Existing duplicated amount parsing (identical shape in all 4 forms, e.g. `transaction_form_screen.dart._save()` lines 265-277): `text.replaceAll(',', '.').replaceAll(RegExp(r'[^\d.]'), '')` → `double.tryParse` → null/≤0 check → `(parsed * 100).round()`. Pre-fill direction (existing in all 4 `initState`s): `(cents / 100).toStringAsFixed(2).replaceAll('.', ',')`.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract shared form primitives and the category picker sheet</name>
  <files>lib/presentation/finance/widgets/finance_form_primitives.dart, lib/presentation/finance/widgets/category_picker_sheet.dart</files>
  <read_first>
    lib/presentation/finance/screens/transaction_form_screen.dart (full file — source of the primitives and the reference category-picker implementation)
    lib/presentation/finance/screens/recurring_payment_form_screen.dart (full file — confirms the second category-picker is functionally identical)
    lib/presentation/tasks/form/widgets/form_primitives.dart (the existing `FormCard`/`FieldRow`/`FieldDivider` in the tasks slice — match this file's structure/naming exactly so the two slices are stylistically consistent)
  </read_first>
  <action>
    Create `lib/presentation/finance/widgets/finance_form_primitives.dart` with public `FormCard`, `FieldRow`, `FieldDivider`, matching `presentation/tasks/form/widgets/form_primitives.dart`'s implementation exactly (same `Card`/`elevation: 0`/`surfaceContainerLow`/`borderRadius: 16` for `FormCard`; same `Icon`+`SizedBox(width: 12)`+`Expanded` row for `FieldRow`; same `Divider(height: 1, indent: 48, endIndent: 16)` for `FieldDivider`).

    Create `lib/presentation/finance/widgets/category_picker_sheet.dart` with `class CategoryPickerSheet extends StatelessWidget` taking `categories` (`List&lt;TransactionCategory&gt;`), `selectedCategoryId` (`int?`), and `locale` (`Locale`) — reproduces the `DraggableScrollableSheet`+`ListView.builder` from `transaction_form_screen.dart._pickCategory()` verbatim (drag handle, locale-aware name resolution, check-icon trailing, `Navigator.of(ctx).pop(cat)` on tap). This is a pure display widget — it does not fetch categories itself, the caller passes the already-loaded list.
  </action>
  <acceptance_criteria>
    - `wc -l lib/presentation/finance/widgets/finance_form_primitives.dart lib/presentation/finance/widgets/category_picker_sheet.dart` both report ≤150.
    - `dart analyze lib/presentation/finance/widgets/finance_form_primitives.dart lib/presentation/finance/widgets/category_picker_sheet.dart` reports zero errors (both compile standalone even though nothing imports them yet).
  </acceptance_criteria>
  <verify>
    <automated>dart analyze lib/presentation/finance/widgets/finance_form_primitives.dart lib/presentation/finance/widgets/category_picker_sheet.dart</automated>
  </verify>
  <done>Both shared widget files exist, compile standalone, and are ≤150 lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract and test the amount-cents parser</name>
  <files>lib/core/utils/amount_parser.dart, test/core/utils/amount_parser_test.dart</files>
  <read_first>lib/core/utils/amount_formatter.dart (the existing sibling utility this complements — match its doc-comment style)</read_first>
  <behavior>
    - `parseAmountCentsOrNull('12,50')` returns `1250`.
    - `parseAmountCentsOrNull('12.50')` returns `1250` (plain-dot input, matches EN-locale typed input).
    - `parseAmountCentsOrNull('1.250,50')` returns `125050` (thousands-dot + comma-decimal, matches the `RegExp(r'[^\d.]')` filter behavior already in the 4 forms — verify against the existing inline logic exactly, including its quirk of stripping ALL non-digit-non-dot characters after comma→dot conversion).
    - `parseAmountCentsOrNull('')`, `parseAmountCentsOrNull('0')`, `parseAmountCentsOrNull('-5')`, `parseAmountCentsOrNull('abc')` all return `null`.
    - `formatCentsForInput(1250)` returns `'12,50'`; `formatCentsForInput(0)` returns `''` (matches the existing `> 0 ? ... : ''` ternary pattern used in every form's `initState`).
  </behavior>
  <action>
    Write `test/core/utils/amount_parser_test.dart` first with the six cases above, confirm it fails (function doesn't exist yet). Then create `lib/core/utils/amount_parser.dart` with `int? parseAmountCentsOrNull(String raw)` and `String formatCentsForInput(int cents)`, moving the exact parsing expression already duplicated across the 4 forms (`raw.replaceAll(',', '.').replaceAll(RegExp(r'[^\d.]'), '')` → `double.tryParse` → `null` if `== null || &lt;= 0` → `(parsed * 100).round()`) and the exact pre-fill expression (`(cents / 100).toStringAsFixed(2).replaceAll('.', ',')` guarded by `cents &gt; 0`). Run the test until green.
  </action>
  <verify>
    <automated>dart test test/core/utils/amount_parser_test.dart</automated>
  </verify>
  <done>amount_parser.dart exists with both functions passing all 6 test cases, matching the exact parsing behavior already present in the 4 duplicated call sites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User-typed amount string → parsed integer cents | Existing boundary, unchanged location (still validated client-side before being handed to a cubit); this plan only centralizes the parsing logic, it does not relax or add any validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-03.1-08-01 | Tampering (financial input parsing drift) | `parseAmountCentsOrNull` | mitigate | TDD: tests written first against the exact behavior of the 4 existing inline implementations (including the "strip all non-digit-non-dot after comma conversion" quirk), not against an idealized re-design — prevents silently changing what counts as valid input. |
</threat_model>

<verification>
`dart test test/core/utils/amount_parser_test.dart` green (6/6 cases). `dart analyze` clean on both new widget files. No screens modified in this plan — nothing else to regression-test yet.
</verification>

<success_criteria>
Three shared utility files exist, compile/test cleanly in isolation, and are available for plans `3.1-12` through `3.1-15` to import.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-architecture-compliance/03.1-08-SUMMARY.md`
</output>
