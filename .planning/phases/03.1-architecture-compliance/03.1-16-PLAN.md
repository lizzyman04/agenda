---
phase: 03.1
plan: 16
type: execute
wave: 3
depends_on: ["03.1-02", "03.1-07", "03.1-08", "03.1-09", "03.1-10", "03.1-11", "03.1-12", "03.1-13", "03.1-14", "03.1-15"]
files_modified:
  - lib/domain/finance/transaction.dart
  - lib/domain/finance/transaction_repository.dart
  - lib/domain/finance/transaction_type.dart
  - lib/domain/finance/budget.dart
  - lib/domain/finance/budget_repository.dart
  - lib/domain/finance/savings_goal.dart
  - lib/domain/finance/savings_goal_contribution.dart
  - lib/domain/finance/goal_repository.dart
  - lib/domain/finance/debt.dart
  - lib/domain/finance/debt_direction.dart
  - lib/domain/finance/debt_repository.dart
  - lib/domain/finance/recurring_payment.dart
  - lib/domain/finance/recurring_cycle.dart
  - lib/domain/finance/recurring_payment_repository.dart
  - lib/domain/finance/transaction_category.dart
  - lib/domain/finance/transaction_category_repository.dart
  - "(every file under lib/ and test/ that imports lib/domain/finance/*.dart — discovered by grep at execution time, see Task 2)"
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every domain/finance type (Transaction, Budget, SavingsGoal, Debt, RecurringPayment, TransactionCategory, their enums and repository interfaces) is reachable at its new nested import path"
    - "No consumer anywhere in lib/ or test/ still imports the old flat domain/finance/*.dart paths"
    - "lib/domain/finance/ no longer holds more than 10 hand-written files directly (files now live in 6 subfolders, ≤3 each)"
  artifacts:
    - path: "lib/domain/finance/transaction/transaction.dart"
      provides: "Transaction entity, moved from the flat domain/finance/ directory"
    - path: "lib/domain/finance/budget/budget.dart"
      provides: "Budget entity, moved from the flat domain/finance/ directory"
    - path: "lib/domain/finance/goal/savings_goal.dart"
      provides: "SavingsGoal entity, moved from the flat domain/finance/ directory"
    - path: "lib/domain/finance/debt/debt.dart"
      provides: "Debt entity, moved from the flat domain/finance/ directory"
    - path: "lib/domain/finance/recurring/recurring_payment.dart"
      provides: "RecurringPayment entity, moved from the flat domain/finance/ directory"
    - path: "lib/domain/finance/category/transaction_category.dart"
      provides: "TransactionCategory entity, moved from the flat domain/finance/ directory"
  key_links: []
---

<objective>
Nest `lib/domain/finance/` (16 hand-written files, currently flat, over the 10-file directory cap) into six per-entity subfolders — `transaction/`, `budget/`, `goal/`, `debt/`, `recurring/`, `category/` — matching the structure research proposed and the naming convention already established by `presentation/finance/goals/` and this phase's new `presentation/finance/widgets/<entity>/` subfolders.

This plan runs in **Wave 3, last among the content-splitting plans**, because it is a directory move touching the import statements of every consumer of `domain/finance/*` — which by this point in the phase includes every file created or modified by plans `3.1-02` (task-form finance link), `3.1-07` through `3.1-15` (finance cubits, shared utilities, list/dashboard screens, all four form splits). Running this before those plans would mean each of them lands on a moving target; running it after means one clean, final import-path pass. Per the sequencing analysis in `03.1-RESEARCH.md`, this must NOT happen in the same plan as the `data/finance` mapper split (`3.1-11`, already done) — that split and this move are two mechanically distinct steps, kept separate so `flutter analyze` can isolate which change broke an import, if any.

Purpose: closes the `lib/domain/finance` directory-size violation (16 → 6 subfolders of ≤3 files each).
Output: 16 domain files moved into 6 subfolders; every consumer's import path updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03.1-architecture-compliance/03.1-RESEARCH.md
</context>

<interfaces>
Target structure (research's "Recommended Project Structure", confirmed against the live 16-file list):
- `transaction/`: transaction.dart, transaction_repository.dart, transaction_type.dart
- `budget/`: budget.dart, budget_repository.dart
- `goal/`: savings_goal.dart, savings_goal_contribution.dart, goal_repository.dart
- `debt/`: debt.dart, debt_direction.dart, debt_repository.dart
- `recurring/`: recurring_payment.dart, recurring_cycle.dart, recurring_payment_repository.dart
- `category/`: transaction_category.dart, transaction_category_repository.dart

Known consumer set at time of writing this plan (verified by `grep -rl "domain/finance/" lib/`): the 6 `infrastructure/finance/*_repository_impl.dart` files, all `application/finance/{budget,dashboard,debt,goal,recurring,transaction}/*.dart` files, `presentation/finance/goals/screens/*.dart` + `presentation/finance/goals/widgets/*.dart`, `presentation/finance/screens/*.dart`, `presentation/finance/widgets/*.dart` (including the new `widgets/<entity>/` subfolders from plans `3.1-08`–`3.1-15`), and `presentation/tasks/form/widgets/finance_link_sheet.dart` + `task_form_logic.dart` from plan `3.1-02`. This list will have grown by the time this plan executes (waves 1-2 create new files) — Task 1's grep is the authoritative source, not this list.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Move the 16 domain/finance files into 6 subfolders</name>
  <files>lib/domain/finance/**</files>
  <read_first>lib/domain/finance/*.dart (list the directory, confirm the 16-file set matches the "Known consumer set" note above before moving anything — if plan 3.1-11 or any earlier plan added/removed a domain/finance file unexpectedly, stop and reconcile before proceeding)</read_first>
  <action>
    Create the 6 subfolders and `git mv` each of the 16 files into its target subfolder per the mapping in `&lt;interfaces&gt;` above (e.g. `git mv lib/domain/finance/transaction.dart lib/domain/finance/transaction/transaction.dart`). Use `git mv`, not a manual delete+recreate, so history is preserved. Do not edit file contents in this task — package-relative imports inside each file (e.g. `transaction_repository.dart` importing `transaction.dart` from the same original directory) need their relative/package import updated only if they reference a sibling that moved to a *different* subfolder (e.g. if any file in `domain/finance/` imports another `domain/finance/` file directly rather than via `package:agenda/...` — check each file: this codebase's convention is `package:agenda/...` absolute imports throughout, confirmed by every file read during planning, so sibling-to-sibling imports also need their path segment updated to include the new subfolder).
  </action>
  <acceptance_criteria>
    - `find lib/domain/finance -maxdepth 1 -name "*.dart" | wc -l` reports 0 (no files remain directly in the flat root).
    - `for d in transaction budget goal debt recurring category; do n=$(find lib/domain/finance/$d -maxdepth 1 -name "*.dart" | wc -l); echo "$d: $n"; done` reports counts matching the target structure (3,2,3,3,3,2) and each ≤10.
    - `git status --short lib/domain/finance/ | grep -c "^R"` reports 16 (all 16 files tracked as renames, confirming `git mv` was used, not delete+recreate).
  </acceptance_criteria>
  <verify>
    <automated>find lib/domain/finance -maxdepth 1 -name "*.dart" | wc -l</automated>
  </verify>
  <done>All 16 files moved via git mv into 6 subfolders; flat domain/finance/ root holds zero direct .dart files.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Repoint every consumer import and verify</name>
  <files>(discovered by grep — see action)</files>
  <read_first>
    lib/domain/finance/transaction/transaction.dart (confirm the new path after Task 1, to build the correct sed pattern)
  </read_first>
  <action>
    Run `grep -rl "package:agenda/domain/finance/" lib/ test/` to get the authoritative, current list of every file importing an old flat `domain/finance/*.dart` path — this will be a larger set than the "Known consumer set" listed in `&lt;interfaces&gt;` since it includes everything created by waves 1-2. For each of the 16 moved files, run a bulk `sed -i` replacement across that consumer list mapping the old import path to the new subfolder path (e.g. `s#package:agenda/domain/finance/transaction\.dart#package:agenda/domain/finance/transaction/transaction.dart#g`, repeated once per moved file — do all 16 substitutions across all matched files in one pass per file-pattern to keep the diff mechanical and reviewable). Apply the same substitutions to `test/` files that import these types directly.

    After the bulk rewrite, run `dart run build_runner build --delete-conflicting-outputs` to regenerate `injection.config.dart` (it may hold stale import paths for any domain type referenced in DI factory signatures — verify by re-running even if you expect no direct domain/finance import there).
  </action>
  <acceptance_criteria>
    - `grep -rl "package:agenda/domain/finance/[a-z_]*\.dart'" lib/ test/ | grep -v "/domain/finance/\(transaction\|budget\|goal\|debt\|recurring\|category\)/"` reports zero matches (proves no file still imports a flat, un-nested domain/finance path — every remaining match is correctly nested under one of the 6 subfolders).
    - `dart run build_runner build --delete-conflicting-outputs` completes without error.
    - `flutter analyze` reports zero new errors or warnings across the whole tree.
    - `flutter test --no-pub` full suite passes with test count ≥ 230 (this phase's measured baseline; no test file's import path breaking should manifest as a *dropped* test count, not just a red run).
  </acceptance_criteria>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs && flutter analyze && flutter test --no-pub</automated>
  </verify>
  <done>Every consumer of domain/finance/* across lib/ and test/ repointed to its new nested path; full analyze clean; full suite green at ≥230 tests; DI graph regenerates cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure directory reorganization; no domain type, enum value, or repository interface signature changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-03.1-16-01 | Tampering (broken import graph) | Bulk sed rewrite across ~40+ consumer files | mitigate | Full-tree `flutter analyze` + full `flutter test --no-pub` run as the final verify step, not a scoped/sampled one — a single missed import anywhere in `lib/` or `test/` surfaces as a compile error, and this task's acceptance criteria explicitly grep for any remaining flat-path reference before declaring done. |
| T-03.1-16-02 | Denial of Service (DI resolution failure) | `injection.config.dart` staleness | mitigate | `build_runner` regenerated and the full DI-dependent suite (`test/config/di/di_test.dart` is part of the full `flutter test --no-pub` run) re-verified in this plan's verify step. |
</threat_model>

<verification>
Zero remaining flat-path `domain/finance` imports anywhere in `lib/`/`test/`. Full-tree `flutter analyze` clean. `flutter test --no-pub` green, test count ≥ 230. `domain/finance/` directory holds zero direct files, 6 subfolders each ≤10.
</verification>

<success_criteria>
domain/finance nested into 6 subfolders of ≤3 files each; every consumer repointed; full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-architecture-compliance/03.1-16-SUMMARY.md`
</output>
