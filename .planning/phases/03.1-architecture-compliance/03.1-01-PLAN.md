---
phase: 03.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tool/check_architecture.dart
  - tool/architecture_exemptions.dart
  - test/tool/check_architecture_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Running `dart run tool/check_architecture.dart` against the live tree prints every current violation (line-count, directory-size, missing README) without crashing"
    - "The guard's line-count exemption mechanism is exercised by a real entry (currencies.dart) with a recorded justification, not a placeholder"
    - "The guard's three checks (line-count, directory-size, README-presence) are each independently verified by a unit test against a synthetic fixture tree, not just eyeballed against the live tree"
  artifacts:
    - path: "tool/check_architecture.dart"
      provides: "CI guard: line-count cap, directory-size cap, README-presence check, exemption allowlist support"
      min_lines: 60
    - path: "tool/architecture_exemptions.dart"
      provides: "Documented, justified allowlist of files exempted from the 150-line cap"
    - path: "test/tool/check_architecture_test.dart"
      provides: "Unit coverage proving the guard's three checks work against fixtures, independent of the live lib/ tree's current state"
  key_links:
    - from: "tool/check_architecture.dart"
      to: "tool/architecture_exemptions.dart"
      via: "import + lineLimitExemptions map lookup"
      pattern: "architecture_exemptions"
---

<objective>
Build the architecture compliance guard (`tool/check_architecture.dart`) that every later plan in this phase measures itself against, per the sequencing directive in `03.1-RESEARCH.md` ("build the guard first"). This plan writes the guard in **dev-tool-only mode**: it exists, it runs, it is unit-tested, and it is not yet wired into `.github/workflows/ci.yml` — CI wiring happens in the final plan (`03.1-18`) once the tree is compliant. Also resolves the `core/constants/currencies.dart` split-or-exempt hard case (per `<decisions_already_made>` D-02: EXEMPT, per research's Split-or-Exempt table) by recording it as the first entry in the exemption allowlist.

Purpose: gives every subsequent plan in this phase a measurable, mechanical definition of "done" instead of ad hoc `wc -l` spot checks, and closes the currencies.dart hard-case decision with zero code churn.
Output: `tool/check_architecture.dart`, `tool/architecture_exemptions.dart`, `test/tool/check_architecture_test.dart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-architecture-compliance/03.1-RESEARCH.md
@analysis_options.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the guard script and the exemption allowlist</name>
  <files>tool/check_architecture.dart, tool/architecture_exemptions.dart</files>
  <read_first>
    analysis_options.yaml (the `analyzer.exclude` list — the guard must mirror it exactly: `**/*.g.dart`, `lib/config/di/injection.config.dart`, `lib/generated/**`)
    .planning/phases/03.1-architecture-compliance/03.1-RESEARCH.md, "Code Examples" section (the full draft script — use as the base, do not redesign from scratch)
  </read_first>
  <action>
    Create `tool/architecture_exemptions.dart` first, exporting `const Map&lt;String, String&gt; lineLimitExemptions` with exactly one entry: `'lib/core/constants/currencies.dart': 'Flat ISO 4217 data table (single static const List&lt;Currency&gt;) — splitting by line range fragments a single semantic unit and destroys Ctrl+F lookup for a currency code, for zero readability gain. See 3.1-RESEARCH.md Split-or-Exempt Recommendations.'` and `const Set&lt;String&gt; readmeExemptDirs = {}` (empty — per the README-scope decision below, every directory gets a README, no exemptions).

    Create `tool/check_architecture.dart` using the research's draft script as the base (dart:io only, zero dependencies). Implement three checks: (1) line-count &gt; 150 per hand-written `.dart` file under `lib/`, skipping files matching `isGenerated()` (mirrors analysis_options.yaml's exclude list) and files present in `lineLimitExemptions` (print exemptions used, do not count as violations); (2) hand-written `.dart` file count &gt; 10 per directory, grouped by `entity.parent.path` (a file's immediate parent only — subdirectories do not count toward their parent's total); (3) missing `README.md` for every directory under `lib/presentation/` and `lib/application/` (recursive, literal reading — every directory, no exemptions, per the README-scope decision recorded in this plan's objective). Import `lineLimitExemptions` and `readmeExemptDirs` from `architecture_exemptions.dart`. Print a violation list to stderr and `exit(1)` if any violations exist, otherwise print "Architecture guard: PASS" and `exit(0)`. This script is NOT called from CI yet — do not touch `.github/workflows/ci.yml` in this plan.
  </action>
  <acceptance_criteria>
    - `dart run tool/check_architecture.dart` runs to completion (does not crash) and prints a violation list matching the current measured baseline: 21 file-length violations, 2 directory-size violations (`lib/domain/finance`, `lib/data/finance`), and missing-README violations for 28 of 30 directories under `lib/presentation` + `lib/application`.
    - `tool/architecture_exemptions.dart`'s `lineLimitExemptions` contains exactly one entry (`currencies.dart`) with a non-empty justification string.
    - `currencies.dart` does NOT appear in the guard's line-count violation list (confirms the exemption is honored), and DOES appear in a printed "Documented exemptions in effect" section.
  </acceptance_criteria>
  <verify>
    <automated>dart run tool/check_architecture.dart; echo "exit code: $?"</automated>
  </verify>
  <done>Guard script and exemption allowlist exist, run cleanly, and currencies.dart is confirmed exempted rather than flagged.</done>
</task>

<task type="auto">
  <name>Task 2: Unit-test the guard's three checks against synthetic fixtures</name>
  <files>test/tool/check_architecture_test.dart</files>
  <read_first>tool/check_architecture.dart (the functions/constants just written in Task 1 — refactor the top-level check logic into testable functions if `main()` currently inlines everything)</read_first>
  <behavior>
    - Test 1: a file with 151 lines under a temp `lib/`-shaped fixture directory is reported as a line-count violation; a file with exactly 150 lines is not.
    - Test 2: a directory containing 11 hand-written `.dart` files is reported as a directory-size violation; one with 10 is not. A `.g.dart` file in the same directory does not count toward the 10.
    - Test 3: a directory under a fixture `presentation/` tree with no `README.md` is reported as a missing-README violation; the same directory with a `README.md` present is not.
    - Test 4: a file listed in `lineLimitExemptions` that exceeds 150 lines is NOT reported as a violation.
  </behavior>
  <action>
    If Task 1's `check_architecture.dart` keeps all logic inline in `main()`, refactor the three check computations (line-count scan, directory-size scan, README-presence scan) into named top-level functions that accept a root `Directory` parameter instead of hardcoding `Directory('lib')`, so tests can point them at a `Directory.systemTemp.createTempSync()` fixture instead of the live `lib/` tree. Keep `main()` as a thin wrapper calling these functions against `Directory('lib')`. Write `test/tool/check_architecture_test.dart` using `package:test` (not `flutter_test` — this is a pure Dart tool, no Flutter dependency needed) with `setUp`/`tearDown` creating and deleting a temp directory per test, covering the four behaviors above.
  </action>
  <verify>
    <automated>dart test test/tool/check_architecture_test.dart</automated>
  </verify>
  <done>All four fixture-based tests pass; the guard's logic is proven correct independent of the live lib/ tree's ever-changing state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | `tool/check_architecture.dart` is a dev-time/CI-time script; it never ships in the app bundle and has no runtime attack surface. It reads the local filesystem only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-03.1-01-01 | Tampering | `tool/architecture_exemptions.dart` | accept | A developer could add an unjustified exemption to silence the guard; mitigated socially (git history + `<automated>` verify prints exemptions on every run so they're visible in CI logs, not silently swallowed), not technically — appropriate for a solo-developer offline project with no external attackers. |
</threat_model>

<verification>
`dart run tool/check_architecture.dart` exits with output matching the measured baseline (informational only — not wired into CI yet). `dart test test/tool/check_architecture_test.dart` passes. `flutter analyze` remains clean (this plan adds Dart-only files, no Flutter widget code).
</verification>

<success_criteria>
The guard script exists, is unit-tested independent of the live tree, correctly flags the known 21/2/28 baseline violations, and correctly exempts `currencies.dart`. Not yet wired into CI (deferred to 03.1-18).
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-architecture-compliance/03.1-01-SUMMARY.md`
</output>
