---
phase: 03.1
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/data/finance/finance_mappers.dart
  - lib/data/finance/transaction_mapper.dart
  - lib/data/finance/transaction_category_mapper.dart
  - lib/data/finance/budget_mapper.dart
  - lib/data/finance/goal_mapper.dart
  - lib/data/finance/debt_mapper.dart
  - lib/data/finance/recurring_payment_mapper.dart
  - lib/infrastructure/finance/transaction_repository_impl.dart
  - lib/infrastructure/finance/transaction_category_repository_impl.dart
  - lib/infrastructure/finance/budget_repository_impl.dart
  - lib/infrastructure/finance/goal_repository_impl.dart
  - lib/infrastructure/finance/debt_repository_impl.dart
  - lib/infrastructure/finance/recurring_payment_repository_impl.dart
  - lib/config/di/finance_module.dart
  - test/data/finance/transaction_mapper_test.dart
  - test/data/finance/savings_goal_mapper_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every mapper's toDomain/toModel conversion is byte-identical to before the split — same field mappings, same autoIncrement-id guard, same enum switch expressions"
    - "finance_mappers.dart no longer exists; six per-entity mapper files exist in its place"
  artifacts:
    - path: "lib/data/finance/transaction_mapper.dart"
      provides: "TransactionMapper, split out of finance_mappers.dart"
    - path: "lib/data/finance/budget_mapper.dart"
      provides: "BudgetMapper, split out of finance_mappers.dart"
    - path: "lib/data/finance/goal_mapper.dart"
      provides: "GoalMapper, split out of finance_mappers.dart"
    - path: "lib/data/finance/debt_mapper.dart"
      provides: "DebtMapper, split out of finance_mappers.dart"
    - path: "lib/data/finance/recurring_payment_mapper.dart"
      provides: "RecurringPaymentMapper, split out of finance_mappers.dart"
    - path: "lib/data/finance/transaction_category_mapper.dart"
      provides: "TransactionCategoryMapper, split out of finance_mappers.dart"
  key_links:
    - from: "lib/infrastructure/finance/transaction_repository_impl.dart"
      to: "lib/data/finance/transaction_mapper.dart"
      via: "import + TransactionMapper() instantiation"
      pattern: "import 'package:agenda/data/finance/transaction_mapper.dart'"
---

<objective>
Split `finance_mappers.dart` (327 lines) into six per-entity files, one class per file — the SPLIT decision from `<decisions_already_made>` #2, following research's recommendation exactly (each `class XMapper { toDomain(); toModel(); }` pair is already fully independent with zero shared state between them, making this the lowest-risk split in the whole phase). This plan runs in Wave 1, before the `data/finance` directory-nesting plan (`3.1-17`), because those six files are exactly the per-entity file set that nest needs — splitting once here means `3.1-17` only has to `git mv` files into subfolders, not also decompose a monolith while doing so (see research's sequencing note on why these must not happen in the same commit/plan).

Purpose: closes the `data/finance/finance_mappers.dart` violation and produces the six files plan `3.1-17` depends on for its per-entity folder structure.
Output: `finance_mappers.dart` deleted; 6 new mapper files; 6 consumer imports + `finance_module.dart` + `injection.config.dart` updated; 2 test files' imports updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
</context>

<interfaces>
`finance_mappers.dart` (327 lines) contains, in order: `TransactionMapper` (19-71), `TransactionCategoryMapper` (74-116), `BudgetMapper` (119-151), `GoalMapper` (156-211, includes the `GoalContribution` embedded-list mapping with its "CRITICAL: initialize as growable list" comment), `DebtMapper` (214-266), `RecurringPaymentMapper` (269-327). Each class only imports its own domain entity + its own data model (some via `as data`/`as domain` prefixes to disambiguate identically-named enums between the two layers — preserve these import aliases exactly per-file).

Consumers (verified by grep): `lib/infrastructure/finance/{transaction,transaction_category,budget,goal,debt,recurring_payment}_repository_impl.dart` (one each, each only uses its own entity's mapper class) and `lib/config/di/finance_module.dart`. `test/data/finance/transaction_mapper_test.dart` imports `finance_mappers.dart` for `TransactionMapper` only. `test/data/finance/savings_goal_mapper_test.dart` imports `finance_mappers.dart` for both `GoalMapper` and `DebtMapper`.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the six per-entity mapper files</name>
  <files>lib/data/finance/transaction_mapper.dart, lib/data/finance/transaction_category_mapper.dart, lib/data/finance/budget_mapper.dart, lib/data/finance/goal_mapper.dart, lib/data/finance/debt_mapper.dart, lib/data/finance/recurring_payment_mapper.dart</files>
  <read_first>lib/data/finance/finance_mappers.dart (full file)</read_first>
  <action>
    Create each of the six files with exactly one mapper class moved verbatim from `finance_mappers.dart`, including its doc comment and only the imports that specific class actually uses (drop the other five classes' unused imports per file — e.g. `transaction_mapper.dart` only needs `data/finance/transaction_model.dart as data`, `domain/finance/transaction.dart`, `domain/finance/transaction_type.dart as domain`). Preserve every `as data`/`as domain` aliasing exactly as in the original file. Do not modify any method body — this is a pure cut-and-paste-by-class split.
  </action>
  <acceptance_criteria>
    - `wc -l lib/data/finance/transaction_mapper.dart lib/data/finance/transaction_category_mapper.dart lib/data/finance/budget_mapper.dart lib/data/finance/goal_mapper.dart lib/data/finance/debt_mapper.dart lib/data/finance/recurring_payment_mapper.dart` all report ≤150 (each individually — largest is `GoalMapper` at ~56 lines and `DebtMapper`/`RecurringPaymentMapper` at ~60, all comfortably under).
    - `dart analyze lib/data/finance/transaction_mapper.dart lib/data/finance/transaction_category_mapper.dart lib/data/finance/budget_mapper.dart lib/data/finance/goal_mapper.dart lib/data/finance/debt_mapper.dart lib/data/finance/recurring_payment_mapper.dart` reports zero errors (each compiles standalone).
  </acceptance_criteria>
  <verify>
    <automated>dart analyze lib/data/finance/transaction_mapper.dart lib/data/finance/transaction_category_mapper.dart lib/data/finance/budget_mapper.dart lib/data/finance/goal_mapper.dart lib/data/finance/debt_mapper.dart lib/data/finance/recurring_payment_mapper.dart</automated>
  </verify>
  <done>All six mapper files exist, compile standalone, each ≤150 lines (comfortably, given the largest class is ~60 lines).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete finance_mappers.dart and repoint every consumer</name>
  <files>lib/data/finance/finance_mappers.dart, lib/infrastructure/finance/transaction_repository_impl.dart, lib/infrastructure/finance/transaction_category_repository_impl.dart, lib/infrastructure/finance/budget_repository_impl.dart, lib/infrastructure/finance/goal_repository_impl.dart, lib/infrastructure/finance/debt_repository_impl.dart, lib/infrastructure/finance/recurring_payment_repository_impl.dart, lib/config/di/finance_module.dart, test/data/finance/transaction_mapper_test.dart, test/data/finance/savings_goal_mapper_test.dart</files>
  <read_first>
    lib/infrastructure/finance/transaction_repository_impl.dart, lib/infrastructure/finance/budget_repository_impl.dart, lib/infrastructure/finance/goal_repository_impl.dart, lib/infrastructure/finance/debt_repository_impl.dart, lib/infrastructure/finance/recurring_payment_repository_impl.dart, lib/infrastructure/finance/transaction_category_repository_impl.dart (grep each for `finance_mappers` import line — confirm which single mapper class each one actually instantiates)
  </read_first>
  <action>
    Delete `lib/data/finance/finance_mappers.dart`. In each of the six `infrastructure/finance/*_repository_impl.dart` files, replace `import 'package:agenda/data/finance/finance_mappers.dart';` with the one specific new mapper import it needs (e.g. `transaction_repository_impl.dart` → `import 'package:agenda/data/finance/transaction_mapper.dart';`). Update `lib/config/di/finance_module.dart`'s import the same way if it references `finance_mappers.dart` directly (check whether it imports specific mapper classes or only re-exports via the repository impls — update whichever imports are present). Regenerate `injection.config.dart` via `build_runner` (it may hold a stale import path). Update `test/data/finance/transaction_mapper_test.dart`'s import to `package:agenda/data/finance/transaction_mapper.dart`, and `test/data/finance/savings_goal_mapper_test.dart`'s import to both `package:agenda/data/finance/goal_mapper.dart` and `package:agenda/data/finance/debt_mapper.dart`.
  </action>
  <acceptance_criteria>
    - `test -f lib/data/finance/finance_mappers.dart && echo STILL_EXISTS || echo DELETED` reports `DELETED`.
    - `grep -rl "finance_mappers" lib/ test/` reports no matches (zero remaining references anywhere).
    - `dart run build_runner build --delete-conflicting-outputs` completes without error.
    - `flutter analyze` reports zero new errors or warnings across the whole tree.
    - `flutter test test/data/finance/transaction_mapper_test.dart test/data/finance/savings_goal_mapper_test.dart` passes unchanged with the same test count as before this split.
    - `flutter test --no-pub` full suite passes with test count ≥ 230 (the phase's measured baseline).
  </acceptance_criteria>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs && flutter analyze && flutter test test/data/finance/transaction_mapper_test.dart test/data/finance/savings_goal_mapper_test.dart</automated>
  </verify>
  <done>finance_mappers.dart deleted; every consumer (6 repository impls, DI module, 2 test files) repointed to the correct new per-entity mapper file; full analyze and targeted mapper tests pass; DI graph regenerates cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure data-layer file split; no mapping logic, enum translation, or `autoIncrement` guard behavior changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-03.1-11-01 | Tampering (silent data corruption) | Six mapper classes | mitigate | Every `toDomain`/`toModel` body moved verbatim, including the `if (id != 0) model.id = id;` autoIncrement guard comment in each class — a subtle bug here would corrupt persisted records; existing `transaction_mapper_test.dart`/`savings_goal_mapper_test.dart` (covering `TransactionMapper`, `GoalMapper`, `DebtMapper`) re-run unchanged as the regression gate for those three; the other three (`TransactionCategoryMapper`, `BudgetMapper`, `RecurringPaymentMapper`) have no dedicated unit test today — covered indirectly by the full suite's finance repository/cubit tests, which exercise them through `flutter test --no-pub`. |
</threat_model>

<verification>
`dart run build_runner build --delete-conflicting-outputs` succeeds. Full-tree `flutter analyze` clean. `flutter test --no-pub` full suite green, test count ≥ 230. Zero remaining references to `finance_mappers`.
</verification>

<success_criteria>
finance_mappers.dart replaced by six ≤150-line per-entity mapper files; every consumer repointed; full suite green; ready for plan `3.1-17`'s data/finance directory nest to move these files into per-entity subfolders.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-architecture-compliance/03.1-11-SUMMARY.md`
</output>
