---
phase: 06
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/presentation/tasks/screens/task_detail_screen.dart
  - lib/presentation/tasks/widgets/detail/task_detail_chips.dart
  - lib/presentation/tasks/widgets/detail/task_detail_section_card.dart
  - lib/presentation/tasks/widgets/detail/task_detail_hero_card.dart
  - lib/presentation/tasks/widgets/detail/task_detail_sections.dart
  - lib/presentation/tasks/widgets/detail/task_detail_action_bar.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Opening a task's detail screen still shows the hero card (title, description, status/priority/size badges), dates card, flags card, GTD card, and finance-link chip exactly as before, conditionally per the same hasDateInfo/hasFlags/hasGtd rules"
    - "Delete confirmation dialog and edit navigation still work identically"
    - "task_detail_screen.dart is at or under 150 lines; every new extracted file is at or under 150 lines"
  artifacts:
    - path: "lib/presentation/tasks/widgets/detail/task_detail_sections.dart"
      provides: "Composed body: hero card + dates/flags/gtd cards + finance-link chip, taking an Item and rendering the conditional sections"
  key_links:
    - from: "lib/presentation/tasks/screens/task_detail_screen.dart"
      to: "lib/presentation/tasks/widgets/detail/task_detail_sections.dart"
      via: "TaskDetailBody(item: item) inside Scaffold.body"
      pattern: "TaskDetailBody\\("
---

<objective>
Decompose `task_detail_screen.dart` (563 lines, the second-largest file in scope) following Pattern 1 (screens own state, widgets take data). This is a `StatelessWidget` with no controllers to worry about, so the split is lower-risk than the form screens — pure UI composition + six small presentational "atom" widgets currently defined as private classes at the bottom of the file.

New widgets go into a new `lib/presentation/tasks/widgets/detail/` subfolder rather than the flat `lib/presentation/tasks/widgets/` root, because the root already holds 4 files and plan `6-06` adds 3 more there — without this subfolder, the root would land at 11 files and create a *new* directory-size violation. `task_detail_screen.dart` itself is NOT moved (stays in `presentation/tasks/screens/`), so `task_list_screen.dart`'s existing import of it is untouched — avoids a cross-plan file conflict with `6-06`.

Purpose: closes the second-largest violation with zero import fallout elsewhere.
Output: `task_detail_screen.dart` at ≤150 lines; 5 new files under `presentation/tasks/widgets/detail/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@lib/presentation/tasks/form/gtd/widgets/gtd_atoms.dart
</context>

<interfaces>
Current `task_detail_screen.dart` (563 lines) structure, read in full during planning:
- `TaskDetailScreen extends StatelessWidget` (14-355): label helpers `_priorityLabel`/`_sizeLabel`/`_recurrenceLabel`/`_priorityColor` (19-48), `_confirmDelete` (50-88, shows an `AlertDialog`, calls `TaskListCubit.softDelete` + undo snackbar), `_navigateToEdit` (90-99), `build()` (101-355: Scaffold+AppBar, hero card with status/priority/size chips + title + description, conditional Dates/Flags/GTD `_SectionCard`s, finance-link `ActionChip`, `bottomNavigationBar` with delete/edit buttons).
- Reusable widgets at file bottom (361-563): `_SectionCard` (361-405), `_DetailRow` (407-448), `_StatusChip` (450-480), `_PriorityChip` (482-505), `_SizeChip` (507-523), `_FlagChip` (525-563).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract the small presentational atoms</name>
  <files>lib/presentation/tasks/widgets/detail/task_detail_chips.dart, lib/presentation/tasks/widgets/detail/task_detail_section_card.dart</files>
  <read_first>lib/presentation/tasks/screens/task_detail_screen.dart (full file, focus on lines 361-563)</read_first>
  <action>
    Create `lib/presentation/tasks/widgets/detail/task_detail_chips.dart` containing the four small chip widgets, made public: `StatusChip`, `PriorityChip`, `SizeChip`, `FlagChip` — move verbatim, only renaming from `_StatusChip` etc. and updating constructor visibility.
    Create `lib/presentation/tasks/widgets/detail/task_detail_section_card.dart` containing `SectionCard` and `DetailRow` (public, moved verbatim from `_SectionCard`/`_DetailRow`).
    Do not touch `task_detail_screen.dart` in this task — it still references the private names; Task 2 rewires everything in one pass to avoid a broken intermediate state.
  </action>
  <acceptance_criteria>
    - `wc -l lib/presentation/tasks/widgets/detail/task_detail_chips.dart lib/presentation/tasks/widgets/detail/task_detail_section_card.dart` both report ≤150.
    - `dart analyze lib/presentation/tasks/widgets/detail/` reports zero errors (the two new files are self-contained and compile in isolation even though nothing imports them yet).
  </acceptance_criteria>
  <verify>
    <automated>dart analyze lib/presentation/tasks/widgets/detail/</automated>
  </verify>
  <done>Both atom files exist, compile standalone, and are ≤150 lines.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract hero card + section composition + action bar, wire into the screen</name>
  <files>lib/presentation/tasks/widgets/detail/task_detail_hero_card.dart, lib/presentation/tasks/widgets/detail/task_detail_sections.dart, lib/presentation/tasks/widgets/detail/task_detail_action_bar.dart, lib/presentation/tasks/screens/task_detail_screen.dart</files>
  <read_first>
    lib/presentation/tasks/screens/task_detail_screen.dart (full file — this task rewrites `build()` entirely)
    lib/presentation/tasks/widgets/detail/task_detail_chips.dart, lib/presentation/tasks/widgets/detail/task_detail_section_card.dart (just-created, from Task 1)
  </read_first>
  <action>
    Create `lib/presentation/tasks/widgets/detail/task_detail_hero_card.dart`: `class TaskDetailHeroCard extends StatelessWidget` taking `item`, rendering the status/priority/size chip row + title + optional description (moves lines ~124-174 verbatim, using `StatusChip`/`PriorityChip`/`SizeChip` from Task 1). Move `_priorityLabel`, `_sizeLabel`, `_priorityColor` into this file as private top-level helpers (used only here).

    Create `lib/presentation/tasks/widgets/detail/task_detail_sections.dart`: `class TaskDetailBody extends StatelessWidget` taking `item`, composing `TaskDetailHeroCard` + the conditional Dates/Flags/GTD `SectionCard`s + the finance-link `ActionChip` block (moves lines ~176-315, using `SectionCard`/`DetailRow`/`FlagChip` from Task 1). Move `_recurrenceLabel` into this file (only used inside the Dates card).

    Create `lib/presentation/tasks/widgets/detail/task_detail_action_bar.dart`: `class TaskDetailActionBar extends StatelessWidget` taking `onDelete` and `onEdit` callbacks, rendering the bottom `OutlinedButton`+`FilledButton` row (moves lines ~317-352 verbatim).

    Rewrite `task_detail_screen.dart`'s `build()` to: `Scaffold(appBar: ..., body: TaskDetailBody(item: item), bottomNavigationBar: SafeArea(child: TaskDetailActionBar(onDelete: () => _confirmDelete(context, l10n), onEdit: () => _navigateToEdit(context))))`. Keep `_confirmDelete` and `_navigateToEdit` in the screen unchanged (they own the `TaskListCubit` interaction, correctly staying at the screen level per Pattern 1).
  </action>
  <acceptance_criteria>
    - `for f in lib/presentation/tasks/screens/task_detail_screen.dart lib/presentation/tasks/widgets/detail/task_detail_hero_card.dart lib/presentation/tasks/widgets/detail/task_detail_sections.dart lib/presentation/tasks/widgets/detail/task_detail_action_bar.dart; do n=$(wc -l &lt; "$f"); [ "$n" -le 150 ] || echo "FAIL $f: $n"; done` prints nothing.
    - `flutter analyze lib/presentation/tasks/` reports zero new errors or warnings.
    - Manual verification (no dedicated widget test exists for this screen today): on-device or `flutter run`, open a task with a due date, recurrence, GTD context, urgent+important flags, and a finance link — confirm all four cards render with correct data, delete shows the confirm dialog and undo snackbar, edit navigates to the pre-filled form.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/presentation/tasks/</automated>
  </verify>
  <done>task_detail_screen.dart and all four newly extracted files are ≤150 lines; screen composes TaskDetailBody + TaskDetailActionBar; manual smoke pass completed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure read-only display refactor; the only mutation path (`softDelete`) is unchanged, still owned by the screen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-06-03-01 | Tampering (UI regression) | `TaskDetailBody`'s conditional rendering (`hasDateInfo`/`hasFlags`/`hasGtd`) | mitigate | Moved verbatim with the exact same boolean expressions; manual smoke pass explicitly exercises a task with all three conditional sections populated to prove none silently dropped. |
</threat_model>

<verification>
`flutter analyze lib/presentation/tasks/` clean. Every file in scope ≤150 lines. Manual on-device smoke pass (no existing widget test for this screen).
</verification>

<success_criteria>
task_detail_screen.dart and its 5 new extracted files are all ≤150 lines; visual output and delete/edit behavior unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/06-architecture-compliance/06-03-SUMMARY.md`
</output>
