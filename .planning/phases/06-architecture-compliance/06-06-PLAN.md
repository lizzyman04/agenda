---
phase: 06
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/presentation/tasks/screens/task_list_screen.dart
  - lib/presentation/tasks/widgets/task_list_view.dart
  - lib/presentation/tasks/screens/day_planner_screen.dart
  - lib/presentation/tasks/widgets/task_picker_sheet.dart
  - lib/presentation/tasks/screens/project_screen.dart
  - lib/presentation/tasks/widgets/add_subtask_sheet.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Task list screen still shows search, filter, empty state, task list with complete/delete/undo, and FAB-driven task creation identically"
    - "Day planner still shows the 1-3-5 slots, warning banner, and task-picker sheet identically"
    - "Project screen still shows subtask progress, the subtask list, and the add-subtask sheet (which still owns and disposes its own TextEditingController) identically"
    - "All three screens are at or under 150 lines"
  artifacts:
    - path: "lib/presentation/tasks/widgets/task_list_view.dart"
      provides: "TaskListEmptyState + TaskListView widgets, extracted from task_list_screen.dart"
    - path: "lib/presentation/tasks/widgets/task_picker_sheet.dart"
      provides: "TaskPickerSheet widget + the slot-size enum, extracted from day_planner_screen.dart"
    - path: "lib/presentation/tasks/widgets/add_subtask_sheet.dart"
      provides: "AddSubtaskSheet — StatefulWidget owning/disposing its own TextEditingController, extracted from project_screen.dart"
  key_links:
    - from: "lib/presentation/tasks/screens/task_list_screen.dart"
      to: "lib/presentation/tasks/widgets/task_list_view.dart"
      via: "TaskListEmptyState(l10n: l10n) / TaskListView(items: items, onEdit: _navigateToItem)"
      pattern: "TaskListView\\("
---

<objective>
Split the three remaining tasks-side screens between 185 and 201 lines by extracting each screen's already-self-contained private widget classes (list/empty-state renderer, bottom-sheet picker, add-subtask sheet) into `lib/presentation/tasks/widgets/`. All three extractions are low-risk: each private class is already well-isolated (takes data + callbacks, or already owns/disposes its own controller correctly per the documented crash pattern), so this is a pure file-move with a visibility change, not a redesign.

Purpose: closes the three smallest tasks-side screen violations in one plan, since each extraction is independently small.
Output: three screens at ≤150 lines; three new files in `presentation/tasks/widgets/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@lib/presentation/finance/goals/README.md
</context>

<interfaces>
`task_list_screen.dart` (201 lines): `_EmptyState` (153-173) and `_TaskList` (175-201) are private `StatelessWidget`s at the file bottom, already taking only `l10n` / `items`+`onEdit` — no state ownership.

`day_planner_screen.dart` (200 lines): `enum _SlotSize` (122), `_WarningBanner` (124-151), `_TaskPickerSheet` (154-200) — `_TaskPickerSheet` is a `StatelessWidget` taking `items`+`slotSize`, reads `DayPlannerCubit` via `context.read` inside its own `onTap`, no controller involved.

`project_screen.dart` (185 lines): `_AddSubtaskSheet`/`_AddSubtaskSheetState` (115-185) — already correctly a `StatefulWidget` that creates `_controller` in `initState` and disposes it in `dispose` (the file's own comment marks this "WR-03" — a prior fix for the exact controller-lifecycle bug documented in `finance/goals/README.md`). This extraction must preserve that ownership exactly — do not change where the controller is created or disposed.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract task_list_screen.dart's list/empty-state widgets</name>
  <files>lib/presentation/tasks/screens/task_list_screen.dart, lib/presentation/tasks/widgets/task_list_view.dart</files>
  <read_first>lib/presentation/tasks/screens/task_list_screen.dart (full file)</read_first>
  <action>
    Create `lib/presentation/tasks/widgets/task_list_view.dart` with public `TaskListEmptyState` (renamed from `_EmptyState`) and `TaskListView` (renamed from `_TaskList`), moved verbatim. Update `task_list_screen.dart`'s `build()` switch expression to reference the new public names with an import added. If the file is still over 150 lines after this single extraction, additionally extract the `AppBar`'s `bottom: PreferredSize(...SearchBar...)` block into a small `TaskSearchBar` `StatelessWidget` (taking `hintText`, `onChanged`) in the same new file, and use it in place of the inline `PreferredSize`+`SearchBar` block.
  </action>
  <acceptance_criteria>
    - `wc -l lib/presentation/tasks/screens/task_list_screen.dart lib/presentation/tasks/widgets/task_list_view.dart` both report ≤150.
    - `flutter test test/presentation/tasks/task_list_screen_test.dart` passes unchanged with the same test count.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/presentation/tasks/ && flutter test test/presentation/tasks/task_list_screen_test.dart</automated>
  </verify>
  <done>task_list_screen.dart ≤150 lines; existing task_list_screen_test.dart passes unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract day_planner_screen.dart's task-picker sheet</name>
  <files>lib/presentation/tasks/screens/day_planner_screen.dart, lib/presentation/tasks/widgets/task_picker_sheet.dart</files>
  <read_first>lib/presentation/tasks/screens/day_planner_screen.dart (full file)</read_first>
  <action>
    Create `lib/presentation/tasks/widgets/task_picker_sheet.dart` with a public `enum SlotSize { big, medium, small }`, public `WarningBanner` (renamed from `_WarningBanner`), and public `TaskPickerSheet` (renamed from `_TaskPickerSheet`), moved verbatim. Update `day_planner_screen.dart`'s `_showTaskPicker` and the three `SlotSection.onTapAdd` callsites to reference `SlotSize.big`/`.medium`/`.small` from the new file, with the import added.
  </action>
  <acceptance_criteria>
    - `wc -l lib/presentation/tasks/screens/day_planner_screen.dart lib/presentation/tasks/widgets/task_picker_sheet.dart` both report ≤150.
    - `flutter test test/presentation/tasks/day_planner_test.dart` passes unchanged with the same test count.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/presentation/tasks/ && flutter test test/presentation/tasks/day_planner_test.dart</automated>
  </verify>
  <done>day_planner_screen.dart ≤150 lines; existing day_planner_test.dart passes unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extract project_screen.dart's add-subtask sheet</name>
  <files>lib/presentation/tasks/screens/project_screen.dart, lib/presentation/tasks/widgets/add_subtask_sheet.dart</files>
  <read_first>
    lib/presentation/tasks/screens/project_screen.dart (full file, focus on the WR-03 comment above `_AddSubtaskSheet`)
    lib/presentation/finance/goals/README.md (the controller-ownership rule this file already correctly follows — confirm the moved widget still follows it)
  </read_first>
  <action>
    Create `lib/presentation/tasks/widgets/add_subtask_sheet.dart` with a public `AddSubtaskSheet`/`AddSubtaskSheetState` (renamed from `_AddSubtaskSheet`/`_AddSubtaskSheetState`), moved verbatim — preserve the exact `initState`/`dispose` controller lifecycle unchanged; this is the pattern the rest of the codebase is being retrofitted to match, do not alter it. Update `project_screen.dart`'s `_showAddSubtaskSheet` to reference the new public name with the import added. No dedicated widget test exists for `project_screen.dart` today (per research Pitfall 4).
  </action>
  <acceptance_criteria>
    - `wc -l lib/presentation/tasks/screens/project_screen.dart lib/presentation/tasks/widgets/add_subtask_sheet.dart` both report ≤150.
    - `flutter analyze lib/presentation/tasks/` reports zero new errors or warnings.
    - Manual verification (no existing test covers this screen): on-device or `flutter run`, open a project, tap add-subtask, type a title, submit, confirm the subtask appears and the sheet closes without a `TextEditingController used after being disposed` error; also test the empty-title case (submit stays open, no crash).
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/presentation/tasks/</automated>
  </verify>
  <done>project_screen.dart ≤150 lines; add-subtask sheet's controller ownership preserved exactly; manual smoke pass completed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure UI-composition refactor; the one file with a controller (`AddSubtaskSheet`) already follows the correct ownership pattern and this plan preserves it unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-06-06-01 | Tampering (controller lifecycle regression) | `AddSubtaskSheet` | mitigate | Task 3's action explicitly forbids altering where the controller is created/disposed; manual smoke pass specifically checks for the documented disposal crash shape. |
</threat_model>

<verification>
`flutter analyze lib/presentation/tasks/` clean. `flutter test test/presentation/tasks/task_list_screen_test.dart test/presentation/tasks/day_planner_test.dart` green. Manual smoke pass for `project_screen.dart` (untested). All three screens and three new files ≤150 lines.
</verification>

<success_criteria>
task_list_screen.dart, day_planner_screen.dart, project_screen.dart all ≤150 lines; existing tests pass unchanged; add-subtask controller ownership preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/06-architecture-compliance/06-06-SUMMARY.md`
</output>
