---
phase: 06
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/presentation/finance/screens/finance_dashboard_screen.dart
  - lib/presentation/finance/widgets/dashboard/dashboard_tab.dart
  - lib/presentation/finance/widgets/dashboard/dashboard_month_header.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "The 6-tab Finance shell (Resumo/Transações/Orçamentos/Objetivos/Dívidas/Recorrências) renders identically"
    - "The Resumo tab still shows the balance/net-worth summary, month navigation (disabled forward arrow on the current month per D-10), and pie+bar charts identically"
    - "finance_dashboard_screen.dart is at or under 150 lines"
  artifacts:
    - path: "lib/presentation/finance/widgets/dashboard/dashboard_tab.dart"
      provides: "DashboardTab — the Resumo tab's cubit-driven state + loaded-state composition, extracted from FinanceDashboardScreen's _DashboardTab + _buildLoaded"
    - path: "lib/presentation/finance/widgets/dashboard/dashboard_month_header.dart"
      provides: "MonthNavigationHeader + EmptyChartMessage, extracted verbatim"
  key_links:
    - from: "lib/presentation/finance/screens/finance_dashboard_screen.dart"
      to: "lib/presentation/finance/widgets/dashboard/dashboard_tab.dart"
      via: "const DashboardTab() inside the TabBarView children list"
      pattern: "DashboardTab\\("
---

<objective>
Split `finance_dashboard_screen.dart` (269 lines) by extracting the Resumo tab's cubit-driven content out of the top-level `FinanceDashboardScreen` shell, which stays a thin `DefaultTabController`+`TabBarView` wrapper. New files go into `presentation/finance/widgets/dashboard/` (per-entity subfolder, matching the convention `6-09` establishes) to avoid overflowing the flat `widgets/` root.

Purpose: closes the finance-dashboard violation.
Output: `finance_dashboard_screen.dart` ≤150 lines; 2 new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
</context>

<interfaces>
`finance_dashboard_screen.dart` (269 lines): `FinanceDashboardScreen` (30-69, `StatelessWidget`, the `DefaultTabController` shell + `TabBar`/`TabBarView` with 6 children) stays. `_DashboardTab`/`_DashboardTabState` (73-108, calls `HomeDashboardCubit.start()` in `initState`, `BlocBuilder` switch) and `_buildLoaded` (110-195, the loaded-state composition: `DashboardSummaryCard` + `_MonthNavigationHeader` + pie/bar charts) move together. `_MonthNavigationHeader` (200-235) and `_EmptyChartMessage` (240-269) move together. The module-level `const String _currencySymbol = 'MT'` constant (line 21) is used by both the shell (none actually — only `_buildLoaded` uses it) — move it into `dashboard_tab.dart` alongside `_buildLoaded`.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract the month-navigation header and empty-chart message</name>
  <files>lib/presentation/finance/widgets/dashboard/dashboard_month_header.dart</files>
  <read_first>lib/presentation/finance/screens/finance_dashboard_screen.dart (full file, focus on lines 198-269)</read_first>
  <action>
    Create `lib/presentation/finance/widgets/dashboard/dashboard_month_header.dart` with public `MonthNavigationHeader` (renamed from `_MonthNavigationHeader`) and `EmptyChartMessage` (renamed from `_EmptyChartMessage`), moved verbatim including their doc comments (the D-10 forward-arrow-disable rationale, and the "keep chart section visible per UI-SPEC" rationale). Do not wire these into the screen yet — Task 2 rewires everything in one pass.
  </action>
  <acceptance_criteria>
    - `wc -l lib/presentation/finance/widgets/dashboard/dashboard_month_header.dart` reports ≤150.
    - `dart analyze lib/presentation/finance/widgets/dashboard/dashboard_month_header.dart` reports zero errors.
  </acceptance_criteria>
  <verify>
    <automated>dart analyze lib/presentation/finance/widgets/dashboard/dashboard_month_header.dart</automated>
  </verify>
  <done>dashboard_month_header.dart exists, compiles standalone, ≤150 lines.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract the dashboard tab and wire everything into the shell</name>
  <files>lib/presentation/finance/widgets/dashboard/dashboard_tab.dart, lib/presentation/finance/screens/finance_dashboard_screen.dart</files>
  <read_first>
    lib/presentation/finance/screens/finance_dashboard_screen.dart (full file)
    lib/presentation/finance/widgets/dashboard/dashboard_month_header.dart (just-created, from Task 1)
  </read_first>
  <action>
    Create `lib/presentation/finance/widgets/dashboard/dashboard_tab.dart` with public `DashboardTab`/`DashboardTabState` (renamed from `_DashboardTab`/`_DashboardTabState`), moving `initState` (the `HomeDashboardCubit.start()` call), `build()`'s `BlocBuilder` switch, and `_buildLoaded` verbatim, using `MonthNavigationHeader`/`EmptyChartMessage` from Task 1's file. Move the `_currencySymbol` constant into this file (it is only used here).

    Rewrite `finance_dashboard_screen.dart` to keep only `FinanceDashboardScreen` — its `TabBarView` children list changes `const _DashboardTab()` to `const DashboardTab()` with the import added.
  </action>
  <acceptance_criteria>
    - `wc -l lib/presentation/finance/screens/finance_dashboard_screen.dart lib/presentation/finance/widgets/dashboard/dashboard_tab.dart` both report ≤150.
    - `flutter analyze lib/presentation/finance/` reports zero new errors or warnings.
    - `flutter test test/presentation/finance/spending_pie_chart_test.dart` passes unchanged (the one existing test that touches a component rendered inside this tab).
    - Manual verification (no screen-level test exists for this file): on-device or `flutter run`, open the Finance tab, confirm all 6 tabs are reachable, confirm the Resumo tab shows balance/net-worth/charts and month navigation works (forward arrow disabled on current month).
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/presentation/finance/ && flutter test test/presentation/finance/spending_pie_chart_test.dart</automated>
  </verify>
  <done>finance_dashboard_screen.dart ≤150 lines; DashboardTab composes MonthNavigationHeader/EmptyChartMessage from the Task 1 file; manual smoke pass completed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure UI-composition refactor; `HomeDashboardCubit.start()` idempotency (already established per STATE.md) is unaffected — still called once per `DashboardTab` mount. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-06-10-01 | Denial of Service (duplicate subscription) | `DashboardTab.initState` | accept | `HomeDashboardCubit.start()` was already made idempotent in phase 03 (per STATE.md) specifically to survive tab rebuilds; this move does not change when or how often `initState` runs, so the existing idempotency guard remains sufficient. |
</threat_model>

<verification>
`flutter analyze lib/presentation/finance/` clean. `flutter test test/presentation/finance/spending_pie_chart_test.dart` green. Manual smoke pass for the dashboard tab (untested at screen level). Both target files ≤150 lines.
</verification>

<success_criteria>
finance_dashboard_screen.dart ≤150 lines; DashboardTab and its month-header sub-widgets extracted; Finance tab renders and functions identically.
</success_criteria>

<output>
After completion, create `.planning/phases/06-architecture-compliance/06-10-SUMMARY.md`
</output>
