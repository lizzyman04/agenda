---
phase: 06
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/data/tasks/item_dao.dart
  - lib/data/tasks/item_query_builder.dart
  - lib/infrastructure/tasks/item_repository_impl.dart
  - lib/infrastructure/tasks/item_repository_impl_queries.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "ItemDao.filterItems(...) returns the exact same result set for the same inputs as before the split"
    - "ItemRepositoryImpl still implements every ItemRepository method identically; DI registration (@LazySingleton(as: ItemRepository)) still resolves to one class"
    - "item_dao.dart and item_repository_impl.dart are at or under 150 lines"
  artifacts:
    - path: "lib/data/tasks/item_query_builder.dart"
      provides: "buildItemFilterQuery(...) — top-level function operating on the public Isar QueryBuilder type, extracted from ItemDao.filterItems"
    - path: "lib/infrastructure/tasks/item_repository_impl_queries.dart"
      provides: "part of 'item_repository_impl.dart' — query-side methods (getItemsByType, getSubtasks, searchByTitle, filterItems, getSubtaskCounts, watchChanges, getDistinctGtdContexts)"
  key_links:
    - from: "lib/data/tasks/item_dao.dart"
      to: "lib/data/tasks/item_query_builder.dart"
      via: "buildItemFilterQuery(_collection, ...).findAll() inside filterItems()"
      pattern: "buildItemFilterQuery\\("
    - from: "lib/infrastructure/tasks/item_repository_impl.dart"
      to: "lib/infrastructure/tasks/item_repository_impl_queries.dart"
      via: "part 'item_repository_impl_queries.dart'; / part of 'item_repository_impl.dart';"
      pattern: "part 'item_repository_impl_queries.dart'"
---

<objective>
Split the two task-persistence-layer files that are over the line cap, using the two mechanisms research identified for this specific pair: `item_dao.dart`'s `filterItems()` is pure `QueryBuilder` composition extractable as a top-level function (Pattern 3); `item_repository_impl.dart` is a single DI-registered class where every method needs the same private `_dao`/`_mapper` fields, so it uses `part`/`part of` (Pattern 4 — the one narrow, justified exception, already used in this codebase for generated `.g.dart` files).

Purpose: closes both data/tasks and infrastructure/tasks violations without changing either class's public interface, so the DI graph and every existing consumer are unaffected.
Output: `item_dao.dart` ≤150 lines, `item_repository_impl.dart` ≤150 lines, 2 new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-architecture-compliance/06-RESEARCH.md
</context>

<interfaces>
Current `item_dao.dart` (178 lines): `filterItems({...7 optional params...})` (45-109, 65 lines) builds `_collection.filter().deletedAtIsNull().optional(...).optional(...)...limit(500).findAll()` — a chain of `.optional()` calls on the Isar `QueryBuilder&lt;ItemModel, ItemModel, QAfterFilterCondition&gt;` type, which is public, so this chain can move to a top-level function taking the collection as a parameter.

Current `item_repository_impl.dart` (227 lines): `@LazySingleton(as: ItemRepository) class ItemRepositoryImpl implements ItemRepository` with fields `_dao`, `_mapper`, `_recurrenceEngine`. CRUD group: `createItem` (29-58), `getItem` (61-71), `updateItem` (94-108), `softDelete` (111-125), `restoreItem` (128-142). Query group: `getItemsByType` (74-81), `getSubtasks` (84-91), `searchByTitle` (145-153), `filterItems` (156-190), `getSubtaskCounts` (193-201), `watchChanges` (204), `getDistinctGtdContexts` (207-218), plus private helper `_toModelType` (222-226).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract the Isar query builder from item_dao.dart</name>
  <files>lib/data/tasks/item_dao.dart, lib/data/tasks/item_query_builder.dart</files>
  <read_first>lib/data/tasks/item_dao.dart (full file, focus on filterItems, lines 45-109)</read_first>
  <action>
    Create `lib/data/tasks/item_query_builder.dart` importing `isar_community/isar.dart` and `item_model.dart`, with a top-level function matching the `.optional()` chain's exact return type (e.g. `QueryBuilder&lt;ItemModel, ItemModel, QAfterFilterCondition&gt; buildItemFilterQuery(IsarCollection&lt;ItemModel&gt; collection, {ItemType? type, bool? isUrgent, bool? isImportant, String? gtdContext, DateTime? dueDateFrom, DateTime? dueDateTo, int? parentId, bool showCompleted = false})`), moving the entire `.filter().deletedAtIsNull().optional(...)...` chain verbatim, including the "promote nullable params to non-nullable locals" comment explaining why local variables are used inside the optional closures.

    Rewrite `item_dao.dart`'s `filterItems()` to: `return buildItemFilterQuery(_collection, type: type, ...).limit(500).findAll();` (keep `.limit(500)` and `.findAll()` in the DAO since those aren't part of the reusable filter-building logic).
  </action>
  <acceptance_criteria>
    - `wc -l lib/data/tasks/item_dao.dart lib/data/tasks/item_query_builder.dart` both report ≤150.
    - `flutter test test/data/tasks/item_dao_test.dart` passes unchanged with the same test count.
    - `flutter analyze lib/data/tasks/` reports zero new errors or warnings.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/data/tasks/ && flutter test test/data/tasks/item_dao_test.dart</automated>
  </verify>
  <done>item_dao.dart ≤150 lines; filter-query composition extracted to a standalone testable function; existing DAO test suite passes unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Split item_repository_impl.dart into CRUD and query parts</name>
  <files>lib/infrastructure/tasks/item_repository_impl.dart, lib/infrastructure/tasks/item_repository_impl_queries.dart</files>
  <read_first>
    lib/infrastructure/tasks/item_repository_impl.dart (full file)
    lib/data/finance/transaction_model.dart (existing part/part-of precedent in this codebase, for the exact directive syntax)
  </read_first>
  <action>
    Add `part 'item_repository_impl_queries.dart';` to the top of `item_repository_impl.dart` (after the existing imports), keep the class declaration, constructor, fields (`_dao`, `_mapper`, `_recurrenceEngine`), and the five CRUD methods (`createItem`, `getItem`, `updateItem`, `softDelete`, `restoreItem`) in this file.

    Create `lib/infrastructure/tasks/item_repository_impl_queries.dart` starting with `part of 'item_repository_impl.dart';`, moving the six query methods (`getItemsByType`, `getSubtasks`, `searchByTitle`, `filterItems`, `getSubtaskCounts`, `watchChanges`, `getDistinctGtdContexts`) and the private `_toModelType` helper verbatim. Because this is a `part of` file within the same library, it retains direct access to `_dao`/`_mapper` with no import needed and no interface change — `ItemRepositoryImpl` remains one class implementing `ItemRepository` with all 12 methods, exactly as before.
  </action>
  <acceptance_criteria>
    - `wc -l lib/infrastructure/tasks/item_repository_impl.dart lib/infrastructure/tasks/item_repository_impl_queries.dart` both report ≤150.
    - `flutter test test/infrastructure/tasks/item_repository_impl_test.dart` passes unchanged with the same test count — this test imports only `item_repository_impl.dart` and exercises the public `ItemRepositoryImpl` class, so it must pass with zero test-file changes.
    - `flutter analyze lib/infrastructure/tasks/` reports zero new errors or warnings.
    - `dart run build_runner build --delete-conflicting-outputs` completes without error (confirms the injectable annotation on `ItemRepositoryImpl` still resolves correctly with the part/part-of split) and `flutter test test/config/di/di_test.dart` passes.
  </acceptance_criteria>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs && flutter analyze lib/infrastructure/tasks/ && flutter test test/infrastructure/tasks/item_repository_impl_test.dart test/config/di/di_test.dart</automated>
  </verify>
  <done>item_repository_impl.dart ≤150 lines; query methods moved to a part file sharing the same library scope; DI graph still resolves ItemRepositoryImpl correctly; existing tests pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Data/infrastructure layer refactor; no query semantics or error-handling behavior changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-06-05-01 | Tampering (query result drift) | `buildItemFilterQuery` | mitigate | Moved verbatim including the nullable-to-local-promotion comment explaining the flow-analysis reason; `item_dao_test.dart` re-run unchanged as the regression gate for every filter combination it covers. |
| T-06-05-02 | Denial of Service (DI resolution failure) | `part`/`part of` split of a `@LazySingleton` class | mitigate | `build_runner` regenerated and `di_test.dart` re-run explicitly in this task's verify step, since a broken part/part-of pairing would surface as a DI resolution failure at runtime, not necessarily an analyzer error. |
</threat_model>

<verification>
`flutter analyze lib/data/tasks/ lib/infrastructure/tasks/` clean. `flutter test test/data/tasks/item_dao_test.dart test/infrastructure/tasks/item_repository_impl_test.dart test/config/di/di_test.dart` green. `build_runner` regeneration succeeds. Both target files ≤150 lines.
</verification>

<success_criteria>
item_dao.dart and item_repository_impl.dart both ≤150 lines; ItemRepository's public interface and DI registration unchanged; existing test suites pass unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/06-architecture-compliance/06-05-SUMMARY.md`
</output>
