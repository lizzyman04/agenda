---
phase: 06
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/presentation/tasks/form/screens/task_form_screen.dart
  - lib/presentation/tasks/form/widgets/finance_link_sheet.dart
  - lib/presentation/tasks/form/widgets/title_type_card.dart
  - lib/presentation/tasks/form/widgets/schedule_fields.dart
  - lib/presentation/tasks/form/widgets/flags_size_notes_fields.dart
  - lib/presentation/tasks/form/task_form_logic.dart
  - lib/presentation/tasks/form/README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Creating and editing a task through TaskFormScreen still saves identical field values as before the split (title, description, type, priority, size, urgent/important, GTD context, waiting-for, due date/time, recurrence, finance link)"
    - "The finance-link bottom sheet still lets the user pick a goal, a debt, or clear the link, and the selection is applied only after the sheet closes"
    - "task_form_screen.dart is at or under 150 lines; every new extracted file is at or under 150 lines"
  artifacts:
    - path: "lib/presentation/tasks/form/task_form_logic.dart"
      provides: "Pure functions: buildFormItem (Item construction for create/update), loadFinanceLinks (goal+debt repo loading), applyGtdResult (GtdResult -> form field values)"
    - path: "lib/presentation/tasks/form/widgets/finance_link_sheet.dart"
      provides: "FinanceLinkSheet widget + FinanceLinkSelection value type — returns the pick via Navigator.pop, does not mutate caller state directly"
  key_links:
    - from: "lib/presentation/tasks/form/screens/task_form_screen.dart"
      to: "lib/presentation/tasks/form/task_form_logic.dart"
      via: "buildFormItem(...) call inside _save()"
      pattern: "buildFormItem\\("
    - from: "lib/presentation/tasks/form/screens/task_form_screen.dart"
      to: "lib/presentation/tasks/form/widgets/finance_link_sheet.dart"
      via: "showModalBottomSheet<FinanceLinkSelection>(builder: (_) => FinanceLinkSheet(...))"
      pattern: "FinanceLinkSheet\\("
---

<objective>
Finish plan `6-01`'s work (per `<decisions_already_made>` #3: the GTD guide extraction is already done in commits `6c06312`/`04c4f84`, leaving `task_form_screen.dart` at 752 lines). Decompose the remaining form body — field groups, the finance-link picker, and the save path — following the exact conventions already documented in `lib/presentation/tasks/form/README.md` (screens own state, widgets take data + callbacks, sheets return a value via `Navigator.pop` rather than mutating caller state).

Purpose: closes the last violation in the `tasks/form/` slice, using the slice's own established patterns rather than inventing new ones.
Output: `task_form_screen.dart` at ≤150 lines; 4 new files under `presentation/tasks/form/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@lib/presentation/tasks/form/README.md
@lib/presentation/tasks/form/gtd/gtd_models.dart
</context>

<interfaces>
Current `task_form_screen.dart` (752 lines) structure, read in full during planning:
- State fields: `_formKey`, 4 `TextEditingController`s (title/description/gtdContext/waitingFor), `_itemType`, `_priority`, `_sizeCategory`, `_isUrgent`, `_isImportant`, `_isNextAction`, `_dueDate`, `_dueTime`, `_recurrenceRule`, `_advancedExpanded`, `_linkedGoalId`, `_linkedDebtId`, `_activeGoals`, `_activeDebts`, `_linkedGoalTitle`, `_linkedDebtTitle`.
- `initState()` (63-89): creates controllers, seeds state from `widget.item`, calls `_loadFinanceLinks()`.
- `_loadFinanceLinks()` (91-124): loads active goals/debts via `getIt<GoalRepository>()`/`getIt<DebtRepository>()`, resolves linked titles.
- `_pickFinanceLink()` (126-234): opens a `showModalBottomSheet` whose builder mutates `_linkedGoalId`/`_linkedDebtId`/titles directly via `setState` from inside `onTap` handlers — this is the pattern to fix per `finance/goals/README.md`'s convention (mutate only after the sheet returns).
- `_save()` (272-371): builds an `Item` via `widget.item!.copyWith(...)` (edit) or `Item(...)` (create), calls `TaskListCubit.updateItem`/`createItem`, handles the boolean result with a snackbar on failure.
- `_openGtdGuide()` (373-402): opens `GtdGuideSheet`, applies the returned `GtdResult` to controllers/state.
- `build()` (405-732): title `TextFormField` + type `SegmentedButton` (437-486), `GtdGuideCard` (490-498, stays untouched — already extracted), `AdvancedOptionsCard` wrapping priority dropdown + due date/time tiles + recurrence radio list + urgent/important switches + size `SegmentedButton` + description + waiting-for fields (501-705), finance-link summary `ListTile` (709-726).
- `_priorityLabel`/`_recurrenceLabel` (734-751): label helpers, only used inside the `AdvancedOptionsCard` children.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract non-widget logic and the finance-link sheet</name>
  <files>lib/presentation/tasks/form/task_form_logic.dart, lib/presentation/tasks/form/widgets/finance_link_sheet.dart, lib/presentation/tasks/form/screens/task_form_screen.dart</files>
  <read_first>
    lib/presentation/tasks/form/screens/task_form_screen.dart (full file — this task rewrites `_loadFinanceLinks`, `_pickFinanceLink`, `_save`, `_openGtdGuide`, `initState`)
    lib/presentation/finance/goals/README.md (the sheet-ownership convention: sheet returns a value via pop; caller applies mutation only after await completes)
    lib/presentation/tasks/form/gtd/gtd_models.dart (GtdResult's exact fields, needed by `applyGtdResult`)
  </read_first>
  <action>
    Create `lib/presentation/tasks/form/task_form_logic.dart` (a plain non-widget Dart file, mirroring the "pure functions in a sibling file" pattern from `gtd_tree_clarify.dart`) with three top-level functions and one data class:
    - `class FinanceLinksSnapshot { final List&lt;SavingsGoal&gt; activeGoals; final List&lt;Debt&gt; activeDebts; final String? linkedGoalTitle; final String? linkedDebtTitle; }`
    - `Future&lt;FinanceLinksSnapshot&gt; loadFinanceLinks(GoalRepository goalRepo, DebtRepository debtRepo, {int? linkedGoalId, int? linkedDebtId})` — moves the body of the current `_loadFinanceLinks()` here verbatim (same `Success`/`firstWhere`/try-catch logic), returning the snapshot instead of calling `setState` directly.
    - `Item buildFormItem({required bool isEditing, required Item? original, required String title, String? description, required ItemType itemType, required Priority priority, required SizeCategory sizeCategory, required bool isUrgent, required bool isImportant, required bool isNextAction, String? gtdContext, String? waitingFor, DateTime? dueDate, int? dueTimeMinutes, String? recurrenceRule, int? linkedGoalId, int? linkedDebtId, required DateTime now})` — moves the `Item(...)`/`copyWith(...)` construction logic from `_save()` verbatim, including the exact parentId-clearing invariant comment and the `linkedGoalId ?? clearField` / `linkedDebtId ?? clearField` behavior for the edit path. Returns the `Item` to save; does NOT call the cubit.
    - A small record-returning function `({String title, String? description, Priority priority, bool isUrgent, bool isImportant, DateTime? dueDate, String? waitingFor, String? gtdContext}) applyGtdResult(GtdResult result, {required String currentDescription})` that computes the final field values from a `GtdResult` (moves the assignment logic out of `_openGtdGuide()`, preserving the exact same conditionals: only overwrite description if currently empty, `isNextAction` always becomes `true`).

    Create `lib/presentation/tasks/form/widgets/finance_link_sheet.dart` with `class FinanceLinkSelection { const FinanceLinkSelection({this.goalId, this.goalTitle, this.debtId, this.debtTitle}); static const cleared = FinanceLinkSelection(); }` and `class FinanceLinkSheet extends StatelessWidget` taking `activeGoals`, `activeDebts`, `linkedGoalId`, `linkedDebtId` — reproduces the current sheet's `DraggableScrollableSheet` + "no link" `ListTile` + goals section + debts section exactly, but every `onTap` now calls `Navigator.of(ctx).pop(FinanceLinkSelection(...))` instead of calling `setState` on the caller. No `TextEditingController` involved, so no dispose risk — this is a pure `StatelessWidget`.

    Update `task_form_screen.dart`: `initState()` calls `loadFinanceLinks(...)` and applies the returned `FinanceLinksSnapshot` via one `setState`. `_pickFinanceLink()` becomes a ~15-line wrapper: `await showModalBottomSheet&lt;FinanceLinkSelection&gt;(..., builder: (_) => FinanceLinkSheet(...))`, then `if (selection != null &amp;&amp; mounted) setState(() { _linkedGoalId = selection.goalId; ... })`. `_save()` calls `buildFormItem(...)` then does only the cubit call + result handling (unify the create/update branches behind a small private `_persist(Item item)` returning the cubit's bool, and a `_handleSaveResult(bool ok)` helper for the shared snackbar-and-pop logic, to avoid duplicating the error-handling block twice). `_openGtdGuide()` calls `applyGtdResult(...)` and applies the record's fields in one `setState`.
  </action>
  <acceptance_criteria>
    - `grep -c "showModalBottomSheet" lib/presentation/tasks/form/screens/task_form_screen.dart` still reports 2 (finance link + GTD guide), confirming no sheet call was dropped.
    - `flutter analyze lib/presentation/tasks/form/` reports zero new errors or warnings.
    - `flutter test test/presentation/tasks/task_form_test.dart` passes unchanged.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/presentation/tasks/form/ && flutter test test/presentation/tasks/task_form_test.dart</automated>
  </verify>
  <done>task_form_logic.dart and finance_link_sheet.dart exist; task_form_screen.dart's save/finance-link/GTD-apply logic delegates to them; existing task_form_test.dart passes unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extract the remaining field-group widgets and verify the whole slice</name>
  <files>lib/presentation/tasks/form/widgets/title_type_card.dart, lib/presentation/tasks/form/widgets/schedule_fields.dart, lib/presentation/tasks/form/widgets/flags_size_notes_fields.dart, lib/presentation/tasks/form/screens/task_form_screen.dart, lib/presentation/tasks/form/README.md</files>
  <read_first>
    lib/presentation/tasks/form/screens/task_form_screen.dart (post-Task-1 state — re-read after Task 1's edits, do not rely on the pre-Task-1 line numbers)
    lib/presentation/tasks/form/widgets/form_primitives.dart (FormCard/FieldRow/FieldDivider — reuse these, do not redefine)
    lib/presentation/tasks/form/widgets/advanced_options_card.dart (the existing wrapper these new widgets become children of)
  </read_first>
  <action>
    Create `lib/presentation/tasks/form/widgets/title_type_card.dart`: `class TitleTypeCard extends StatelessWidget` wrapping the title `TextFormField` (using `titleController`, `isEditing` for autofocus, a `validator` callback) and the `ItemType` `SegmentedButton`, taking `titleController`, `isEditing`, `itemType`, `onItemTypeChanged` — moves this block out of `build()` unchanged in appearance.

    Create `lib/presentation/tasks/form/widgets/schedule_fields.dart`: `class ScheduleFields extends StatelessWidget` holding the priority `DropdownButtonFormField`, due-date `ListTile` (with clear/pick trailing icons), due-time `ListTile` (conditional on `dueDate != null`), and the recurrence `RadioListTile` list — takes `priority`, `dueDate`, `dueTime`, `recurrenceRule`, and callbacks `onPriorityChanged`, `onPickDate`, `onClearDate`, `onPickTime`, `onRecurrenceChanged`. Move `_priorityLabel` and `_recurrenceLabel` into this file as private top-level helpers (they are only used here).

    Create `lib/presentation/tasks/form/widgets/flags_size_notes_fields.dart`: `class FlagsSizeNotesFields extends StatelessWidget` holding the urgent/important `SwitchListTile`s, the `SizeCategory` `SegmentedButton`, the description `TextFormField`, and the waiting-for `TextFormField` — takes `isUrgent`, `isImportant`, `sizeCategory`, `descriptionController`, `waitingForController` (not owned — the screen still creates/disposes them), and change callbacks.

    Wire both new widgets as `AdvancedOptionsCard`'s children (replacing the inline field blocks) and `TitleTypeCard` in place of the inline title+type block. Re-measure `task_form_screen.dart`. If it is still over 150 lines after this, apply one additional cut per the research's fallback guidance: extract `initState`'s item-to-state-field mapping into a small `TaskFormInitialState` record returned by a `deriveInitialFormState(Item? item)` function in `task_form_logic.dart`, assigned in one block in `initState`. Do not stop until every file in the slice is ≤150 lines — this is a hard acceptance gate, not a target.

    Finally, update `lib/presentation/tasks/form/README.md`'s "Status" section: replace the "task_form_screen.dart is still 752 lines" note with the final compliant state, and add the four new widget files + `task_form_logic.dart` to the file/role tables (matching the existing table format).
  </action>
  <acceptance_criteria>
    - `for f in lib/presentation/tasks/form/screens/task_form_screen.dart lib/presentation/tasks/form/widgets/finance_link_sheet.dart lib/presentation/tasks/form/widgets/title_type_card.dart lib/presentation/tasks/form/widgets/schedule_fields.dart lib/presentation/tasks/form/widgets/flags_size_notes_fields.dart lib/presentation/tasks/form/task_form_logic.dart; do n=$(wc -l &lt; "$f"); [ "$n" -le 150 ] || echo "FAIL $f: $n"; done` prints nothing.
    - `flutter analyze lib/presentation/tasks/form/` reports zero new errors or warnings.
    - `flutter test test/presentation/tasks/task_form_test.dart test/presentation/tasks/gtd_test.dart test/presentation/tasks/gtd_decision_tree_test.dart` passes unchanged (test count ≥ existing baseline for these three files).
    - Manual verification (no dedicated controller-lifecycle test exists for this specific split): on-device or `flutter run`, open the task form in create mode, fill title/priority/due date/time/recurrence/urgent/important/size/description/waiting-for/finance-link, save, reopen the saved task, confirm every field persisted. Then edit and clear the due date, confirm no `TextEditingController used after being disposed` crash.
  </acceptance_criteria>
  <verify>
    <automated>flutter analyze lib/presentation/tasks/form/ && flutter test test/presentation/tasks/task_form_test.dart test/presentation/tasks/gtd_test.dart test/presentation/tasks/gtd_decision_tree_test.dart</automated>
  </verify>
  <done>All files in the tasks/form slice (excluding gtd/, already compliant) are ≤150 lines; README reflects final state; existing tests pass; manual smoke pass completed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure refactor of existing form UI; no new input parsing or persistence path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-06-02-01 | Tampering (data integrity) | `buildFormItem` in task_form_logic.dart | mitigate | Moved verbatim from the original `_save()` body, including the exact parentId-clearing invariant comment and `clearField` sentinel usage; existing `task_form_test.dart` re-run to confirm save behavior is byte-identical, not just visually similar. |
| T-06-02-02 | Tampering (UI state) | `FinanceLinkSheet` | mitigate | Converted from caller-mutating to pop-returning per the documented crash precedent in `finance/goals/README.md`; manual smoke pass specifically exercises open→pick→save. |
</threat_model>

<verification>
`flutter analyze lib/presentation/tasks/form/` clean. `flutter test test/presentation/tasks/task_form_test.dart test/presentation/tasks/gtd_test.dart test/presentation/tasks/gtd_decision_tree_test.dart` green. Every file in the slice ≤150 lines (`wc -l` loop). Manual on-device smoke pass for the untested save/finance-link paths.
</verification>

<success_criteria>
task_form_screen.dart and all newly extracted files are ≤150 lines; existing task form tests pass unchanged; the finance-link sheet follows the pop-then-mutate convention; README updated.
</success_criteria>

<output>
After completion, create `.planning/phases/06-architecture-compliance/06-02-SUMMARY.md`
</output>
