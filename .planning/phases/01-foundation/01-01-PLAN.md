---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - analysis_options.yaml
  - lib/main.dart
  - lib/app.dart
  - lib/core/config/app_config.dart
  - lib/core/constants/app_constants.dart
  - lib/core/constants/storage_keys.dart
  - android/app/src/main/kotlin/com/omeu/space/agenda/MainActivity.kt
  - android/app/build.gradle
  - .gitignore
autonomous: true
requirements:
  - DATA-01
  - UX-02

must_haves:
  truths:
    - "flutter create produces an Android + iOS only project (no web or desktop directories)"
    - "pubspec.yaml lists all Phase 1 direct and dev dependencies with locked versions"
    - "analysis_options.yaml enforces very_good_analysis strict rules"
    - "Layer directories exist in lib/ following the prescribed seven-layer structure"
    - "FlutterFragmentActivity is the base class for MainActivity.kt"
    - "AppConfig exposes schemaVersion, appName, packageName, and notification namespace constants"
    - "flutter analyze exits 0 with no issues"
  artifacts:
    - path: "pubspec.yaml"
      provides: "All locked dependency versions; correct package name and description"
      contains: "com.omeu.space.agenda"
    - path: "analysis_options.yaml"
      provides: "very_good_analysis lint ruleset"
      contains: "very_good_analysis"
    - path: "android/app/src/main/kotlin/com/omeu/space/agenda/MainActivity.kt"
      provides: "FlutterFragmentActivity base class"
      contains: "FlutterFragmentActivity"
    - path: "lib/core/config/app_config.dart"
      provides: "Compile-time constants"
      contains: "schemaVersion"
  key_links:
    - from: "pubspec.yaml"
      to: "analysis_options.yaml"
      via: "include: package:very_good_analysis/analysis_options.yaml"
      pattern: "very_good_analysis"
    - from: "android/app/src/main/kotlin/com/omeu/space/agenda/MainActivity.kt"
      to: "io.flutter.embedding.android.FlutterFragmentActivity"
      via: "class MainActivity extends FlutterFragmentActivity"
      pattern: "FlutterFragmentActivity"
---

<objective>
Scaffold a Flutter project (Android + iOS only) with the correct package identity, all Phase 1
dependencies pinned, strict linting active, seven-layer directory structure created, and
FlutterFragmentActivity substituted for the default MainActivity.

Purpose: Every subsequent plan builds on this scaffold. Getting package name, SDK floor,
layer directories, and the FlutterFragmentActivity swap right in Wave 1 prevents cascading
rework across Phases 2–5.

Output: A compilable Flutter shell (no feature logic) that passes `flutter analyze` and
establishes every architectural convention Phase 2 will inherit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold Flutter project, configure pubspec, analysis_options, and layer dirs</name>

  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01 through D-15, package name, SDK floor)
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack table, anti-patterns)
    - CLAUDE.md (What NOT to Use list — every prohibited package)
  </read_first>

  <files>
    pubspec.yaml,
    analysis_options.yaml,
    lib/main.dart,
    lib/app.dart,
    lib/core/config/app_config.dart,
    lib/core/constants/app_constants.dart,
    lib/core/constants/storage_keys.dart,
    lib/core/extensions/datetime_extensions.dart,
    lib/core/extensions/string_extensions.dart,
    lib/domain/.gitkeep,
    lib/data/.gitkeep,
    lib/infrastructure/.gitkeep,
    lib/application/.gitkeep,
    lib/presentation/.gitkeep,
    lib/config/.gitkeep
  </files>

  <action>
**Step 1 — Create the Flutter project (Android + iOS only):**
```bash
flutter create \
  --org com.omeu.space \
  --project-name agenda \
  --platforms android,ios \
  --description "Privacy-first personal HQ for tasks and finances" \
  .
```
This overwrites the default scaffold. Verify no `web/`, `linux/`, `windows/`, `macos/` directories exist after creation.

**Step 2 — Replace pubspec.yaml** with the following exact content (all versions locked per
RESEARCH.md Standard Stack and CLAUDE.md):

```yaml
name: agenda
description: "Privacy-first personal HQ for tasks and finances"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: '>=3.38.1'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Database — isar_community (NOT original isar — abandoned April 2023)
  isar_community: 3.3.2
  isar_community_flutter_libs: 3.3.2

  # State management
  flutter_bloc: 9.1.1
  bloc: 9.1.1
  equatable: 2.0.8

  # Dependency injection
  get_it: 9.2.1
  injectable: 2.7.1+4

  # Persistence and routing
  shared_preferences: 2.5.5
  go_router: 17.2.0
  path_provider: 2.1.5

  # Internationalization
  intl: 0.20.2

  # --- Phase 4 (Notifications + Backup) — uncomment when Phase 4 starts ---
  # flutter_local_notifications: 21.0.0
  # timezone: 0.11.0
  # flutter_timezone: 5.0.2
  # file_picker: 11.0.2
  # csv: 8.0.0

  # --- Phase 5 (App Lock) — uncomment when Phase 5 starts ---
  # flutter_screen_lock: 9.2.2+2
  # local_auth: 3.0.1
  # flutter_secure_storage: 10.0.0

  # --- Phase 3 (Finance Charts) — uncomment when Phase 3 starts ---
  # fl_chart: 1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0

  # Code generation
  isar_community_generator: 3.3.2
  build_runner: ^2.13.1
  injectable_generator: 2.12.1

  # Linting
  very_good_analysis: 10.2.0

  # Testing
  bloc_test: 10.0.0
  mocktail: 1.0.5

flutter:
  uses-material-design: true
  generate: true
```

Run `flutter pub get` after writing this file. Confirm exit 0.

**Step 3 — Write analysis_options.yaml** (project root):

```yaml
include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - lib/generated/**
    - lib/config/di/injection.config.dart
    - "**/*.g.dart"
  errors:
    missing_required_param: error
    missing_return: error
    todo: ignore

linter:
  rules:
    public_member_api_docs: false
```

The `exclude` list silences linter on generated files (Isar schemas, injectable output, l10n output). `public_member_api_docs: false` overrides the VGV default — internal app code does not need doc comments on every public member.

**Step 4 — Create layer directory stubs.** Create a `.gitkeep` file (empty) in each placeholder directory so git tracks the structure:
- `lib/domain/.gitkeep`
- `lib/data/.gitkeep`
- `lib/infrastructure/.gitkeep`
- `lib/application/.gitkeep`
- `lib/presentation/.gitkeep`
- `lib/config/.gitkeep`

Also create the immediate subdirs needed in this plan:
- `lib/core/config/`
- `lib/core/constants/`
- `lib/core/extensions/`

**Step 5 — Write lib/core/config/app_config.dart** (per D-09):

```dart
/// Compile-time application constants.
///
/// Access directly: `AppConfig.schemaVersion` — no DI registration needed.
abstract final class AppConfig {
  /// Application display name.
  static const String appName = 'AGENDA';

  /// Reverse-DNS package identifier.
  static const String packageName = 'com.omeu.space.agenda';

  /// Human-readable version string shown in Settings.
  static const String version = '1.0.0';

  /// Build number for platform stores.
  static const int buildNumber = 1;

  /// Current Isar schema version.
  ///
  /// Bump this constant AND add a corresponding migration block in
  /// [MigrationRunner._runMigration] every time the Isar schema changes.
  static const int schemaVersion = 1;

  // ---------------------------------------------------------------------------
  // Notification ID namespaces
  // Deterministic derivation: entityId * 10 + notificationType
  // Defined here in Phase 1 so Phase 4 can reference without circular dep.
  // ---------------------------------------------------------------------------

  /// Base multiplier for task notification IDs.
  static const int taskNotificationBase = 10;

  /// Base multiplier for finance notification IDs.
  static const int financeNotificationBase = 20;

  /// Base multiplier for system notification IDs (briefing, motivational).
  static const int systemNotificationBase = 30;
}
```

**Step 6 — Write lib/core/constants/app_constants.dart**:

```dart
/// Project-wide magic numbers and limits.
///
/// Prevents literal duplication across layers. All values are Dart constants.
abstract final class AppConstants {
  /// Duration of the undo snackbar for soft-deleted tasks (TASK-05).
  static const Duration undoSnackbarDuration = Duration(seconds: 5);

  /// Maximum number of iOS scheduled notifications (platform limit).
  /// Phase 4 notification manager must not exceed this.
  static const int iosMaxScheduledNotifications = 64;

  /// Budget alert threshold (80 %) — triggers NOTF-03 first alert.
  static const double budgetAlertThresholdLow = 0.80;

  /// Budget alert threshold (100 %) — triggers NOTF-03 second alert.
  static const double budgetAlertThresholdHigh = 1.00;

  /// Default quiet hours start (22:00) — NOTF-10.
  static const int quietHoursStartHour = 22;

  /// Default quiet hours end (07:00) — NOTF-10.
  static const int quietHoursEndHour = 7;

  /// 1-3-5 Rule slot counts — TASK-08.
  static const int rule135BigTasks = 1;
  static const int rule135MediumTasks = 3;
  static const int rule135SmallTasks = 5;
}
```

**Step 7 — Write lib/core/constants/storage_keys.dart**:

```dart
/// SharedPreferences key strings.
///
/// Centralised here so typos in key names are caught at compile time.
abstract final class StorageKeys {
  /// Isar schema version integer — read by [MigrationRunner] before Isar opens.
  static const String schemaVersion = 'schema_version';

  /// IETF language tag of the active locale ('en' or 'pt').
  /// Written by [LocaleCubit]; read on cold start before [runApp].
  static const String locale = 'locale';

  /// Whether the first-launch privacy statement has been shown (UX-03).
  static const String privacyStatementShown = 'privacy_statement_shown';

  /// Whether app lock PIN is configured (DATA-05).
  static const String pinConfigured = 'pin_configured';

  /// App lock timeout in seconds (0 = lock immediately on background).
  static const String lockTimeoutSeconds = 'lock_timeout_seconds';

  /// Quiet hours enabled flag (NOTF-10).
  static const String quietHoursEnabled = 'quiet_hours_enabled';

  /// Quiet hours start hour 0-23 (NOTF-10).
  static const String quietHoursStart = 'quiet_hours_start';

  /// Quiet hours end hour 0-23 (NOTF-10).
  static const String quietHoursEnd = 'quiet_hours_end';
}
```

**Step 8 — Write core extension stubs.**

`lib/core/extensions/datetime_extensions.dart`:
```dart
/// DateTime convenience extensions used across layers.
extension DateTimeExtensions on DateTime {
  /// Returns midnight (00:00:00.000) of this date.
  DateTime get startOfDay => DateTime(year, month, day);

  /// Returns 23:59:59.999 of this date.
  DateTime get endOfDay =>
      DateTime(year, month, day, 23, 59, 59, 999);

  /// True if this date falls on the same calendar day as [DateTime.now()].
  bool get isToday {
    final now = DateTime.now();
    return year == now.year && month == now.month && day == now.day;
  }

  /// True if this date is strictly before today (ignoring time).
  bool get isPast => startOfDay.isBefore(DateTime.now().startOfDay);

  /// True if this date is strictly after today (ignoring time).
  bool get isFuture => startOfDay.isAfter(DateTime.now().startOfDay);
}
```

`lib/core/extensions/string_extensions.dart`:
```dart
/// String convenience extensions used across layers.
extension StringExtensions on String {
  /// True when the string is empty after trimming whitespace.
  bool get isBlank => trim().isEmpty;

  /// Returns the string with its first character uppercased.
  String get capitalised =>
      isEmpty ? this : '${this[0].toUpperCase()}${substring(1)}';
}

/// Nullable String convenience extensions.
extension NullableStringExtensions on String? {
  /// True when the string is null or empty after trimming.
  bool get isNullOrBlank => this == null || this!.trim().isEmpty;
}
```

**Step 9 — Write lib/main.dart** (minimal shell — DI and runApp only):

```dart
import 'package:flutter/material.dart';

import 'app.dart';

Future<void> main() async {
  WidgetsFlutterBinding.ensureInitialized();
  // configureDependencies() wired in Plan 03 — placeholder comment only here.
  runApp(const AgendaApp());
}
```

**Step 10 — Write lib/app.dart** (bare MaterialApp — l10n and routing wired in Plans 03/04):

```dart
import 'package:flutter/material.dart';

/// Root application widget.
///
/// Locale, routing, and BlocProviders are wired in later plans.
class AgendaApp extends StatelessWidget {
  const AgendaApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'AGENDA',
      theme: ThemeData(
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.deepPurple),
        useMaterial3: true,
      ),
      home: const Scaffold(
        body: Center(child: Text('AGENDA')),
      ),
    );
  }
}
```

**Step 11 — Verify `flutter analyze` exits 0:**
```bash
flutter analyze
```
Fix any issues before moving to acceptance criteria checks.
  </action>

  <verify>
    <automated>flutter analyze --no-fatal-infos</automated>
  </verify>

  <acceptance_criteria>
    - `pubspec.yaml` contains `isar_community: 3.3.2` (not `isar:`)
    - `pubspec.yaml` contains `very_good_analysis: 10.2.0` under dev_dependencies
    - `pubspec.yaml` contains `flutter: '>=3.38.1'` in the environment block
    - `pubspec.yaml` contains `com.omeu.space.agenda` nowhere in itself (that lives in Android manifest, not pubspec), but `name: agenda` is present
    - `analysis_options.yaml` contains `include: package:very_good_analysis/analysis_options.yaml`
    - `analysis_options.yaml` contains `lib/generated/**` in the exclude list
    - `lib/core/config/app_config.dart` contains `static const int schemaVersion = 1`
    - `lib/core/config/app_config.dart` contains `static const String appName = 'AGENDA'`
    - `lib/core/constants/storage_keys.dart` contains `static const String schemaVersion = 'schema_version'`
    - `lib/core/constants/app_constants.dart` contains `static const Duration undoSnackbarDuration = Duration(seconds: 5)`
    - `lib/domain/.gitkeep` exists (directory tracked by git)
    - `lib/infrastructure/.gitkeep` exists
    - `lib/application/.gitkeep` exists
    - `lib/presentation/.gitkeep` exists
    - `lib/config/.gitkeep` exists
    - `flutter analyze --no-fatal-infos` exits 0
    - No `web/`, `linux/`, `windows/`, `macos/` directories exist at project root
  </acceptance_criteria>

  <done>
    Flutter project scaffolded for Android + iOS only; pubspec locked to all Phase 1 versions;
    very_good_analysis linting active; seven-layer directory structure exists; core constants,
    AppConfig, StorageKeys, and extension files written; flutter analyze exits 0.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace FlutterActivity with FlutterFragmentActivity in MainActivity.kt</name>

  <read_first>
    - android/app/src/main/kotlin/com/omeu/space/agenda/MainActivity.kt (current content)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-19 — FlutterFragmentActivity requirement)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pitfall 6 — FlutterFragmentActivity)
  </read_first>

  <files>
    android/app/src/main/kotlin/com/omeu/space/agenda/MainActivity.kt
  </files>

  <action>
Read the current content of `android/app/src/main/kotlin/com/omeu/space/agenda/MainActivity.kt`.

Replace the entire file with:

```kotlin
package com.omeu.space.agenda

import io.flutter.embedding.android.FlutterFragmentActivity

/**
 * Main activity for AGENDA.
 *
 * Extends [FlutterFragmentActivity] (not [io.flutter.embedding.android.FlutterActivity])
 * because [local_auth] requires a FragmentActivity context for biometric authentication
 * (DATA-06). Changing this after Phase 5 biometric code is written would require retesting
 * all fragment-dependent plugins. Do not revert to FlutterActivity.
 *
 * See: pub.dev/packages/local_auth README — "Android integration"
 * See: AGENDA CONTEXT.md D-19
 */
class MainActivity : FlutterFragmentActivity()
```

The file must import `io.flutter.embedding.android.FlutterFragmentActivity`, not
`io.flutter.embedding.android.FlutterActivity`. The class body is empty — Flutter handles
the rest via the embedding.
  </action>

  <verify>
    <automated>grep -c "FlutterFragmentActivity" android/app/src/main/kotlin/com/omeu/space/agenda/MainActivity.kt</automated>
  </verify>

  <acceptance_criteria>
    - `android/app/src/main/kotlin/com/omeu/space/agenda/MainActivity.kt` contains `FlutterFragmentActivity` (not `FlutterActivity`)
    - The import line is `import io.flutter.embedding.android.FlutterFragmentActivity`
    - The class declaration is `class MainActivity : FlutterFragmentActivity()`
    - `flutter analyze --no-fatal-infos` still exits 0 after this change
  </acceptance_criteria>

  <done>
    MainActivity.kt extends FlutterFragmentActivity. The local_auth biometric prerequisite
    (D-19, DATA-06) is satisfied before any auth code is written. This prevents a
    PlatformException in Phase 5 that would require retesting all fragment-dependent plugins.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pubspec → package registry | Dependency version pinning is the only external interaction in this plan |
| Kotlin layer boundary | MainActivity.kt interacts with the Flutter embedding framework |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | pubspec.yaml — dependency versions | mitigate | All versions are locked (no `any` or `^` on security-critical packages); prohibited packages documented in CLAUDE.md and checked during analysis |
| T-01-02 | Information Disclosure | pubspec.yaml — no firebase/sentry/http | mitigate | `flutter analyze` + CLAUDE.md What NOT to Use list; analysis_options.yaml excludes no network-related packages from linting |
| T-01-03 | Elevation of Privilege | MainActivity.kt — FlutterFragmentActivity | accept | Change is intentional and required for local_auth (DATA-06); does not expose additional attack surface vs FlutterActivity |
| T-01-04 | Tampering | analysis_options.yaml — linter exclusions | mitigate | Only generated files excluded (`lib/generated/**`, `*.g.dart`, `injection.config.dart`); production source is fully linted |
</threat_model>

<verification>
After both tasks complete:

```bash
# Lint
flutter analyze --no-fatal-infos

# Layer structure
ls lib/core lib/domain lib/data lib/infrastructure lib/application lib/presentation lib/config

# Package identity
grep "name: agenda" pubspec.yaml
grep "isar_community: 3.3.2" pubspec.yaml
grep "FlutterFragmentActivity" android/app/src/main/kotlin/com/omeu/space/agenda/MainActivity.kt

# No forbidden packages
grep -E "isar:|isar_flutter_libs:|firebase_|sentry_flutter|riverpod|provider|hive|mockito|easy_localization|slang|\"get\":" pubspec.yaml
# Expected: no output (none of these should appear)

# SDK floor
grep "flutter: '>=" pubspec.yaml
# Expected: flutter: '>=3.38.1'
```
</verification>

<success_criteria>
1. `flutter analyze --no-fatal-infos` exits 0 — no lint violations
2. `pubspec.yaml` lists `isar_community: 3.3.2` (not abandoned `isar:`), all Phase 1 packages at locked versions, `flutter: '>=3.38.1'`
3. Seven layer directories (`core/`, `domain/`, `data/`, `infrastructure/`, `application/`, `presentation/`, `config/`) exist under `lib/`
4. `MainActivity.kt` extends `FlutterFragmentActivity` — biometric prerequisite satisfied
5. `AppConfig.schemaVersion == 1`, `AppConfig.appName == 'AGENDA'`, notification namespace constants defined
6. No web, linux, windows, or macOS directories in project root
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Packages added and their locked versions
- Directory structure created
- FlutterFragmentActivity swap confirmed
- Any lint issues encountered and resolved
- `flutter analyze` exit status
</output>
